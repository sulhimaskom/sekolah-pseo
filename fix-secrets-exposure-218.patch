diff --git a/.github/workflows/on-pull.yml b/.github/workflows/on-pull.yml
index 62d507a..6aa537f 100644
--- a/.github/workflows/on-pull.yml
+++ b/.github/workflows/on-pull.yml
@@ -23,13 +23,6 @@ jobs:
     runs-on: ubuntu-24.04-arm
     timeout-minutes: 120
 
-    env:
-      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-      IFLOW_API_KEY: ${{ secrets.IFLOW_API_KEY }}
-      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
-      VITE_SUPABASE_KEY: ${{ secrets.VITE_SUPABASE_KEY }}
-      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
-
     steps:
       - name: Wait in Queue
         uses: softprops/turnstyle@v3
diff --git a/.github/workflows/on-push.yml b/.github/workflows/on-push.yml
index e4b1241..2be254a 100644
--- a/.github/workflows/on-push.yml
+++ b/.github/workflows/on-push.yml
@@ -7,6 +7,17 @@ permissions:
   contents: write
   pull-requests: write
 
+x-agent-env: &agent_env
+  IFLOW_API_KEY: ${{ secrets.IFLOW_API_KEY }}
+  VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
+  VITE_SUPABASE_KEY: ${{ secrets.VITE_SUPABASE_KEY }}
+  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
+  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
+  GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
+  API_KEY: ${{ secrets.GEMINI_API_KEY }}
+  SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
+  VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_KEY }}
+
 concurrency:
   group: global
   cancel-in-progress: false
@@ -15,18 +26,6 @@ jobs:
   analyze:
     name: on-push
     runs-on: ubuntu-24.04-arm
-    env:
-      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-      IFLOW_API_KEY: ${{ secrets.IFLOW_API_KEY }}
-      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
-      VITE_SUPABASE_KEY: ${{ secrets.VITE_SUPABASE_KEY }}
-      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
-      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
-      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
-      API_KEY: ${{ secrets.GEMINI_API_KEY }}
-      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
-      VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_KEY }}
-
     steps:
       - name: Wait in Queue
         uses: softprops/turnstyle@v3
@@ -72,6 +71,7 @@ jobs:
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
 
       - name: 00 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/00.md)" \
@@ -80,6 +80,7 @@ jobs:
             --share false
 
       - name: 01 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/01.md)" \
@@ -88,6 +89,7 @@ jobs:
             --share false
 
       - name: 02 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/02.md)" \
@@ -96,6 +98,7 @@ jobs:
             --share false
 
       - name: 03 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/03.md)" \
@@ -104,6 +107,7 @@ jobs:
             --share false
 
       - name: 04 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/04.md)" \
@@ -112,6 +116,7 @@ jobs:
             --share false
 
       - name: 05 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/05.md)" \
@@ -120,6 +125,7 @@ jobs:
             --share false
 
       - name: 06 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/06.md)" \
@@ -128,6 +134,7 @@ jobs:
             --share false
 
       - name: 07 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/07.md)" \
@@ -136,6 +143,7 @@ jobs:
             --share false
 
       - name: 08 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/08.md)" \
@@ -144,6 +152,7 @@ jobs:
             --share false
 
       - name: 09 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/09.md)" \
@@ -152,6 +161,7 @@ jobs:
             --share false
 
       - name: 10 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/10.md)" \
@@ -160,6 +170,7 @@ jobs:
             --share false
 
       - name: 11 flow
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat .github/prompt/11.md)" \
@@ -169,6 +180,7 @@ jobs:
 
       - name: on-push
         if: steps.check_issues.outputs.open_issue_count == '0'
+        env: *agent_env
         timeout-minutes: 120
         run: |
           timeout -k 1m 90m opencode run "$(cat <<'PROMPT'
