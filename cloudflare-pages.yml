name: Sekolah PSEO
compatibility_date: '2024-01-01'

pages:
  - name: Sekolah PSEO
    build:
      command: npm run build
      cwd: /
    env:
      NODE_VERSION: '20'
    kv_namespaces:
      - binding: SCHOOL_DATA
        id: your-kv-namespace-id
    limits:
      cpu_ms: 10000
