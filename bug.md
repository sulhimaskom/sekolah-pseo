# Bug Report

- [x] bug: `scripts/config.test.js` fails because it hardcodes the directory name `sekolah-pseo` for `ROOT_DIR` validation.
- [x] bug: "Copy to Clipboard" for NPSN is missing in `src/presenters/templates/school-page.js`, which is mentioned as a requirement in memory.
