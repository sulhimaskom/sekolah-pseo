# Bugs found by BugLover

- [x] bug: `scripts/config.test.js` failing ROOT_DIR check because it expects a hardcoded directory name.
- [x] bug: `scripts/utils.js` prefixes negative numbers with a single quote in CSV output, causing data validation errors.
