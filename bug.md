# Bug List

- [x] bug: Fragile `ROOT_DIR` test in `scripts/config.test.js` fails when the project directory is not named `sekolah-pseo`.
- [x] bug: Missing `globals` dependency causing ESLint to fail.
