# Phase 1 — Diagnostic & Comprehensive Scoring Report (268th verification, 2026-08-19)

**Evaluation Date**: 2026-08-19
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo
**Default Branch**: main (`866af3d` — 267th-run ledger, post-PR #829 merge; HEAD synced with origin/main)
**Trigger**: `ulw-loop` — Phase 0 probe: **1 open PR (#829, docs-led)** → PR HANDLER MODE (merged) → re-probe **0 open PRs / 0 open issues** → Phase 0.3 EMPTY → PHASE 1 (AUDIT MODE, read-only + ledger output). This is the **268th verification run** in the repo's ledger (prior record: 30-audit-report-2026-08-19-267th).
**Mode**: Full command matrix re-executed fresh on `866af3d`. No cached results.

## Skills used (contract §5)

Surveyed available skills — `.opencode/skills` directory does not exist in this repo (matches prior-run observations); `.opencode/skill/` holds 7 user-installed engineering skills (madappgang-claude-code-debugging-strategies, maxritter-claude-codepro-backend-models-standards, modu-ai-moai-adk-moai-tool-opencode, muratcankoylan-agent-skills-for-context-engineering-memory-systems, obra-superpowers-systematic-debugging, proffesor-for-testing-agentic-qe-skill-builder, vasilyu1983-ai-agents-public-git-commit-message). `git-master` (built-in) was **loaded and registered** for the PR-handler commit/merge workflow; `vasilyu1983-ai-agents-public-git-commit-message` is registered as the repo's commit-message convention skill (the repository already uses the established ledger-commit convention). The repo's own deterministic security gate (`scripts/check-workflow-security.js`) was executed directly as the security probe. Established verification discipline applied directly, per the 250th–267th-run precedent.

## Delegation self-check (contract §6)

No subagent delegation was used this run. All probes were **direct, deterministic single-source operations** (exit-code captures, targeted greps, file reads, API queries) that covered the held-finding scope faster than background agents and produced line-addressable evidence — consistent with the 250th–267th-run precedent:
- **Phase 0 probe (direct)**: `gh pr list --state open` (1 → PR #829) → PR HANDLER MODE → merged #829 (`866af3d`, squash, remote branch auto-deleted) → re-probe `gh pr list` (0) + `gh issue list` (0) → Phase 0.3 EMPTY → Phase 1. PR handler activity occurred this run (merged #829).
- **PR #829 verification (direct, pre-merge)**: branch 0 behind / 1 ahead of main (no rebase needed); diff = 3 docs-only ledger files (281 insertions, 0 deletions); lint 0/0; build 0 (2 pages, budgets PASS); test:js 1334/0/4skip; test:py 27/27; npm ci 0 vulns; labels docs + P3 present; no comments/reviews unresolved; no required status checks configured. Merged per merge conditions (all green), `--admin` per contract.
- **Held-finding re-verification (direct)**: F225 (4 import edges, src/services/BuildOrchestrator.js:52-56 requires ../../scripts/{manifest, build-performance, enrichment, sitemap}), F237 (6 `generate*Pages` refs in scripts/build-pages.js + 12 `build*PageData` refs in src/services/PageBuilder.js), F236 (3 quality implementations), F229 (`0056ad8` unreachable; **unhardened `curl|bash` installer present in BOTH on-pull.yml:63 AND on-push.yml:63**), F231 (build-performance.js:344 monitorBuild report-after-stop), F232 (manifest.js:130-140 lat/lon in hash), F234 (fetch-data.js:183-198 EXTERNAL_DATA_DIR validation), F233 (IntegrationError.details at src/core/resilience.js:15-32), F239 (api.md:3888 Comparison Module + api.md:6627 Test Helpers Module sections present), F251 (data-schema.test.js prettier-clean), F228 (README.md:305-319 / SECURITY.md:46-50 / deployment.md accurate), F230 (blueprint.md/task.md prettier-clean), F246-F250 (footer year injectable src/presenters/templates/shared/footer.js:24; no `/provinsi/undefined/` path at source — only test-assertion occurrences in scripts/homepage.test.js:710/712; isValidCoordinate at data-schema.js:222; escape-only reset src/presenters/templates/homepage.js; strict coordinate parse + npsn dedupe data-schema.js:52-57/139) — **all verified holding**.
- **F004 secrets re-count (direct)**: **57 `secrets.*` refs / 10 unique names** across `.github/workflows/*.yml` — byte-identical to 267th basis (0 workflow commits since `e00195f`), **zero growth**.
- **CI probes (direct, GitHub API)**: on-pull **29/29 recent completed successes, 0 failures** (last 30 runs; 1 in-progress — this run's PR-merge-triggered) — **F063 streak ≥24 maintained**; orchestrator **6/6 recent FAILED** (latest 2026-08-19T00:50:16Z) — F038 failure window held (~66+ days).
- **F002 issue-create probe (direct, GraphQL)**: repository node id resolved (`R_kgDOQKx7JA`), `createIssue` → **FORBIDDEN "Resource not accessible by integration" — 403 CONCLUSIVE (70th)** — token lacks `issues: write`; ledger docs are the output channel.

## Command matrix (all fresh on `866af3d`, exit codes recorded)

| Command                                    | Result | Evidence |
| ------------------------------------------ | ------ | -------- |
| `npm ci`                                   | ✅ 0   | deps installed; **0 vulnerabilities** (F028 maintained RESOLVED); EBADENGINE warning surfaced again (lint-staged@17.3.0 wants node ≥22.22.1 vs runtime v20.20.2 — F064 held) |
| `npm run lint`                             | ✅ 0   | eslint: **0 errors / 0 warnings** |
| `npm run build`                            | ✅ 0   | 2 pages, 0 failed, budgets PASS (37.74 pages/sec — flat vs 267th) |
| `npm run test:js`                          | ✅ 0   | **1334 pass / 0 fail / 4 skipped** (1338 total; flat vs 267th — PR handler merged docs-only, no tests added) |
| `npm run test:js:coverage`                 | ✅ 0   | **97.38% stmt / 93.57% branch / 99.58% funcs** — above 80/75 thresholds (flat vs 267th) |
| `npm run test:py`                          | ✅ 0   | "All tests passed!" (run_tests.py wrapper suite, 27/27) |
| `python3 -m pytest --version`              | ❌ 1   | **UNAVAILABLE this run** — `No module named pytest` in the fresh runner (no `pip install -r requirements.txt` executed this run) — **14th obs: env-parity continued unavailable** (13th obs was 267th) |
| `npm run format:check`                     | ❌ 1   | **199 files fail** — 198 `docs/issues/` ledger + `SECURITY_AUDIT_NOTE.md`; **zero source files** (F005, **116th obs**; F251 verified holding — see below). Ledger-only drift by convention; grew by 3 from the 267th ledger merged in #829 |
| `npx prettier --check scripts/data-schema.test.js` | ✅ 0 | **F251 FIXED verified holding (19th clean obs)** — the 249th-run PR-handler fix continues to hold the "zero source drift" invariant |
| `npx prettier --check docs/blueprint.md docs/task.md` | ✅ 0 | **F230 maintained RESOLVED (32nd clean obs)** |
| `node scripts/check-workflow-security.js`  | ❌ 1   | **12 violations**: 2 CRITICAL (DUPLICATE_API_KEY ×2 — on-push.yml, parallel.yml) + 10 HIGH (4× ID_TOKEN_WRITE + 4× ACTIONS_WRITE_NON_MERGE + 2× GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN) — F037, **169th observation**. Still ungated (F227) |
| `npm audit`                                | ✅ 0   | **0 vulnerabilities** (F028 maintained RESOLVED) |
| `npm run check-freshness`                  | ❌ 1   | **STALE — 30 days** (last update 2026-07-20, threshold 7; 2 records) — F018 HELD (held at 30d, same-day re-observation); SITE_URL placeholder warning surfaced (F025 held) |
| CI probe (on-pull, last 30)                | ✅     | **29/29 completed successes visible, 0 failures + 1 in-progress (this run's PR-merge-triggered)** — **F063 HELD (IMPROVING: completed-success streak ≥24 maintained)**; no NEW failure |
| CI probe (orchestrator)                    | ❌     | **6/6 most recent runs FAILED** (latest 2026-08-19T00:50:16Z — **HELD, ~66+ days**) |
| F004 re-count (`secrets.*`)                | ❌     | **57 refs / 10 unique names** in `.github/workflows/*.yml` — **zero growth** (byte-identical to 267th basis; 0 workflow commits since `e00195f`) |
| F005 re-count (`prettier --check .`)       | ❌     | **199 files** (198 ledger + SECURITY_AUDIT_NOTE.md); **116th consecutive obs**; zero source files (F251 verified holding, 19th clean obs) |
| F011 tag count                             | ❌     | 0 tags — no releases (held) |
| F025 site probe                            | ❌     | src/core/config.js:56-60 SITE_URL default `https://example.com` placeholder re-observed (held; warning surfaced during check-freshness) |
| F028 audit probe (`npm audit`)             | ✅     | **RESOLVED maintained** — npm audit **0 vulnerabilities** |
| F029 side-effect trace (`git status`)      | ✅     | **NOT re-observed** — working tree clean after full suite (0 modified files) |
| F063 CI streak probe                       | ✅     | on-pull: **29/29 completed successes in last 30 runs, 0 failures** + 1 in-progress (streak ≥24 maintained) |
| F064 runtime drift probe                   | ❌     | `.nvmrc` 22 vs runtime node v20.20.2 vs CI node 20 vs devcontainer node 20; **lint-staged EBADENGINE (node ≥22.22.1 required)** at install (held; Node 20 EOL since April 2026); **pytest availability: UNAVAILABLE this run (14th obs — continued unavailable; no `pip install` executed in the fresh runner)** |
| F225 layer-inversion probe                 | ❌     | `src/services/BuildOrchestrator.js:52-56` requires `../../scripts/{manifest, build-performance, enrichment, sitemap}` — services→controllers inversion (4 import edges, held) |
| F227 no-CI-gates probe                     | ❌     | no lint/build/test/audit steps in any workflow; `npm ci || true` fail-open (parallel.yml:72,347) (held) |
| F228 docs-drift probe                      | ✅     | **RESOLVED maintained (30th clean obs at source)** — README.md:305-319 (12-violation note matches current count) / SECURITY.md:46-50 / deployment.md accurate at HEAD `866af3d` |
| F229 TASK-102 fix probe                    | ❌     | **HELD** — commit `0056ad8` still UNREACHABLE (**33rd obs**); unhardened `curl -fsSL https://opencode.ai/install | bash` (no pipefail/retry) present in **on-pull.yml:63 AND on-push.yml:63** |
| F230 prettier-fix probe                    | ✅     | **RESOLVED maintained (32nd clean obs)** — `prettier --check docs/blueprint.md docs/task.md` clean at HEAD |
| F231/F232/F234 fix probes                  | ✅     | **All 3 247th/249th-run fixes verified holding (22nd clean obs)** — monitorBuild report-after-stop (build-performance.js:344); manifest hash includes lat/lon (manifest.js:130-140); fetch-data.js EXTERNAL_DATA_DIR validation (fetch-data.js:183-198) |
| F233 cause-preservation probe              | ✅     | `IntegrationError.details` verified at source (src/core/resilience.js:15-32); full rethrow-of-non-transient decision still deferred (held) |
| F236 duplication probe                     | ❌     | three data-quality implementations: check-freshness.js / data-quality.js / freshness-report.js (each with .test.js) (held) |
| F237 duplication probe                     | ❌     | 6 `generate*Pages` refs in scripts/build-pages.js + 12 `build*PageData` refs in src/services/PageBuilder.js (held) |
| F239 api.md coverage probe                 | ✅     | **FIXED maintained (18th obs)** — `Comparison Module` (api.md:3888) + `Test Helpers Module` (api.md:6627) sections present; all tree-listed modules covered |
| F242 release-process probe                 | ❌     | release.md documents SemVer process but no release automation exists (F011 0 tags corroborates) — held |
| F245 inline-`var` probe                    | ❌     | 80 ES5 `var` occurrences in inline client JS (src/presenters/templates/homepage.js `<script>` block) — count method matches prior runs (held) |
| F246/F247/F248/F249/F250 fix probes        | ✅     | All 5 248th-run hardening fixes verified holding at source (footer.js:24/30 year injectable; zero `/provinsi/undefined/` at source — only test-assertion refs at scripts/homepage.test.js:710/712; data-schema.js:222 isValidCoordinate; homepage.js:714 escape-only reset semantics; data-schema.js:52-57/139 strict coordinate parse + npsn dedupe) |
| F002 issue-create probe                    | ❌     | **GraphQL "Resource not accessible by integration" (createIssue) — 403 CONCLUSIVE (70th)** — token lacks `issues: write`; GitHub-native issue creation remains blocked → ledger docs are the output channel |

## Domain score table (composite **70.3**, Δ **±0.0** vs 267th)

| Domain                                | Score    | Grade | vs 267th |
| ------------------------------------- | -------- | ----- | -------- |
| **A. Code Quality**                   | 80.0/100 | B-    | **±0.0** |
| **B. System Quality**                 | 71.5/100 | C     | **±0.0** |
| **C. Experience Quality**             | 79.0/100 | C+    | **±0.0** |
| **D. Delivery & Evolution Readiness** | 50.5/100 | F     | **±0.0** |
| **Composite**                         | **70.3** | C     | **±0.0** |

Composite = simple mean: (80.0 + 71.5 + 79.0 + 50.5) / 4 = 281.0 / 4 = 70.25 → **70.3** (rounding convention per prior records).

**Why movement this run**: **Flat run (PR handler merged docs-only ledger PR #829; Phase 1 read-only verification)**. PR handler activity confirmed healthy merge path (#829: synced, green, merged, branch auto-deleted). F239 maintained (18th obs), F251 verified holding (19th clean obs), F231/F232/F234 held (22nd obs), F228 (30th) and F230 (32nd) maintained RESOLVED. No new findings, no code fixes. Only observable deltas: ledger grew by 3 prettier-unclean files (F005: 196 → 199, all ledger + SECURITY_AUDIT_NOTE.md; zero source), pytest availability continued UNAVAILABLE this run (14th obs — no `pip install` executed in the fresh runner; F064-adjacent env-parity), F063 completed-success streak maintained ≥24 (29/29 visible), F038 failure window held (~66+ days), F229 unreachable 32nd → 33rd obs, F037 168th → 169th obs, F002 69th → 70th obs.

## Criteria-level breakdown (268th basis)

Scoring method: criterion score 0–100; domain score = Σ(score × weight) / 100 (42nd-run convention, weights sum to 100 per domain).

### A. Code Quality — 80.0/100 (Δ ±0.0)

| Criterion             | Weight | Score | Evidence / rationale |
| --------------------- | ------ | ----- | -------------------- |
| Correctness           | 15     | 90    | F231/F232/F234 verified holding (22nd clean obs); suite 1334 pass / 0 fail flat; no new runtime defects |
| Readability & Naming  | 10     | 75    | Consistent snake_case scripts + camelCase services; JSDoc throughout (held) |
| Simplicity            | 10     | 75    | F236 held — three divergent data-quality implementations |
| Modularity & SRP      | 15     | 80    | F237/F225 held — generate*Pages/build*PageData duplication, services→controllers inversion (4 edges) |
| Consistency           | 5      | 70    | **F251 verified holding (19th clean obs)** — `scripts/data-schema.test.js` prettier-clean; zero source drift. F245 held (80 ES5 `var` sites in inline client scripts) |
| Testability           | 15     | 92    | 1334 tests, coverage 97.38/93.57/99.58; require.main gate verified holding |
| Maintainability       | 10     | 65    | Largest files held (homepage.js 727L, BuildOrchestrator.js 715L, utils.js 450L); duplication cluster (F236/F237) |
| Error Handling        | 10     | 78    | F233 cause-preservation verified holding (IntegrationError.details at src/core/resilience.js:15-32); full rethrow-of-non-transient decision still deferred |
| Dependency Discipline | 5      | 80    | 1 runtime dep (pino); 0 vulns (F028); requirements.txt floating `>=` ranges (held); **pytest env availability continued UNAVAILABLE this run (F064-adjacent env parity, 14th obs)** |
| Determinism           | 5      | 65    | F246 footer-year injectable verified holding; freshness-report/check-freshness timezone dependence held |

### B. System Quality — 71.5/100 (Δ ±0.0)

| Criterion                    | Weight | Score | Evidence / rationale |
| ---------------------------- | ------ | ----- | -------------------- |
| Stability                    | 20     | 52    | F063 streak ≥24 maintained (29/29 visible, no new failure); F038 orchestrator 6/6 failed ~66+ days caps this criterion |
| Performance Efficiency       | 15     | 80    | F231 verified holding — returned metrics real; build 37.74 pages/sec, budgets PASS |
| Security Practices           | 20     | 58    | F234 verified holding (EXTERNAL_DATA_DIR injection closed); F037 12 violations held (169th obs); npm audit 0 vulns; NO audit step in CI (F227); F252 unpinned mutable action tags + curl\|bash held (on-pull.yml:63 + on-push.yml:63); F253 --admin self-merge + validator blind spots held |
| Scalability Readiness        | 15     | 73    | Sitemap chunking (50k cap); incremental manifest (F232 holding); single-threaded build is the limit |
| Resilience & Fault Tolerance | 15     | 90    | F233 cause preserved + F249 reset payload truthful; TASK-100 hardening held (jitter, Retry-After, HALF_OPEN single-probe, abort-on-timeout) |
| Observability                | 15     | 87    | pino structured logs; build performance report; freshness report; GITHUB_STEP_SUMMARY; no tracing (held) |

### C. Experience Quality — 79.0/100 (Δ ±0.0)

| Criterion                  | Weight | Score | Evidence / rationale |
| -------------------------- | ------ | ----- | -------------------- |
| Accessibility              | 10     | 80    | comparison.js accessible (FEAT-005); semantic HTML templates (held) |
| User Flow Clarity          | 10     | 83    | F247 verified holding — search-result fallback deterministic (no `/provinsi/undefined/` path at source) |
| Feedback & Error Messaging | 10     | 70    | CLI interactive menu; pino logs; F025 SITE_URL placeholder degrades prod URLs silently (held) |
| Responsiveness             | 10     | 70    | Static site — inherently fast; dark-mode + print CSS present (held) |
| API Clarity (DX)           | 12     | 68    | **F239 maintained (18th obs)** — all tree-listed modules have api.md sections (Comparison at api.md:3888 + Test Helpers at api.md:6627 present at HEAD) |
| Local Dev Setup (DX)       | 12     | 58    | F240 held — husky hooks not wired (`"prepare": "husky"` absent); two competing hook systems; pytest env flip-flop (F064-adjacent — UNAVAILABLE again this run, 14th obs) |
| Documentation Accuracy (DX)| 10     | 77    | **F239 maintained + F251 verified holding**; F228 maintained RESOLVED (30th clean obs); F005 ledger-only drift by convention |
| Debuggability (DX)         | 8      | 81    | pino structured logs + verbose flags; no tracing (held) |
| Build/Test Feedback Loop (DX) | 8    | 88    | Local loop: lint/build/test/coverage all < 15s; CI loop slow/unreliable (F063 improving, F038 held) |

### D. Delivery & Evolution Readiness — 50.5/100 (Δ ±0.0)

| Criterion                    | Weight | Score | Evidence / rationale |
| ---------------------------- | ------ | ----- | -------------------- |
| CI/CD Health                 | 20     | 25    | F227 no gates; F038 orchestrator 6/6 failed ~66+ days; F063 window maintained (≥24 consecutive successes, 29/29 visible) but flake root cause unfixed (F229 unreachable, 33rd obs); F252 supply-chain hardening gap |
| Release & Rollback Safety    | 20     | 50    | F011 0 tags / no release process; F242 verified holding (release.md documents process, automation absent); F253 --admin self-merge + auto-merge-if-slow instructions |
| Config & Env Parity          | 15     | 60    | `.env.example` present (7 vars); EXTERNAL_DATA_DIR + PERF_* vars undocumented; F025 SITE_URL placeholder; F064 `.nvmrc` 22 vs node 20 drift; pytest availability flip-flops across runner envs (this run: unavailable — 14th obs) |
| Migration Safety             | 15     | 67    | No DB migrations (flat CSV pipeline); NPSN uniqueness constraint held; F232 verified holding (stale-page migration hazard closed) |
| Technical Debt Exposure      | 15     | 60    | F005 pure ledger-only at 199 files (116th obs; zero source — F251 19th clean obs); F007 workflow YAML 2045L outlier; F037 held (F050 push-block, 45th documented pass) |
| Change Velocity & Blast Radius | 15    | 60    | PR flow healthy (828+ merged — #829 merged this run); single-branch workflow keeps blast radius contained; CI feedback slow (F063/F038); F050 blocks all workflow-file delivery |

## Global penalty rules (automatic)

- Build failure → −20: **NOT triggered** (build PASS, 2/2 pages, budgets met)
- Test failure → −15: **NOT triggered** (1334 JS + 27 Python wrapper, 0 fail; pytest unavailable in this runner is a documented env-parity observation, not a code defect)
- Critical vulnerability → −20: **NOT triggered** (npm audit 0 vulnerabilities; F028 maintained RESOLVED)

## Environment parity note (pytest unavailable again — 14th obs, continued unavailable)

The direct pytest path (`npm run test:py:pytest` / `npm run test:all`) is **environment-dependent**: available in the 250th run's runner, unavailable in the 251st (`No module named pytest`), available again in the 252nd, unavailable in the 253rd, available in the 254th, unavailable in the 255th–263rd (**9 consecutive**), **AVAILABLE in the 264th — `pip install -r requirements.txt` succeeded on first attempt**, UNAVAILABLE in the 265th (fresh runner, no `pip install` executed), UNAVAILABLE in the 266th (fresh runner, no `pip install` executed — 12th obs; flip back), UNAVAILABLE in the 267th (fresh runner, no `pip install` executed — 13th obs), and **UNAVAILABLE in the 268th — fresh runner, `No module named pytest`, no `pip install` executed (14th obs; continued unavailable)**. The repo's CI-safe path is `npm run test:ci` → `npm run test:py` (`tests/run_tests.py` stdlib wrapper, 27/27 pass). Recorded as an F064-adjacent environment-parity observation (floating `>=` ranges in requirements.txt; no `pip install` step in any workflow; runner images differ).