# Issue Records — 50th batch (91st verification run, 2026-08-08)

**Evaluation Date**: 2026-08-08
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`c85cb30`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`;
87th consecutive 403 re-verified this run via GraphQL `createIssue` → FORBIDDEN). Per repo
convention findings ship as labeled docs records, ready for bulk-issue creation the moment
permissions are granted — each carrying exactly one category label and one priority label
(contract §4).

---

## Issue 1 — F066 (P1, test): dist-destruction flake — LATENT (12/12 clean this run)

- **Category**: `test` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (91st run)
- **Status**: **LATENT — no re-observation; ~1/99 cumulative**

### Summary

12-cycle `rm -rf dist && npm run build && ls dist/sitemap-index.xml` loop — **12/12 clean**.
No dist wipe observed this run. Cumulative observed rate remains ~1/99 (one event at the 89th
run's first cycle; ~24 clean cycles since). No deterministic repro exists, so per the FAIL-SAFE
rule no root-cause guess is recorded.

### Evidence

```
12-cycle loop (build→test→ls dist) → 12/12 sitemap-index.xml present
Cumulative: ~1 event / ≈99 cycles (observed once at 89th run, never reproduced since)
```

### Impact / Risk

Intermittent CI artifact loss (missing `dist/sitemap-index.xml` in a follow-up step) if a test
worker wipes build output. Latent; low probability.

### Suggestion (for a future Phase-2 candidate when reproduced)

If repro becomes deterministic, guard `node --test` with exclusive temp `CONFIG.DIST_DIR`
redirection in every integration-ish test (F052 pattern extension); or hard-enforce a
build→suite→assert-artifacts-survive workload.

---

## Issue 2 — F005 (P3, docs): Prettier drift — RESUMED 86→88 (+2)

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (91st run)
- **Status**: **RESUMED — 88 files (+2 vs 90th's 86)**

### Description

`npx prettier --check .` exit 1 — **88 files**, up from 86. All 88 live under
`docs/issues/**` (historical ledger records); **zero source files** affected (verified by
filtering out `docs/issues`). CI `format:check` continues to fail.

### Evidence

```
npx prettier --check . → "Code style issues found in 88 files"
88 files, all under docs/issues/** (ledger), 0 in src/ or scripts/
The 90th run's own records (42/44) are among the non-compliant files
```

### Impact / Risk

Low: cosmetic-only for docs lineage; failing `format:check` pollutes CI history. One
`prettier --write docs/issues` pass closes it forever, but sits in the cosmetic-prohibited
bucket (Phase 2 constraint) and low value. Counting artifact confirmed: each run's own records
are non-compliant, so the count grows ~2–3 per run; the 89th-run "halt" was an artifact.

---

## Issue 3 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD

- **Category**: `security` / **Priority**: `P0`
- **Evaluation date**: 2026-08-08 (91st run)
- **Status**: **HELD (2 CRITICAL + 10 HIGH) — unchanged**

### Description

`node scripts/check-workflow-security.js` → **exit 1, 12 violations** across 5 workflow
files: 2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml: job-level `API_KEY`
duplicates `GEMINI_API_KEY`); 10× HIGH (`ID_TOKEN_WRITE` in architect-agent/opencode/
orchestrator/parallel, `ACTIONS_WRITE_NON_MERGE` ×4, `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×3).
CI-pipeline surfaces, not runtime; Security criterion scored via this cluster at 45.

### Evidence

```
node scripts/check-workflow-security.js → exit 1; 'violations': 12 (passed: false)
→ DUPLICATE_API_KEY on-push.yml, parallel.yml (2) CRITICAL
→ ID_TOKEN_WRITE / ACTIONS_WRITE_NON_MERGE x4 each; GH_TOKEN x3 (10) HIGH
```

### Impact / Risk

Overbroad CI permissions + duplicate secret alias. Remediation needs `.github/workflows/*`
edits (other-agent domain per F064). No runtime exposure; static site unaffected.

---

## Issue 4 — F063 (P1, ci): orchestrator.yml DEAD — fictitious GH_TOKEN

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (91st run)
- **Status**: **RE-CONFIRMED — 10/10 recent runs failed**

### Description

`gh run list --workflow=orchestrator.yml --limit 10` → **10/10 completed/failure**
(2026-07-30 … 2026-08-08 nightlies). The workflow references `secrets.GH_TOKEN`, which does
not exist in this repo — checkout/auth dies immediately. Same lines flag the security
checker's `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`.

### Evidence

```
gh run list --workflow=orchestrator.yml --limit 10 → 10/10 failure
  2026-08-08T01:04 … 2026-07-30T01:48 (all completed/failure)
```

---

## Issue 5 — F002 (P1, ci): agent token lacks `issues: write` — 87th consecutive 403

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (91st run)
- **Status**: **CONFIRMED 87th consecutive** (GraphQL createIssue → FORBIDDEN)

### Description

GraphQL probe reproduces `createIssue` → `{"errors":[{"type":"FORBIDDEN",…
"Resource not accessible by integration"}]}`. The token can never create GitHub issues,
so EVERY Phase-1 output contract ships as labeled docs records (repo convention). 87
consecutive runs documented.

---

## Issue 6 — F018 (P1, bug): data STALE 19 days (blocked)

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (91st run)
- **Status**: **CONFIRMED — STALE 19d** (threshold 7d, 2 records)

### Evidence

```
node scripts/check-freshness.js → "Last Update: 2026-07-20 (19 days ago)" / "Status: ⚠️ STALE"
```

Upstream source is JSON-only and ETL `fetch-data.js` consumes a CSV contract — refresh needs
an upstream-contract change (genuine feature cycle), not local code.

---

## Issue 7 — F024 (P2, bug): build emits sitemap — HELD (every fresh build this run)

- **Category**: `bug` / **Priority**: `P2`
- **Status**: **HELD — sitemap-index.xml present after every fresh build**

Every fresh `npm run build` emitted `dist/sitemap-index.xml` + `dist/sitemap-001.xml`
(5 URLs) — 12/12 in the flake loop plus the initial build. Remaining gap: F066 latency only.

---

## Issue 8 — F064 (P2, ci): lint-staged engine drift — EBADENGINE

- **Category**: `ci` / **Priority**: `P2`
- **Status**: **CONFIRMED (EBADENGINE)**

`lint-staged@17.3.0` declares `engines.node >=22.22.1`; CI runs Node v20.20.2 while
`.nvmrc` says 22. `npm install` logs EBADENGINE (install succeeds). Pre-commit lint-staged
hooks may fail at runtime on the CI image.

---

## Issue 9 — F028 (P2, security): npm dependency vulnerabilities — maintained RESOLVED

`npm audit → found 0 vulnerabilities` (F028 held RESOLVED).

---

## Issue 10 — F057 (P3, docs): phantom `addNumbers` — maintained FIXED

`grep -c "addNumbers" docs/api.md → 0`.

---

**Net movement this run (91st)**: F005 moved 86→88 (+2, counting-artifact now documented as
structural: each run's own records are non-compliant); F066 no re-observation (12/12 clean);
everything else held / reproduced. No new root-cause-identifiable defect recorded. F002 87th.
