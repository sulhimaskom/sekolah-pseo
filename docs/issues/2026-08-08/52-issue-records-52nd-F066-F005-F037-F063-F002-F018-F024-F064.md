# Issue Records — 52nd batch (93rd verification run, 2026-08-08)

**Evaluation Date**: 2026-08-08
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`7fd0fb5`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks
`issues: write`; 89th consecutive 403 re-verified this run via GraphQL
`createIssue` AND REST `gh issue create` → FORBIDDEN). Per repo convention
findings ship as labeled docs records, ready for bulk-issue creation the moment
permissions are granted — each carrying exactly one category label and one
priority label (contract §4).

---

## Issue 1 — F066 (P1, test): dist-destruction flake — LATENT (6/6 clean)

- **Category**: `test` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (93rd run)
- **Status**: **LATENT — no re-observation; ~1/117 cumulative**

### Summary

6-cycle `rm -rf dist && npm run build && ls dist/sitemap-index.xml` loop —
**6/6 clean**. No dist wipe observed this run. Cumulative observed rate ~1/117
(one event at the 89th run's first cycle; ~41 clean cycles since). No
deterministic repro exists, so per the FAIL-SAFE rule no root-cause guess is
recorded.

### Evidence

```
6-cycle loop (build→ls dist) → 6/6 sitemap-index.xml present
Cumulative: ~1 event / ≈117 cycles (observed once at 89th run, never reproduced since)
```

### Impact / Risk

Intermittent CI artifact loss (missing `dist/sitemap-index.xml` in a follow-up
step) if a test worker wipes build output. Latent; low probability.

---

## 2 — F005 (P3, docs): Prettier drift — HELD at 88 (second +0 run)

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (93rd run)
- **Status**: **HELD at 88 files (+0)** — drift halted, second flat run

### Description

`npx prettier --check .` exit 1 — **88 files**, unchanged from 91st/92nd. All 88
live under `docs/issues/**` (historical ledger records); **zero source files**
affected (verified by filtering out `docs/issues`). CI `format:check` continues
to fail.

### Evidence

```
npx prettier --check . → "Code style issues found in 88 files"
88 files, all under docs/issues/** (ledger), 0 in src/ or scripts/
This run's records (51–53) authored Prettier-compliant — drift stays halted
```

### Impact / Risk

Low: cosmetic-only for docs lineage; failing `format:check` pollutes CI history.
One `prettier --write docs/issues` pass closes it forever, but sits in the
cosmetic-prohibited bucket (Phase 2 constraint). Positive trend confirmed: two
consecutive flat runs — the ledger stopped self-growing.

---

## 3 — F037/F038 (P1, security): workflow secret exposure — 12 violations HELD

- **Category**: `security` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (93rd run)
- **Status**: **HELD — exactly identical to 92nd**

### Description

`node scripts/check-workflow-security.js` → **exit 1, 12 violations** across 5
workflow files: 2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml);
10× HIGH (`ID_TOKEN_WRITE` ×4, `ACTIONS_WRITE_NON_MERGE` ×4,
`GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×3). CI-pipeline surfaces, not runtime;
Security criterion scored via this cluster at 45.

### Evidence

```
node scripts/check-workflow-security.js → exit 1; 12 violations (2 CRITICAL + 10 HIGH)
→ DUPLICATE_API_KEY on-push.yml, parallel.yml (2) CRITICAL
→ ID_TOKEN_WRITE architect/opencode/orchestrator/parallel (4) HIGH
→ ACTIONS_WRITE_NON_MERGE ×4; GH_TOKEN ×3 (3) HIGH
```

### Impact

Overbroad CI permissions + duplicate secret alias. Remediation needs
`.github/workflows/*` edits (other-agent domain per F064). No runtime exposure;
static site unaffected. Label: `security` / `P1`.

---

## 4 — F063 (P1, ci): orchestrator.yml DEAD — fictitious GH_TOKEN

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (93rd run)
- **Status**: **RE-CONFIRMED — 5/5 recent runs failed (10/10 two runs ago)**

### Description

`gh run list --workflow=orchestrator.yml --limit 5` → **5/5 completed/failure**
(2026-08-04 … 2026-08-08 nightlies). The workflow references `secrets.GH_TOKEN`,
which does not exist — checkout/auth dies immediately. Same lines flagged by the
security checker's `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`.

---

## 5 — F002 (P1, ci): agent token lacks `issues: write` — 89th consecutive

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (93rd run)
- **Status**: **CONFIRMED 89th consecutive** (GraphQL + REST → FORBIDDEN)

### Description

GraphQL probe reproduces `createIssue` → `{"errors":[{"type":"FORBIDDEN",
"message":"Resource not accessible by integration"}]}`; REST `gh issue create`
returns the same `FORBIDDEN`. The token can never create GitHub issues, so EVERY
Phase-1 output contract ships as labeled docs records (repo convention). 89
consecutive runs documented.

---

## 6 — F018 (P1, bug): data STALE 20 days (blocked)

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (93rd run)
- **Status**: **CONFIRMED — STALE 20d** (threshold 7d, 2 records)

### Evidence

```
node scripts/check-freshness.js → "Last Update: 2026-07-20 (19 days ago)"
```

Upstream source is JSON-only and ETL `fetch-data.js` consumes a CSV contract —
refresh needs an upstream-contract change (genuine feature cycle), not local code.

---

## 7 — F024 (P2, bug): build emits sitemap — HELD

- **Category**: `bug` / **Priority**: `P2`
- **Status**: **HELD — sitemap-index.xml present after every fresh build**

Every fresh `npm run build` emitted `dist/sitemap-index.xml` +
`dist/sitemap-001.xml` (5 URLs) — 6/6 in the flake loop plus the initial build.
Remaining gap: F066 latency only.

---

## 8 — F064 (P2, ci): lint-staged engine drift — EBADENGINE

- **Category**: `ci` / **Priority**: `P2`
- **Status**: **CONFIRMED (EBADENGINE)**

`lint-staged@17.3.0` declares `engines.node >=22.22.1`; CI runs Node v20.20.2
(`.nvmrc` says 22). `npm install` logs EBADENGINE (install succeeds). Pre-commit
lint-staged hooks may fail at runtime on the CI image.

---

## 9 — F028 (P2, security): npm dependency vulnerabilities — maintained RESOLVED

`npm audit → found 0 vulnerabilities` (F028 held RESOLVED).

---

## 10 — F057 (P3, docs): phantom `addNumbers` — maintained FIXED

`grep -c "addNumbers" docs/api.md → 0`.

---

**Net movement this run (93rd)**: F005 **HELD at 88 (+0) — second flat run** (ledger
records authored compliant since 91st; no growth). F066 no re-observation (6/6
clean, ~1/117 cumulative). Everything else held / reproduced. No new
root-cause-identifiable defect recorded (`*11 consecutive runs*`). F002 89th.
