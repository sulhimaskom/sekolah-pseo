# Issue Records — 46th batch (87th verification run, 2026-08-08)

**Evaluation Date**: 2026-08-08
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`6649beb`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`,
83rd consecutive 403 re-verified this run via GraphQL `createIssue` → FORBIDDEN). Per repo
convention these findings ship as labeled docs records and are ready for bulk-issue creation
the moment token permissions are granted — every finding carries exactly one category label
and one priority label (contract §4).

---

## Issue 1 — F066 (P1 → FIXED, maintained): sitemap.test.js dist destruction — held FIXED, 3/3

- **Category**: `test` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **FIXED-HELD (maintained, 3/3 consecutive verification since 84th fix)**

### Summary

The 84th-run fix (temp-dir redirect of `CONFIG.DIST_DIR` in `scripts/sitemap.test.js`)
**holds**. Fresh verification this run: `npm run build` → `dist/sitemap-index.xml` present →
`npm run test:js` (1056 pass / 0 fail / 4 skipped) → `dist/sitemap-index.xml` **survives**
(3rd consecutive). F024's "sitemap emitted" guarantee now holds end-to-end (build → test →
dist/) for four consecutive runs (84th–87th). The last F014-family test-side effect remains
eliminated.

### Evidence

```
RUN: rm -rf dist && npm run build → ls dist/sitemap-index.xml → present
     npm run test:js (exit 0, 1056 pass) → ls dist/sitemap-index.xml → STILL PRESENT
```

Source: `scripts/sitemap.test.js` — `CONFIG.DIST_DIR` redirected to
`sitemap-test-dist-${process.pid}` immediately after `require('./config')` (mirrors the
F052 pattern in `build-pages.test.js` / `build-orchestrator.test.js`).

### Impact / Risk

None currently — fix maintained. Regression risk: any future test writing to the real
`CONFIG.DIST_DIR` would reintroduce the class; the F052 pattern is the standing template.

---

## Issue 2 — F005: Prettier drift HELD at 83 files (P3, docs) — 0 growth, first run without drift

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **HELD — 83 files (83→83, +0)** — trend broken

### Summary

`npx prettier --check .` exits 1 with **83 files** — unchanged from the 86th run. **Zero
source files** affected; 100% under `docs/issues/**`. Notably, the 86th run's own ledger
records (30-audit-report-86th, 31-issue-records-45th, 32-phase2-3-decision-86th) were merged
**Prettier-compliant**, so the self-compounding drift (previously ~+3 files/run) halted this
run.

### Evidence

`npx prettier --check .` → "Code style issues found in 83 files. Run Prettier with --write
to fix." — dir breakdown: 100% `docs/issues/` (83 `.md` matches). The non-compliant tail
list shows files 26–29 (84th/85th-run records); 30–32 (86th-run records) are absent —
compliant. `git status` post-run clean (source untouched).

### Impact / Risk

Formatting debt confined to the audit ledger; no functional impact. If the compliant-record
habit holds, the count is now stable (though a one-time `prettier --write docs/issues/**`
would zero it entirely).

### Suggested fix (Phase-2 candidate, would-be-cosmetic)

Run `npx prettier --write docs/issues/**` once, then keep the "prettier before commit"
habit that the 86th run demonstrated (add `docs/issues/**` to `.prettierignore` or run
prettier on new record files before merging). Held as P3 — formatting-only.

---

## Issue 3 — F063 (P1, ci): Orchestrator workflow dead — fictitious GH_TOKEN — RE-CONFIRMED 10/10

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **RE-CONFIRMED — 10/10 recent runs `failure`**

### Summary

`gh run list --workflow=orchestrator.yml --limit 10` → **10/10 `failure`**
(2026-07-30 → 2026-08-08, daily cadence). Root cause unchanged: the workflow references a
fictitious `secrets.GH_TOKEN` (finding F037/F038 family — `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`,
2 occurrences in `orchestrator.yml`), so the orchestrator cannot authenticate and dies
every scheduled run.

### Evidence

```
Runs: 10 — failure @ 2026-08-08T01:04, 2026-08-07T02:18, ... 2026-07-30T01:48 (10/10)
```

### Impact / Risk

The repository's automated maintenance loop is dead — no self-healing occurs between
verification runs. Requires `workflows: write` permission to edit
`.github/workflows/orchestrator.yml` (blocked by F050 token perimeter).

---

## Issue 4 — F037/F038 (P0, security): Workflow secret exposure — 12 violations — HELD

- **Category**: `security` / **Priority**: `P0`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **HELD — 12 violations (2 CRITICAL + 10 HIGH), unchanged**

### Summary

`node scripts/check-workflow-security.js` → exit 1, **12 violations** across 5 workflows
(`architect-agent.yml`, `on-push.yml`, `opencode.yml`, `orchestrator.yml`, `parallel.yml`):

| Severity | Rule                             | Files                                                 |
| -------- | -------------------------------- | ----------------------------------------------------- |
| CRITICAL | DUPLICATE_API_KEY                | on-push.yml, parallel.yml (2)                         |
| HIGH     | ID_TOKEN_WRITE                   | architect-agent, opencode, orchestrator, parallel (4) |
| HIGH     | ACTIONS_WRITE_NON_MERGE          | architect-agent, opencode, orchestrator, parallel (4) |
| HIGH     | GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN | architect-agent, orchestrator (2)                     |

### Evidence

Full violation dump reproduced this run (see audit report 87th, security command). Count
identical to runs 74–86 — no regression, no improvement.

### Impact / Risk

CI-pipeline attack surface (privilege escalation vectors, secret duplication). Runtime
surface unaffected (static site generator). Fix requires `workflows: write` — blocked by
F050 token perimeter.

---

## Issue 5 — F002 (P1, ci): Agent token lacks `issues: write` — CONFIRMED 83rd

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **CONFIRMED — 83rd consecutive (GraphQL 403)**

### Summary

GraphQL `createIssue` mutation → `{"errors":[{"type":"FORBIDDEN",...,"message":"Resource
not accessible by integration"}]}`. Auth context: `github-actions[bot] (GITHUB_TOKEN)` —
token lacks the permission scope required for issue creation.

### Evidence

```
gh api -X POST graphql -f query='mutation { createIssue(...) { issue { number } } }'
→ FORBIDDEN "Resource not accessible by integration"
```

### Impact / Risk

All Phase-1/2/3 issue-creation output contracts are blocked; findings ship as docs records
instead. Unblocking requires a token with `issues: write` (repo Actions settings).

---

## Issue 6 — F018 (P1, bug): Data STALE 19 days — CONFIRMED

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **CONFIRMED — 19 days stale (threshold 7)**

### Summary

`node scripts/check-freshness.js` → "Data is stale! Last update was 19 days ago (threshold:
7 days)" — last update 2026-07-20, 2 records. Unchanged from the 86th run (19d).

### Evidence

```
Last Update: 2026-07-20 (19 days ago) · Record Count: 2 · Status: ⚠️ STALE
```

### Impact / Risk

Site content is 2.7× past the freshness threshold. Refresh blocked on an upstream
contract change (source is JSON-only; ETL consumes CSV) — a real fix cycle without a
JSON-source contract was deferred per runs 80+ decision records.

---

## Issue 7 — F014 (P2, test): Parallel test flake — CONFIRMED clean 13th consecutive

- **Category**: `test` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **CONFIRMED clean — 13th consecutive clean run (latent)**

### Summary

`npm run test:js` → **1056 pass / 0 fail / 4 skipped** (1060 total) — 13th consecutive
green run. The F014-family flake (previously observed when running test files in parallel
with shared `CONFIG.DIST_DIR`) has not manifested since the F052/F066 temp-dir patterns
landed.

### Evidence

```
# tests 1060 · pass 1056 · fail 0 · cancelled 0 · skipped 4 · duration 7.5s
```

### Impact / Risk

Latent — no current impact. Regression risk if a future test writes to shared real paths.

---

## Issue 8 — F024 (P2, bug): Build emitted sitemap — HELD 12/12

- **Category**: `bug` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **HELD — 12/12 fresh builds emit sitemap-index.xml**

### Summary

`npm run build` (fresh, after `rm -rf dist`) → `dist/sitemap-index.xml` present,
"Generated 1 sitemap files with 5 URLs total". F066's gap closed — the build's sitemap
emission guarantee now holds end-to-end.

### Evidence

```
rm -rf dist && npm run build → ls dist/sitemap-index.xml → present
```

---

## Issue 9 — F064 (P2, ci): lint-staged engine drift — CONFIRMED (EBADENGINE)

- **Category**: `ci` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **CONFIRMED — v20.20.2 runtime vs lint-staged@17.3.0 requirement (>=22.22.1)**

### Summary

`npm install` → `npm warn EBADENGINE ... lint-staged@17.3.0 ... required: { node: '>=22.22.1' },
current: { node: 'v20.20.2' }`. `.nvmrc` pins 22 but the CI runner executes v20 — config
drift, not a source defect.

### Evidence

```
npm warn EBADENGINE Unsupported engine { package: 'lint-staged@17.3.0', required: node>=22.22.1, current: v20.20.2 }
```

---

## Issue 10 — F028 (P2, security): npm dependency vulnerabilities — maintained RESOLVED

- **Category**: `security` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **maintained RESOLVED — npm audit 0 vulnerabilities**

### Evidence

```
npm install → "found 0 vulnerabilities" (132 packages audited)
```

---

## Issue 11 — F017 (P3, docs): Phantom addNumbers in api.md — maintained FIXED

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (87th run)
- **Status**: **maintained FIXED — 0 live matches**

### Evidence

`grep -rc "addNumbers" docs/api.md` → `0`. No resurrection of the phantom documented
symbol.

---

## No new findings this run

All 11 tracked findings held with zero new defects. Net movement: **F005 growth halted**
(83→83, +0 — 86th run's ledger merged compliant); F066 3/3; F014 clean 13th; F063 10/10
re-confirmed; F002 83rd.

## Action log (UTC)

| Time  | Action  | Target                          | Result                     |
| ----- | ------- | ------------------------------- | -------------------------- |
| 15:22 | Phase-0 | gh pr/issue list                | 0 PRs / 0 issues → Phase 1 |
| 15:26 | Records | docs/issues/2026-08-08/ (33–35) | 46th batch written         |
