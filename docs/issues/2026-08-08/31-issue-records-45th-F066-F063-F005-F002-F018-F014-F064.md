# Issue Records — 45th batch (86th verification run, 2026-08-08)

**Evaluation Date**: 2026-08-08
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`13c4331`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`,
82nd consecutive 403 re-verified this run). Per repo convention these findings ship as
labeled docs records and are ready for bulk-issue creation the moment token permissions are
granted — every finding carries exactly one category label and one priority label (contract §4).

---

## Issue 1 — F066 (P1 → FIXED, maintained): sitemap.test.js dist destruction — held FIXED, 2/2

- **Category**: `test` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (86th run)
- **Status**: **FIXED-HELD (maintained, 2/2 verification this run)**

### Summary
The 84th-run fix (temp-dir redirect of `CONFIG.DIST_DIR` in `scripts/sitemap.test.js`)
**holds**. Fresh verification this run: `npm run build` → `dist/sitemap-index.xml` present →
`npm run test:js` (1056 pass / 0 fail) → `dist/sitemap-index.xml` **survives** (2/2). The
last F014-family test-side effect is eliminated; F024's "sitemap emitted" guarantee now
holds end-to-end (build → test → dist/) as verified for three consecutive runs (84th–86th).

### Evidence
```
RUN 1: npm run build → ls dist/sitemap-index.xml → present
       npm run test:js → ls dist/sitemap-index.xml → STILL PRESENT
RUN 2: (identical sequence) → sitemap survives
```
Source: `scripts/sitemap.test.js` — `CONFIG.DIST_DIR` redirected to
`sitemap-test-dist-${process.pid}` immediately after `require('./config')` (mirrors the
F052 pattern in `build-pages.test.js` / `build-orchestrator.test.js`).

### Impact / Risk
None currently — fix maintained. Regression risk: any future test that writes to the real
`CONFIG.DIST_DIR` would reintroduce the class; the F052 pattern is the standing template.

### Suggested follow-up (Phase-2 candidate, optional)
A thin shared test helper (e.g. `scripts/test-helpers.js`) that standardizes the
temp-dir redirect would remove the copy-paste spread across the 3 test files. Low priority;
not required while the pattern is held.

---

## Issue 2 — F005: Prettier drift grew 80 → 83 files (P3, docs)

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (86th run)

### Summary
`npx prettier --check .` exits 1 with **83 files** (up from 80 at the 44th batch). All 83
are under `docs/issues/**`; **zero source files** affected. The +3 are the 85th run's own
ledger records (27-audit-report-85th, 28-issue-records-44th, 29-phase2-3-decision-85th)
merged non-Prettier-compliant — a self-compounding drift: each run's newly written ledger
files add ~3 non-compliant files to the next run's count.

### Evidence
`npx prettier --check .` → "Code style issues found in 83 files. Run Prettier with --write
to fix." — dir breakdown: 100% `docs/issues/`. `git status` post-run clean (source untouched).

### Impact / Risk
Consistency/formatting debt confined to the audit ledger; no functional impact. Drift is
strictly increasing (~1–3 files per run) because each run commits its own new records
without a prettier pass.

### Suggested fix (Phase-2 candidate, would-be-cosmetic)
Run `npx prettier --write docs/issues/**` once, then adopt a "prettier before commit"
habit for ledger records (e.g. add `docs/issues/**` to `.prettierignore` or run prettier
on new record files before merging). Held as P3 — formatting-only.

---

## Issue 3 — F063: Orchestrator workflow dead — RE-CONFIRMED 10/10 (P1, ci)

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (86th run)

### Summary
`gh run list --workflow=orchestrator.yml --limit 10` → **10/10 most recent runs all
`failure`** (2026-07-30 → 2026-08-08). The 85th run's 40-run window confirmed 40/40
consecutive failures back to 2026-06-30 — the failure is chronic. Root cause re-confirmed
at source: `orchestrator.yml:33,41` pass fictitious `${{ secrets.GH_TOKEN }}` — a secret
alias that does not exist in-repo, so `actions/checkout` cannot authenticate (git exit 128).

### Evidence
`gh run list --workflow=orchestrator.yml --limit 10` → 10/10 `failure`; ledger 85th:
40-run window 40/40 (2026-06-30 → 2026-08-08); `.github/workflows/orchestrator.yml:33`
(`GH_TOKEN: ${{ secrets.GH_TOKEN }}`), `:41` (`token: ${{ secrets.GH_TOKEN }}`).

### Impact / Risk
The scheduled maintainer workflow contributes zero value; every daily run burns CI minutes,
reports failure, and masks real regressions (a broken orchestrator cannot detect anything).
P1 unchanged.

### Fix (blocked)
Requires `workflows: write` to edit the workflow (F050) or a maintainer to change
`secrets.GH_TOKEN` → `secrets.GITHUB_TOKEN`.

---

## Issue 4 — F002: Agent token lacks `issues: write` — 82nd consecutive (P1, ci)

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (86th run)

### Summary
`gh issue create` → GraphQL `Resource not accessible by integration (createIssue)`. The
loop token remains a workflow-triggered `github-actions[bot]` integration token with
contents+pull-requests only. This blocks the Phase-1/2/3 issue-output contract; findings
ship as labeled docs records instead (established convention, runs 1–86).

### Impact / Risk
GitHub-issue visibility of findings depends on a maintainer granting `issues: write` to
the workflow token (or a PAT with issues scope). All 45 record-batches remain bulk-create
ready.

---

## Issue 5 — F018: Data STALE 19 days (P1, bug)

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (86th run)

### Summary
`node scripts/check-freshness.js` → Last Update 2026-07-20 (**19 days**, threshold 7),
Record Count 2, Status STALE. The refresh path remains blocked: the upstream source serves
`result/*.json` only (no CSV for `fetch-data`/ETL to consume).

### Impact / Risk
Deployed site serves increasingly stale school data; freshness gate fails on every run.

---

## Issue 6 — F037/F038 + F056–F059: Workflow security cluster (P0/P1, security)

- **Category**: `security` / **Priority**: `P0` (F037/F038), `P1/P2` (F056–F059)
- **Evaluation date**: 2026-08-08 (86th run)

### Summary
`node scripts/check-workflow-security.js` → exit 1, **12 violations** (2 CRITICAL +
10 HIGH) across 6 workflow files, unchanged since the 39th run:
- CRITICAL ×2: `DUPLICATE_API_KEY` (on-push.yml, parallel.yml — API_KEY aliases
  GEMINI_API_KEY)
- HIGH ×10: `ID_TOKEN_WRITE` + `ACTIONS_WRITE_NON_MERGE` in architect-agent.yml,
  opencode.yml, orchestrator.yml, parallel.yml; `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` in
  architect-agent.yml + orchestrator.yml
Re-confirmed at source: `.github/workflows/on-pull.yml` (the primary CI) self-invokes the
ulw-loop (`opencode run /ulw-loop`) and declaratively sets `id-token: write` +
`continue-on-error: true` at the top level (F037/F065 class); `opencode.yml:8`
`issue_comment` trigger with write tokens on a public repo.

### Impact / Risk
Public repo: unauthenticated comment can trigger a write-token agent; shell-injection
surface via `custom_prompt`. Highest-severity cluster in the ledger; unchanged for 8 runs.

### Fix (blocked)
Workflow edits need `workflows: write` (F050). Phase-2 patch exists on
`fix/phase2-harden-F037-F038-F029` (42nd run); push blocked by token scope.

---

## Issue 7 — F014: Parallel test flake (P2, test) — clean 12th, latent

- **Category**: `test` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (86th run)

### Summary
NOT observed this run (clean 12th consecutive). Full JS suite 1056/1056 pass (0 fail,
4 skipped), coverage gate met, post-test `git status` clean. F052/F066 redirects hold.
Retained as latent.

---

## Issue 8 — F064: lint-staged engine drift (P2, ci)

- **Category**: `ci` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (86th run) — re-confirmed

### Summary
`npm install` emits `EBADENGINE` — `lint-staged@17.3.0` requires node `>=22.22.1`, runtime
is v20.20.2 (CI `actions/setup-node node-version: 20`). `.nvmrc` says 22. Config/env parity
gap; no functional failure (husky/lint-staged degrade silently). Dependabot bumped
lint-staged to 17.3.0 (2026-08-03) — that on-push deps run is the last failure in on-push.

---

## Maintained-RESOLVED (no action)

| ID | Finding | Category | Priority | Status |
| -- | ------- | -------- | -------- | ------ |
| F024 | Build emitted sitemap | bug | P2 | maintained — sitemap present and survives test suite (F066 gap closed) |
| F028 | npm dep vulnerabilities | security | P2 | maintained RESOLVED (0 vulns via npm audit) |
| F017 | Phantom addNumbers in api.md | docs | P3 | maintained FIXED (0 live matches) |

## Summary

| Severity | This run | Ledger (held) |
| -------- | -------- | ------------- |
| NEW | — | — |
| Grew | F005 (P3, 80→83) | — |
| Re-confirmed | F063 (P1, 10/10; 40/40 window at 85th) | — |
| P0 | — | F037/F038 |
| P1 | — | F002, F063, F018 |
| P2 | — | F014, F064, F065 |
| P3 | — | F005 (held) |

**Total tracked findings: 51** (incl. F001–F066 minus resolved). No new findings this run.
All labeled (category + priority) and bulk-create ready. GitHub issue-creation (contract
Phase-1/2/3 output) remains blocked by F002 — shipped as labeled docs records per convention.
