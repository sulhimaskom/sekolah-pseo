# Issue Records — 76th verification (labeled findings, GitHub-issue output blocked by F002)

**Labels**: multiple (see each record). **Format**: evaluation date / domain score table /
criteria-level breakdown / evidence / files affected. Local `gh issue create` returns HTTP
403 `createIssue` (F002 held 72 sessions; re-verified this run) — these records are the
repo's labeled issue channel for this run. Deliverable ships as docs PR (convention).

## Issue 1 — F014 parallel test flake: clean 3/3 this run (latent, P2)

- **Category/priority**: `test` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Testability 70/100; Determinism 74/100)
- **Criteria breakdown**: flake is the top determinism defect; not observed this run but
  intermittently produced 53F+19C on default-concurrency runs (75th run).
- **Evidence** (firsthand this run):
  - `npm run test:js` ×3 → **1056 pass / 0 fail / 4 skipped, all three runs**.
  - Prior observation pattern: fs/tmp races in `etl-run.test.js` mkdtemp cleanup
    (`IntegrationError: File access check failed for /tmp/etl-run-*/raw.csv` ENOENT);
    serial always green.
- **Files**: `scripts/etl-run.test.js`, `package.json` (test scripts)
- **Fix suggestion**: per-test unique tmp workspace with explicit cleanup wait, or
  `--test-concurrency=1` in CI test scripts (75th-run suggestion retained).

## Issue 2 — Workflow security regressions: 12 live violations (F037, F038, F013, F056–F059)

- **Category/priority**: `security` / `P0`
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Security 46/100); D. CI/CD Health 46/100
- **Criteria breakdown**: Security practices — CRITICAL duplicate API keys ship on every
  push; HIGH id-token/actions grants on non-merge workflows; fictitious `GH_TOKEN` in
  Checkout steps. Gate is dead: `.husky/pre-commit` swallows the checker
  (`|| echo skipped`), no CI job invokes it.
- **Evidence** (`node scripts/check-workflow-security.js` → exit 1, 12 violations):
  - CRITICAL `DUPLICATE_API_KEY`: `parallel.yml`, `on-push.yml`
  - HIGH `ID_TOKEN_WRITE`: `parallel.yml`, `orchestrator.yml`, `opencode.yml`, `architect-agent.yml`
  - HIGH `ACTIONS_WRITE_NON_MERGE`: same four workflows
  - HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`: `orchestrator.yml`, `architect-agent.yml`
- **Impact**: F037 (public-repo `issue_comment` → unauthenticated write-token LLM agent)
  and F038 (proven shell RCE via `workflow_dispatch custom_prompt` heredoc injection)
  remain externally triggerable on a PUBLIC repo — unfixed for 25+ runs.
- **Files**: `.github/workflows/parallel.yml`, `on-push.yml`, `orchestrator.yml`,
  `opencode.yml`, `architect-agent.yml`, `.husky/pre-commit`
- **Fix** (blocked on `workflows: write` / Actions-level secrets): delete API_KEY aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop id-token/actions from non-merge workflows, wire the checker
  as a required CI job.

## Issue 3 — F063 orchestrator chronic failure: dead `GH_TOKEN` secret (10th visible)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health 46/100); B. System (Stability)
- **Criteria breakdown**: CI/CD Health — highest-autonomy scheduled agent dead for ~9 days;
  root cause isolated to a fictitious secret, not a code defect.
- **Evidence** (firsthand this run):
  - `gh run list --workflow=orchestrator.yml` → latest 9 scheduled runs all `failure`.
  - `gh run view <31231794492> --log-failed` → Checkout dies on all attempts:
    `##[error]fatal: could not read Username for 'https://github.com': terminal prompts
disabled` (×3), git exit 128.
  - `orchestrator.yml:33,41` pass `GH_TOKEN: ${{ secrets.GH_TOKEN }}` — same fictitious-secret
    class as F061; unavailable to scheduled runs.
- **Impact**: scheduled automation surface dead; mirrored blindly in `architect-agent.yml`.
- **Files**: `.github/workflows/orchestrator.yml:33,41`, `architect-agent.yml`
- **Fix**: point Checkout/`GH_TOKEN` at `${{ secrets.GITHUB_TOKEN }}` (or a real PAT) —
  blocked at Actions-secret level; no token with `workflows: write` available to this loop.

## Issue 4 — F018 data STALE: 19 days (threshold 7)

- **Category/priority**: `bug` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Migration Safety 66/100; B. Scalability 76/100
- **Criteria breakdown**: data pipeline freshness — `check-freshness` gate STALE; affects
  build correctness of site content.
- **Evidence**: `node scripts/check-freshness.js` exit 1 — `Last Update: 2026-07-20 (19
days)`, `Status: STALE`; `data/schools.csv` unchanged (2 rows, `updated_at` 2026-07-20).
- **Files**: `data/schools.csv`, `external/raw.csv`

## Issue 5 — F005 Prettier drift: 70 files (docs/issues population)

- **Category/priority**: `docs` / `P3`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Consistency 55/100); C. Documentation Accuracy 47/100
- **Criteria breakdown**: source (`scripts/`, `src/`) is Prettier-clean; the drift is
  entirely the `docs/issues/**` ledger population, which grows every run by design.
- **Evidence**: `npx prettier --check .` exit 1 — "Code style issues found in **70 files**"
  (68 → 70; +2 from this run's records).
- **Files**: `docs/issues/**`
- **Fix**: `npx prettier --write docs/issues` or scope `format:check` to `scripts/`+`src/`.

## Issue 6 — F002 issue-creation block: 72nd consecutive (ci, P1)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. CI/CD Health 46/100
- **Criteria breakdown**: loop token is a fine-grained `github-actions[bot]` with
  `contents`/`pull-requests` only; lacks `issues: write` (and `workflows: write`, F050).
- **Evidence**: `gh issue create` → `GraphQL: Resource not accessible by integration
(createIssue)`; `/installation` 401, branch-protection probe 403.
- **Files**: repo settings (Actions token permissions)

## Issue 7 — F062 + F064: docs drift & node-matrix mismatch (held)

- **Category/priority**: `docs` / `ci`, `P3` / `P2`
- **Evaluation date**: 2026-08-08
- **Evidence**:
  - `docs/release.md:67` references phantom `release.yml` (no such workflow file);
    `docs/api.md:554` documents phantom `addNumbers()`.
  - `.nvmrc` declares node `22`; CI (`on-pull.yml:53`) sets 20; runtime v20.20.2 —
    `lint-staged@17.3.0` wants >=22.22.1 (EBADENGINE on `npm ci`, re-confirmed).

## Issue 8 — F028 npm audit HIGH — RESOLVED (9th consecutive clean)

- **Category/priority**: `security` / `P2` retired
- **Evaluation date**: 2026-08-08
- **Evidence**: `npm audit` exit 0, **0 vulnerabilities**, 132 packages.
- **Files**: `package-lock.json`

## Issue 9 — F024 build sitemap — RESOLVED (2nd run witnessed)

- **Category/priority**: `bug` / `P2` retired
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Stability 78/100); A. Correctness 77/100
- **Evidence**: `npm run build` exit 0 then `ls dist/*.xml` →
  `sitemap-index.xml`, `sitemap-001.xml` present; `BuildOrchestrator.js:489-497`
  `generateSitemaps(schools)` wired into `build()` tail. robots.txt promise honored.
- **Resolution**: `5643384` (PR #608, merged 2026-08-08). **F024 closed, verified 2nd run.**
