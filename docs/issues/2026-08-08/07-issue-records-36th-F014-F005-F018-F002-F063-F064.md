# Issue Records — 77th verification (labeled findings, GitHub-issue output blocked by F002)

**Labels**: multiple (see each record). **Format**: evaluation date / domain score table /
criteria-level breakdown / evidence / files affected. Local `gh issue create` returns HTTP
403 `createIssue` (F002 held 73 sessions; re-verified this run) — these records are the
repo's labeled issue channel for this run. Deliverable ships as docs PR (convention).

## Issue 1 — F014 parallel test flake: clean 3rd consecutive run (latent, P2)

- **Category/priority**: `test` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Testability 72/100, +2; Determinism 74/100 → 76/100)
- **Criteria breakdown**: flake was the top determinism defect; not observed this run (nor the
  76th). JS suite produced **1056 pass / 0 fail / 4 skipped** twice in-session.
- **Evidence** (firsthand this run):
  - `npm run test:js` ×2 → **1056 pass / 0 fail / 4 skipped, both runs**.
  - Prior observation pattern: fs/tmp races in `etl-run.test.js` mkdtemp cleanup
    (`IntegrationError: File access check failed for /tmp/etl-run-*/raw.csv` ENOENT);
    serial always green.
- **Files**: `scripts/etl-run.test.js`, `package.json` (test scripts)
- **Fix suggestion** (retained): per-test unique tmp workspace with explicit cleanup wait, or
  `--test-concurrency=1` in CI test scripts.

## Issue 2 — Workflow security regressions: 12 live violations (F037, F038, F013, F056–F059)

- **Category/priority**: `security` / `P0`
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Security 46/100); D. CI/CD Health 46/100
- **Criteria breakdown**: Security practices — CRITICAL duplicate API keys ship on every push;
  HIGH id-token/actions grants on non-merge workflows; fictitious `GH_TOKEN` passed in Checkout
  steps. Gate is dead: no CI job invokes the workflow-security checker (pre-commit-only hook).
- **Evidence** (`node scripts/check-workflow-security.js` → exit 1, 12 violations, full list in
  the 77th audit report §Workflow-Security Violation Enumeration):
  - CRITICAL `DUPLICATE_API_KEY`: `parallel.yml`, `on-push.yml`
  - HIGH `ID_TOKEN_WRITE`: `parallel.yml:16`, `orchestrator.yml:9`, `opencode.yml:18`,
    `architect-agent.yml:13`
  - HIGH `ACTIONS_WRITE_NON_MERGE`: `parallel.yml:15`, `orchestrator.yml:13`,
    `opencode.yml:22`, `architect-agent.yml:17`
  - HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`: `orchestrator.yml`, `architect-agent.yml`
- **Impact**: F037 (public-repo `issue_comment` → unauthenticated write-token LLM agent) and
  F038 (shell RCE via `workflow_dispatch custom_prompt` heredoc injection) remain externally
  triggerable on a PUBLIC repo — unfixed for 25+ runs.
- **Files**: `.github/workflows/parallel.yml`, `on-push.yml`, `orchestrator.yml`,
  `opencode.yml`, `architect-agent.yml`, `.husky/pre-commit`
- **Fix** (blocked on `workflows: write` / Actions-level secrets): delete API_KEY aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop id-token/actions from non-merge workflows, wire the checker
  as a required CI job.

## Issue 3 — F063 orchestrator chronic failure: dead `GH_TOKEN` secret (12th consecutive)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health 46/100); B. System (Stability below)
- **Criteria breakdown**: highest-autonomy scheduled agent dead for ~11 days; root cause is a
  fictitious secret, not a code defect.
- **Evidence** (firsthand this run):
  - `gh run list --workflow=orchestrator.yml --limit 12` → **12 consecutive `failure` runs**
    (2026-07-28 → 2026-08-08).
  - `gh run view 31231794492 --log-failed` → Checkout dies on all attempts:
    `##[error]fatal: could not read Username for 'https://github.com': terminal prompts disabled`
    (×3), git exit 128.
  - `orchestrator.yml:33,41` pass `GH_TOKEN: ${{ secrets.GH_TOKEN }}` — unavailable to
    scheduled runs.
- **Files**: `.github/workflows/orchestrator.yml:33,41`, `architect-agent.yml`
- **Fix**: point Checkout/`GH_TOKEN` at `${{ secrets.GITHUB_TOKEN }}` (or a real PAT) —
  blocked at Actions-secret level; no token with `workflows: write` available to this loop.

## Issue 4 — F018 data STALE: 19 days (threshold 7)

- **Category/priority**: `bug` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Migration Safety 66/100; B. Scalability 76/100
- **Evidence**: `node scripts/check-freshness.js` exit 1 — `Last Update: 2026-07-20 (19 day)`,
  `Status: STALE`; `data/schools.csv` unchanged (2 rows).
- **Files**: `data/schools.csv`, `external/raw.csv`

## Issue 5 — F005 Prettier drift: 72 files, 100% docs/issues ledger

- **Category/priority**: `docs` / `P3`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Consistency 55→56/100); C. Documentation Accuracy 47/100
- **Criteria breakdown**: source (`scripts/`, `src/`) is Prettier-clean; the entire drift is the
  `docs/issues/**` ledger population, which grows every run by design. 0 source files affected.
- **Evidence**: `npx prettier --check .` exit 1 — "Code style issues found in **72 files**";
  filtered enumeration shows **0 files outside `docs/issues/`**.
- **Files**: `docs/issues/**`
- **Fix**: `npx prettier --write docs/issues` or scope `format:check` to `scripts/`+`src/`.

## Issue 6 — F002 issue-creation block: 73rd consecutive (ci, P1)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. CI/CD Health 46/100
- **Evidence**: `gh issue create` → `GraphQL: Resource not accessible by integration
  (createIssue)`; token is `github-actions[bot]` with `contents`/`pull-requests` only.
- **Files**: repo settings (Actions token permissions)

## Issue 7 — F064: node-version mismatch (lint-staged engines)

- **Category/priority**: `ci` / `P2`
- **Evaluation date**: 2026-08-08
- **Evidence**: `.nvmrc` declares node `22`; CI (`on-pull.yml:53`) sets node 20; runtime
  v20.20.2 — `lint-staged@17.3.0` wants `>=22.22.1` (EBADENGINE on `npm install`, re-confirmed).
- **Files**: `.nvmrc`, `.github/workflows/on-pull.yml`, `package-lock.json`

## Issue 8 — F025 live-site root 404 (ledged; not re-verifiable this sandbox)

- **Category/priority**: `bug` / `P1`
- **Evaluation date**: 2026-08-08
- **Evidence**: sandbox egress blocked (curl → HTTP 000); carried from 76th ledger where
  `https://sekolah-pseo.pages.dev/` returned root 404 while robots.txt 200 and Pages API said
  `built` (pages deploy indexed without a root route).
- **Files**: `public/` deploy config, Cloudflare Pages

## Issue 9 — F024 + F045–F049: RESOLVED (deterministic verification)

- **Category/priority**: `bug` / `refactor`, `P2` / `P3` retired
- **Evaluation date**: 2026-08-08
- **Evidence**: `rm -rf dist && npm run build` ×3 → sitemaps present 3/3
  (`dist/sitemap-index.xml`, `dist/sitemap-001.xml`); F045–F049 source-verified fixed in 54th run.
- **Files**: `src/services/BuildOrchestrator.js`