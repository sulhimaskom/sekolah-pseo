# Issue Records — 75th verification (labeled findings, GitHub-issue output blocked by F002)

**Labels**: multiple (see each record). **Format**: evaluation date / domain score table /
criteria-level breakdown / evidence / files affected. Local `gh issue create` returns HTTP
403 `createIssue` (F002 held 71 sessions; re-verified) — these records are the repo's
labeled issue channel for this run. Deliverable ships as docs PR (convention).

## Issue 1 — F024 build omits sitemap: RESOLVED by PR #608

- **Category/priority**: `bug` / `P2` (resolution verified; issue retired)
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Stability → 76/100 after resolution); A. Correctness
- **Evidence** (firsthand this run):
  - `npm run build` exit 0; then `ls dist/*.xml` →
    `sitemap-index.xml`, `sitemap-001.xml` **present**.
  - `src/services/BuildOrchestrator.js:489-497` — `generateSitemaps(schools)`
    wired into `build()` tail via `Promise.all([…, sitemapPromise])`.
  - `scripts/build-pages.test.js` gained build-time sitemap assertion (PR #608).
- **Impact**: FIXED — the robots.txt-promised `/sitemap-index.xml` now exists
  after full and incremental builds; search-engine crawl gap closed.
- **Resolution**: `5643384` (PR #608, merged 2026-08-08). **F024 closed.**

## Issue 2 — F014 parallel test flakiness re-observed (intermittent)

- **Category/priority**: `test` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Testability 68/100; Determinism 72/100)
- **Evidence**:
  - First default-concurrency `npm run test:js` run: **53 failures + 19 cancellations**
    (fs/tmp races in `etl-run.test.js` mkdtemp cleanup vs async spawn; e.g.
    `IntegrationError: File access check failed for /tmp/etl-run-*/raw.csv` ENOENT).
  - Serial `node --test --test-concurrency=1`: 1060/1060 pass.
  - Two subsequent default runs: 1056 pass/0 fail each.
- **Impact**: intermittent CI reds; flakes remain the top testability defect
  (first re-observation since the 41st run).
- **Fix suggestion**: isolate each test process in a unique tmp workspace with
  explicit cleanup-after-completion wait; or set `--test-concurrency=1` in
  `package.json` CI test scripts.

## Issue 3 — Workflow security regressions: 12 live violations (F037, F038, F013, F056-F059)

- **Category/priority**: `security` / `P0`
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Security 46/100)
- **Evidence** (`node scripts/check-workflow-security.js` → exit 1, 12 violations):
  - CRITICAL `DUPLICATE_API_KEY`: `on-push.yml:26`, `parallel.yml:36` (`API_KEY` = `GEMINI_API_KEY`)
  - HIGH `ID_TOKEN_WRITE` (non-OIDC): `architect-agent.yml`, `opencode.yml`, `orchestrator.yml`, `parallel.yml`
  - HIGH `ACTIONS_WRITE_NON_MERGE`: same four workflows
  - HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`: `orchestrator.yml:33,41`, `architect-agent.yml:37`, `template.md:33`
- **Impact**: repo ships regressed secrets on every push; `.husky/pre-commit`
  swallows the checker (`|| echo skipped`) so the security gate is dead; no CI
  job invokes the checker.
- **Fix** (blocked on `secrets.Config`/`workflows: write`): delete API_KEY aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop id-token/actions from non-merge workflows, wire
  the checker as a CI job.

## Issue 4 — F063 orchestrator chronic failure: dead `GH_TOKEN` secret (root cause isolated)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health 46/100); B. System (Stability)
- **Evidence** (firsthand this run):
  - `gh run list --workflow=orchestrator.yml` → latest 8 scheduled runs all `failure`.
  - `gh run view <31231794492> --log-failed` → every attempt dies in Checkout:
    `##[error]fatal: could not read Username for 'https://github.com': terminal prompts disabled` (×3), git exit 128.
  - `orchestrator.yml:33,41` set/pass `GH_TOKEN: ${{ secrets.GH_TOKEN }}` — same
    fictitious-secret class as F061; unavailable to scheduled runs.
- **Impact**: highest-autonomy scheduled agent dead for **8 days**; blindly
  mirrored also in `architect-agent.yml`.
- **Fix**: point Checkout/`GH_TOKEN` at `${{ secrets.GITHUB_TOKEN }}` (or a real PAT).

## Finding 5 — F018 data STALE: 19 days (threshold 7)

- **Category/priority**: `bug` / `P1`
- **Evaluation date**: 2026-08-08
- **Evidence**: `node scripts/check-freshness.js` exit 1 — `Last Update: 2026-07-20
  (19 days)`, `Status: STALE`; `data/schools.csv` unchanged (2 rows, `updated_at` 2026-07-20).
- **Files**: `data/schools.csv`, `external/raw.csv`

## Finding 6 — F005 Prettier drift: 68 files (docs/issues population)

- **Category/priority**: `docs` / `P3`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Consistency 55/100)
- **Evidence**: `npm run format:check` exit 1 — "Code style issues found in
  **68 files**"; population = `docs/issues/**` ledger (incl. prior audit reports).
- **Files**: `docs/issues/**`
- **Fix**: `npx prettier --write docs/issues` or scope `format:check` to `scripts/`+`src/`.

## Finding 7 — F062 + F064: docs drift & node-matrix mismatch (held)

- **Category/priority**: `docs` / `ci`, `P3` / `P2`
- **Evaluation date**: 2026-08-08
- **Evidence**:
  - `docs/release.md:67` references phantom `release.yml` (no such workflow file);
    `docs/api.md:554` documents phantom `addNumbers()`.
  - `.nvmrc` declares node `22`; CI (`on-pull.yml:53`) sets 20; runtime v20.20.2 —
    `lint-staged@17.3.0` wants >=22.22.1 (EBADENGINE on `npm ci`).

## Finding 8 — F028 npm audit HIGH — RESOLVED (8th consecutive clean)

- **Category/priority**: `security` / `P4` retired
- **Evidence**: `npm audit` exit 0, **0 vulnerabilities**, 131 packages.
- **Files**: `package-lock.json`