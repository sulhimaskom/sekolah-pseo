# Issue Records — 47th batch (88th verification run, 2026-08-08)

**Evaluation Date**: 2026-08-08
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`09bf5be`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`,
84th consecutive 403 re-verified this run via GraphQL `createIssue` → FORBIDDEN). Per repo
convention these findings ship as labeled docs records and are ready for bulk-issue creation
the moment token permissions are granted — every finding carries exactly one category label
and one priority label (contract §4).

---

## Issue 1 — F066 (P1 → FIXED, maintained): sitemap.test.js dist destruction — held FIXED, 4/4

- **Category**: `test` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **FIXED-HELD (maintained, 4/4 consecutive verification since 84th fix)**

### Summary

The 84th-run fix (temp-dir redirect of `CONFIG.DIST_DIR` in `scripts/sitemap.test.js`)
**holds**. Fresh verification this run: `npm run build` → `dist/sitemap-index.xml` present →
`npm run test:js` (1056 pass / 0 fail / 4 skipped) → `dist/sitemap-index.xml` **survives**
(4th consecutive). F024's "sitemap emitted" guarantee now holds end-to-end (build → test →
dist/) for five consecutive runs (84th–88th). The last F014-family test-side effect remains
eliminated.

### Evidence

```
RUN: npm run build (exit 0, sitemap emitted) → npm run test:js (exit 0, 1056 pass)
     → ls dist/ | grep sitemap → sitemap-001.xml + sitemap-index.xml STILL PRESENT
```

Source: `scripts/sitemap.test.js` — `CONFIG.DIST_DIR` redirected to
`sitemap-test-dist-${process.pid}` immediately after `require('./config')` (mirrors the
F052 pattern in `build-pages.test.js` / `build-orchestrator.test.js`).

### Impact / Risk

None currently — fix maintained. Regression risk: any future test writing to the real
`CONFIG.DIST_DIR` would reintroduce the class; the F052 pattern is the standing template.

---

## Issue 2 — F005: Prettier drift HELD at 83 files (P3, docs) — 0 growth, 2nd consecutive run

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **HELD — 83 files (83→83, +0)** — trend halted for 2 consecutive runs

### Summary

`npx prettier --check .` exits 1 with **83 files** — unchanged from the 87th run. **Zero source
files** affected; 100% under `docs/issues/**`. The 87th run's own ledger records
(33-audit-report-87th, 34-issue-records-46th, 35-phase2-3-decision-87th) were merged
**Prettier-compliant**, extending the self-compounding drift halt (previously ~+3 files/run)
to a second consecutive run. Remaining 83 files are historical ledger records predating the
compliance convention.

### Evidence

```
npx prettier --check . → exit 1, "Code style issues found in 83 files"
83 files, ALL under docs/issues/** (docs/issues + docs/audit excluded → 0 source hits)
```

### Impact / Risk

Low. Cosmetic-only, docs/ ledger scope. Fails the `format:check` CI gate
(`npm run format:check` exits non-zero) — a one-time `prettier --write docs/issues`
normalization pass would close it permanently, but is out of Phase-2 scope (cosmetic
prohibition) and low value vs. churn.

---

## Issue 3 — F063 (P1): orchestrator.yml dead — fictitious GH_TOKEN, 10/10 failures

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **RE-CONFIRMED — 10/10 recent runs failed**

### Summary

`gh run list --workflow=orchestrator.yml --limit 10` → **10/10 `completed/failure`**
(2026-07-30 … 2026-08-08, nightly cadence). Root cause (F063, ledgered): the workflow
references `secrets.GH_TOKEN` which does not exist in this repository — all orchestrator
runs die at checkout/auth. The 12-violation security check (`GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`
×3) flags the same lines.

### Evidence

```
gh run list --workflow=orchestrator.yml --limit 10
  → completed/failure ×10 (2026-08-08T01:04 … 2026-07-30T01:48)
node scripts/check-workflow-security.js
  → 2× GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN in orchestrator.yml
```

### Impact / Risk

A scheduled maintenance workflow is dead — no orchestrator-driven automation is running.
Fix requires a `workflows: write` token (blocked by F050 token perimeter) or a maintainer
edit. No data/source impact.

---

## Issue 4 — F037/F038 (P0, security): workflow secret exposure — 12 violations held

- **Category**: `security` / **Priority**: `P0`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **HELD (2 CRITICAL + 10 HIGH)** — unchanged

### Summary

`node scripts/check-workflow-security.js` → **12 violations** across 5 workflow files:
2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml — `API_KEY` duplicates
`GEMINI_API_KEY`); 10× HIGH (`ID_TOKEN_WRITE` ×4, `ACTIONS_WRITE_NON_MERGE` ×4,
`GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×3). These are CI-pipeline surfaces, not runtime code —
Security criterion scored at 46 via the workflow cluster.

### Evidence

```
node scripts/check-workflow-security.js → 12 violations (files checked: 6)
architect-agent.yml / on-push.yml / opencode.yml / orchestrator.yml / parallel.yml
```

### Impact / Risk

Excessive CI permissions + secret duplication. Remediation requires editing
`.github/workflows/*` (blocked by F050 token perimeter). No runtime exposure; the deployed
static site is unaffected.

---

## Issue 5 — F002 (P1, ci): agent token lacks `issues: write` — 84th consecutive 403

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **CONFIRMED 84th** (GraphQL `createIssue` → FORBIDDEN)

### Summary

This run's GraphQL probe reproduces the block: `createIssue` → `FORBIDDEN — Resource not
accessible by integration`. The agent token (`github-actions[bot]` GITHUB_TOKEN) cannot
create issues, so Phase-1 output (GitHub issues from findings) ships as labeled docs records
per repo convention. 84 consecutive runs have documented this.

### Evidence

```
gh api graphql -f query='mutation { createIssue(...) ... }'
  → {"data":{"createIssue":null},"errors":[{"type":"FORBIDDEN", ... "Resource not accessible by integration"}]}
```

### Impact / Risk

Blocks the mandatory Phase-1 issue-creation output and Phase-3 issue creation. Workaround
(docs records + bulk-create script `scripts/bulk-create-issues.sh`) in place. Requires a
maintainer to grant the workflow token `issues: write`.

---

## Issue 6 — F018 (P1, bug): data STALE 19 days

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **CONFIRMED — STALE 19d** (threshold 7d)

### Summary

`node scripts/check-freshness.js` → Last Update **2026-07-20 (19 days ago)**, Record Count 2,
Status **STALE**. `data/schools.csv` contains only 2 records; upstream source is JSON-only
and the ETL `fetch-data.js` consumes a CSV contract — refresh is blocked on an upstream
contract change, not on local code.

### Evidence

```
node scripts/check-freshness.js
  → "Last Update: 2026-07-20 (19 days ago)" / "Status: ⚠️ STALE" / "Record Count: 2"
```

### Impact / Risk

Site content is stale and minimal (2 schools). P1 for product value; blocked by upstream
data contract (JSON) — ETL change consumes a real fix cycle without a JSON-source contract.

---

## Issue 7 — F014 (P2, test): parallel test flake — clean 14th consecutive (latent)

- **Category**: `test` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **CONFIRMED clean 14th consecutive** (latent)

### Summary

JS suite fully green this run (1056 pass / 0 fail / 4 skipped, duration 7.5s). No flake
reproduced for 14 consecutive runs. The finding remains ledgered as latent (historically
intermittent under parallel execution); standing mitigation: deterministic temp-dir
isolation (F052/F066 pattern) and test-ordered shutdown.

### Evidence

```
npm run test:js → # tests 1060 / pass 1056 / fail 0 / cancelled 0 / skipped 4
```

### Impact / Risk

Low (latent). No action required unless flake recurs.

---

## Issue 8 — F024 (P2, bug): build emitted sitemap — HELD 13/13

- **Category**: `bug` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **HELD 13/13 fresh builds**

### Summary

Every fresh `npm run build` this run emitted `dist/sitemap-index.xml` + `dist/sitemap-001.xml`
(5 URLs). The F066 gap (tests destroying dist) is closed, so the emit guarantee holds
end-to-end for 13 consecutive verification builds.

### Evidence

```
npm run build → "Generated 1 sitemap files with 5 URLs total" → ls dist/sitemap-*.xml present
```

### Impact / Risk

None currently. Maintained.

---

## Issue 9 — F064 (P2, ci): lint-staged engine drift — v20 vs >=22.22.1

- **Category**: `ci` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **CONFIRMED (EBADENGINE)**

### Summary

`lint-staged@17.3.0` declares `engines.node >=22.22.1`; CI runs Node v20.20.2 (`.nvmrc`=22
but Actions setup uses `node-version: 20` in on-pull.yml). `npm ci` logs EBADENGINE
(install succeeds). Pre-commit hooks using lint-staged may fail at runtime on the CI image.

### Evidence

```
node -p "require('./node_modules/lint-staged/package.json').engines" → { node: '>=22.22.1' }
node --version → v20.20.2
```

### Impact / Risk

Low-medium — husky/lint-staged pre-commit path may misbehave; fixes are env/config
(.nvmrc vs workflow node-version alignment) not source code. Requires workflow edit
(F050-blocked) or maintainer action.

---

## Issue 10 — F028 (P2, security): npm dependency vulnerabilities — maintained RESOLVED

- **Category**: `security` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **maintained RESOLVED — 0 vulnerabilities**

### Evidence

```
npm audit → "found 0 vulnerabilities"
```

### Impact / Risk

None. Maintained.

---

## Issue 11 — F017 (P3, docs): phantom `addNumbers` in api.md — maintained FIXED

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08 (88th run)
- **Status**: **maintained FIXED (0 live matches)**

### Evidence

```
grep -n "addNumbers" docs/api.md → (no matches — 0 live references)
```

### Impact / Risk

None. Maintained.

---

**No NEW findings this run (88th).** All ledgered findings reproduced or held; F005 drift
halted 2nd consecutive run; F066 maintained FIXED 4/4; F014 clean 14th.
