# Issue Records — 74th verification (labeled findings, GitHub-issue output blocked by F002)

**Labels**: multiple (see each record). **Format**: evaluation date / domain table / criteria breakdown / evidence / files affected.
Local `gh issue create` currently returns HTTP 403 `createIssue` (F002 held 71 sessions; re-verified this run) —
these records are the repo's labeled issue channel for this run. Deliverable shipped as docs PR (convention).

## Issue 1 — F063 orchestrator chronic failure: root cause isolated to dead `GH_TOKEN` secret

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health 46/100); B. System Quality (Stability 74/100)
- **Evidence** (firsthand this run):
  - `gh run list --workflow=orchestrator.yml` → last 6 scheduled runs all `failure` (2026-08-03 → 2026-08-08).
  - `gh run view <31231794492> --log-failed` → every attempt dies in the Checkout step:
    `##[error]fatal: could not read Username for 'https://github.com': terminal prompts disabled` (×3), `git` exit 128.
  - `orchestrator.yml:38` Checkout uses `token: ${{ secrets.GH_TOKEN }}`; `orchestrator.yml:33` sets
    `GH_TOKEN: ${{ secrets.GH_TOKEN }}` in env. Same pattern as F061 (fictitious env secret) — this secret is
    unavailable to scheduled runs, so checkout cannot authenticate.
- **Impact**: the repo's highest-autonomy scheduled agent has been dead for **6 consecutive days**; no
  orchestrator-side remediation (or any issue creation it performs) can land until fixed.
- **Fix** (blocked on `secrets.Config` access or `workflows:write`): point Checkout/`GH_TOKEN` at
  `${{ secrets.GITHUB_TOKEN }}` (or a real PAT) in `orchestrator.yml` and `architect-agent.yml` — also exactly
  what `check-workflow-security.js` demands (HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`).

## Issue 2 — Workflow security regressions: 12 live violations (F037, F038, F056-F059, F013)

- **Category/priority**: `security` / `P0`
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Security 46/100)
- **Evidence**: `node scripts/check-workflow-security.js` → exit 1, 12 violations (re-verified firsthand, unchanged from 73rd):
  - CRITICAL `DUPLICATE_API_KEY`: `on-push.yml:26`, `parallel.yml:36` (`API_KEY` = `GEMINI_API_KEY`)
  - HIGH `ID_TOKEN_WRITE` (non-OIDC): `architect-agent.yml`, `opencode.yml`, `orchestrator.yml`, `parallel.yml`
  - HIGH `ACTIONS_WRITE_NON_MERGE`: same four workflows
  - HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`: `orchestrator.yml:33,41`, `architect-agent.yml:37`, `template.md:33`
- **Impact**: repo ships regressed secrets on every push; `.husky/pre-commit:3` swallows the checker failure via `|| echo skipped` so **the security gate is dead**; no CI workflow invokes the checker. Duplicate `API_KEY` == `GEMINI_API_KEY` is the exact CRITICAL class that F063's dead-secret symptom exemplifies.
- **Fix**: delete API_KEY aliases, s/GH_TOKEN/GITHUB_TOKEN, drop id-token/actions from non-merge workflows, wire checker as a CI job. (Blocked on `workflows:write`.)

## Finding 3 — Prettier drift grew 67→68 files

- **Category/priority**: `docs` / `P3`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Consistency 55/100)
- **Evidence**: `npm run format:check` exit 1 — "Code style issues found in **68 files**" (was 67; +1 from 73rd-run report). Failure population remains `docs/issues/*` plus the audit-report files themselves.
- **Impact**: the format gate in repo README claims a code-quality gate that cannot pass; every new docs ledger file ratchets the drift +1.
- **Fix**: `npx prettier --write` on `docs/issues/**` + make the report generator emit prettier-clean markdown; or scope `format:check` to `scripts/` + `src/` only (documents already tracked by the ledger).

## Issue 4 — F024 build omits sitemap

- **Category/priority**: `bug` / `P2`
- **Evaluation date**: 2026-08-08
- **Evidence**: after `npm run build`, `dist/` has no `sitemap-index.xml` (`ls dist/*.xml` → no such file; `build-pages.js`/`BuildOrchestrator.js` contain no sitemap reference; sitemap generation is a separate manual step, `sitemap.test.js` only).
- **Impact**: `robots.txt` (generated by build) advertises `/sitemap-index.xml` that the build never produces (brand-new doc/behavior mismatch); search engines see a 404.
- **Fix**: invoke `scripts/sitemap.js` at the end of `BuildOrchestrator.build()` + add a build-time assertion.

## Issue 5 — F018 data STALE: 19 days (threshold 7)

- **Category/priority**: `bug` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (freshness criterion under Stability)
- **Evidence**: `node scripts/check-freshness.js` exits 1 — `Last Update: 2026-07-20 (19 days ago)`, `Record Count: 2`, `Status: STALE`. Data file `data/schools.csv` unchanged (2 synthetic rows; `updated_at` 2026-07-20). 18→19d worsening this run.
- **Impact**: production directive render stale school data; freshness CI gate (cron every hour per `on-pull.yml`) reports stale data continuously.
- **Fix**: re-run ETL with live data source (`npm run etl` + `npm run fetch-data`) or update the seed data; add a freshness assertion to CI.

## Issue 6 — F062 docs drift: phantom `release.yml` and `addNumbers()`

- **Category/priority**: `docs` / `P3`
- **Evaluation date**: 2026-08-08
- **Domain**: C. Experience Quality (Documentation Accuracy 44/100)
- **Evidence**:
  - `docs/release.md:67` → "Pushing a tag matching `v*` triggers the **release workflow** (`.github/workflows/release.yml`)" — **no such file exists** in `.github/workflows/`.
  - `docs/api.md:554-575` → documents `addNumbers(a, b)` with usage example — **no such export exists** anywhere in `scripts/` or `src/`.
- **Impact**: documentation actively misleads contributors (API consumers will import a nonexistent function; release pipeline docs point to a nonexistent file); docs-accuracy criterion fixed at 44.
- **Fix**: add a real `release.yml` (with tag trigger and deploy), or remove/replace the phantom citations.

## Issue 7 — F002 issue-output channel blocked (token lacks `issues: write`)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (Change velocity)
- **Evidence**: `gh issue create ...` → `GraphQL: Resource not accessible by integration (createIssue)` — 71st consecutive hit. Meanwhile `gh pr create` **works** (probe PR #606 created + cleanly closed this run).
- **Impact**: all loop output (audits, findings) must ship as docs-PRs; the repo has zero GitHub issues for 7+ days despite 12 live findings; Issue Manager mode can never activate.
- **Fix** (repo-admin only): give the agent token `issues: write` (Actions secret `OP_AUTOMATION_PAT` augmented), or set `permissions: issues: write` in the driving workflow.

## Issue 8 — F064 dependency/engine drift: lint-staged needs node ≥22.22.1

- **Category/priority**: `ci` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (Config & Env Parity 15/73)
- **Evidence**: `npm ci` → `npm warn EBADENGINE (package: @eslint/config-array, want: {node >=22.22.1}, current: v20.20.2)`. `.nvmrc` says `22`; `engine`: `>=20.0.0`; `on-pull.yml` pins `node-version: 20`.
- **Impact**: three sources of truth disagree (`.nvmrc` / package.json engines / CI YAML), breaking pre-commit hooks and producing flaky local/CI parity.
- **Fix**: align everywhere to one version (recommend v22.22.1+) and bump CI to match.
