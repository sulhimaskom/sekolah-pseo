# Issue Records — 83rd verification (labeled findings; GitHub-issue output blocked by F002)

**Labels**: multiple (see each record). **Format**: evaluation date / domain score table /
criteria-level breakdown / evidence / files affected. Local `gh issue create` returns HTTP 403
`createIssue` (F002 held 79 sessions; re-verified this run) — these records are the repo's
labeled issue channel for this run. Deliverable ships as docs PR (repo convention).

## Issue 1 — F063 orchestrator chronic failure: dead `GH_TOKEN` secret (active)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health 42/100); B. System (Stability)
- **Criteria breakdown**: highest-autonomy self-scheduled agent (orchestrator.yml) has failed
  every scheduled run for 21 consecutive days (2026-07-18 → 2026-08-08); root cause is a
  fictitious secret, not a code defect.
- **Evidence** (this session):
  - `gh run list --workflow=orchestrator.yml --limit 8` → **8 visible, ALL `failure`**
    (2026-08-03T02:07:18Z → 2026-08-08T01:04:58Z).
  - `orchestrator.yml:33,41` pass `${{ secrets.GH_TOKEN }}`; the secret does not exist in the
    repo → Checkout step cannot authenticate (`fatal: could not read Username… terminal
    prompts disabled`, git exit 128).
- **Files**: `.github/workflows/orchestrator.yml`
- **Fix** (blocked on token): replace GH_TOKEN → GITHUB_TOKEN or provide real PAT with
  `workflows: write`.

## Issue 2 — Security: 12 workflow violations live (F037/F038, F013, F056–F059)

- **Category**: `security` / **Priority**: `P0` (CRITICAL violations)
- **Evaluation date**: 2026-08-08
- **Domain**: B. Security 46/100; D. CI/CD Health
- **Criteria breakdown**: CRITICAL duplicated API keys ship on every push; HIGH
  id-token/actions grants on non-merge workflows; fictitious GH_TOKEN in Checkout. The gate is
  a dead — no CI job invokes `check-workflow-security.js` (pre-commit hook only).
- **Evidence** (`node scripts/check-workflow-security.js` → exit 1, **12 violations**):
  - CRITICAL DUPLICATE_API_KEY: `parallel.yml:37`, `on-push.yml:26` (API_KEY = GEMINI_API_KEY)
  - HIGH ID_TOKEN_WRITE: `orchestrator.yml:9`, `opencode.yml:18`, `parallel.yml:16`, `architect-agent.yml:13`
  - HIGH ACTIONS_WRITE_NON_MERGE: `orchestrator.yml:13`, `opencode.yml:22`, `parallel.yml:15`, `architect-agent.yml:17`
  - HIGH GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN: `orchestrator.yml:33,41`, `architect-agent.yml`
  - **F004 re-count: 57 `secrets.*` refs / 10 unique names** across workflows.
- **Files**: `.github/workflows/*.yml`, `scripts/check-workflow-security.js`
- **Fix** (blocked on F050 `workflows: write`): remove API_KEY aliases, GH_TOKEN →
  GITHUB_TOKEN, drop id-token/actions from non-merge workflows, wire checker as required CI job.

## Issue 3 — F002 no `issues: write` permission (79th consecutive blocked)

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. CI/CD Health; Phase-1 output pipeline
- **Criteria breakdown**: integration token can read/write contents + pull-requests but not
  issues; in-graph `gh issue create` → 403 `createIssue` every run. Phase-1 prerequisite
  (GitHub-issue output) is structurally blocked.
- **Evidence**: `gh issue create --title "F002-probe"` → `GraphQL: Resource not accessible by
  integration (createIssue)`; `gh api user` → 403. `gh pr create`/`gh pr merge` work.
- **Fix** (out-of-loop): add `issues: write` to the workflow/agent permissions, or use a PAT.

## Issue 3 — F018 data STALE 19 days (P1) + refresh path blocked (upstream JSON-only)

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Migration Safety; B. Data pipeline
- **Criteria breakdown**: `check-freshness.js` → STALE (last update 2026-07-20, **19 days**,
  threshold 7). Upstream source (`suryavip/daftar-sekolah-indonesia`) now ships **JSON only**
  (`result/*.json`, no CSV — verified via `gh api` contents list: `010100.json`, `daftar-sekolah.sql`),
  so `fetch-data.js findCsvFiles()` has no `.csv` to consume → refresh will fail.
- **Files**: `data/schools.csv`, `scripts/fetch-data.js`, `scripts/etl.js`
- **Fix**: add JSON source support to fetch-data/etl or restore CSV upstream (code change —
  deferred fix-batch).

## Issue 5 — F005 Prettier drift — 74 files (docs ledger only, P3)

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-08
- **Domain**: C. Documentation Accuracy / DX Consistency
- **Criteria**: `npx prettier --check .` exits 1; **100% of warnings are under
  `docs/issues/**`** (generated audit reports and issue records), 0 source files affected.
- **Evidence**: `npx prettier --check .` → 74 files with code-style issues, all
  `docs/issues/2026-08-*/…`; `npm run lint` — clean; source is Prettier-clean.
- **Files**: `docs/issues/**`
- **Fix**: format audit/records in docs PR pipeline or accept ledger-only drift (low risk).

## Issue 6 — F014 parallel flake: clean 9th consecutive run (latent, P2)

- **Category**: `test` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Testability
- **Criteria**: fs/tmp race absent 9 consecutive verifications; suite **1056 pass / 0 fail /
  4 skipped** in this session (test:js + coverage run).
- **Evidence**: `npm run test:js` → `# tests 1060 / # pass 1056 / # fail 0 / # skipped 4`;
  `npm run test:js:coverage` → statements 94.94%, branches 92.2%, functions 96.65% (above 80/75
  gate, exit 0); post-test `git status` clean.
- **Files**: `scripts/etl-run.test.js`, `scripts/fetch-data.test.js`, `package.json`
- **Fix** (retained): per-test tmp dirs with cleanup-wait, or `--test-concurrency=1` in CI.

## Issue 7 — F024 sitemap emission: maintained RESOLVED (flaky), verified 8/8

- **Category**: `bug` / **Priority**: `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Determinism; B. Stability
- **Evidence**: `npm run build` → `dist/sitemap-index.xml` + `dist/sitemap-001.xml` present
  (28ms, budgets met); **8/8 clean** across runs. The sitemap now emits deterministically —
  RESOLVED and maintained.
- **Files**: `scripts/build-pages.js`, `scripts/sitemap.js`

## Issue 8 — F028: npm audit clean (maintained RESOLVED, P2)

- **Category**: `security` / **Priority**: `P2`
- **Evidence**: `npm audit` → `found 0 vulnerabilities` (exit 0). Continuously verified.

## Issue 9 — F064 lint-staged engine mismatch (P2) + F065 continue-on-error on critical CI steps (P2)

- **Category**: `ci` / **Priority**: `P2`
- **Date**: 2026-08-08
- **Criteria**:
  - F064: `node -v` = v20.20.2; `.nvmrc` = 22; `npm ls lint-staged` = 17.3.0 (demands
    >=22.22.10). `npm install` emits EBADENGINE warning.
  - F065: `on-pull.yml:44,51` mark **Checkout**/**Setup Node** `continue-on-error: true`;
    `parallel.yml:227` also continues on error. If these infra steps fail, the pipeline may
    still report success — masking failures.
- **Files**: `.nvmrc`, `.github/workflows/on-pull.yml`, `.github/workflows/parallel.yml`, `package.json`

## Summary table

| ID | Category | Priority | Status |
| -- | -------- | -------- | ------ |
| F063 | ci | P1 | CONFIRMED 21st consecutive |
| F037/F038 (incl. other 10 viol.) | security | P0 | HELD (12 violations live) |
| F002 | ci | P1 | BLOCKED (79th) |
| F018 | bug | P1 | CONFIRMED (19d STALE; refresh blocked) |
| F005 | docs | P3 | HELD (74 files, ledger-only) |
| F014 | test | P2 | clean 9th (latent) |
| F024 | bug | P2 | maintained RESOLVED (8/8) |
| F028 | security | P2 | maintained RESOLVED |
| F064 | ci | P2 | CONFIRMED |
| F065 | ci | P2 | CONFIRMED |