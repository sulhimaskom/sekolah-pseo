# Issue Records — 79th verification (labeled findings, GitHub-issue output blocked by F002)

**Labels**: multiple (see each record). **Format**: evaluation date / domain score table /
criteria-level breakdown / evidence / files affected. Local `gh issue create` returns HTTP 403
`createIssue` (F002 held 75 sessions; re-verified this run) — these records are the repo's
labeled issue channel for this run. Deliverable ships as docs PR (convention).

## Issue 1 — F063 orchestrator chronic failure: dead `GH_TOKEN` secret (20th consecutive)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health 42/100); B. System (Stability)
- **Criteria breakdown**: the highest-autonomy self-scheduled agent (orchestrator.yml) has
  failed every scheduled run for 20 consecutive days (2026-07-20 → 2026-08-08); root cause is a
  fictitious secret, not a code defect. Streak extended 13 → 20 this run (window widened).
- **Evidence** (this session):
  - `gh run list --workflow=orchestrator.yml --limit 20` → **20 visible, ALL `failure`**
    (2026-07-20T02:22:22Z → 2026-08-08T01:04:58Z).
  - `gh run view <latest> --log-failed` → Checkout dies on all attempts:
    `##[error]fatal: could not read Username for 'https://github.com': terminal prompts disabled`
    (×3, git exit 128) — `orchestrator.yml` passes fictitious `secrets.GH_TOKEN`.
- **Files**: `.github/workflows/orchestrator.yml`
- **Fix** (blocked on token): replace `${{ secrets.GH_TOKEN }}` with `${{ secrets.GITHUB_TOKEN }}`
  or provide a real fine-grained PAT; needs `workflows: write` on the loop token (F050) or a
  maintainer with secret access.

## Issue 2 — Workflow security regressions: 12 live violations (F037, F038, F013, F056–F059)

- **Category/priority**: `security` / `P0`
- **Evaluation date**: 2026-08-08
- **Domain**: B. System Quality (Security 46/100); D. CI/CD Health 42/100
- **Criteria breakdown**: Security practices — CRITICAL duplicate API keys ship on every push;
  HIGH id-token/actions grants on non-merge workflows; fictitious `GH_TOKEN` passed in Checkout
  steps. Gate is dead: no CI job invokes `check-workflow-security.js` (pre-commit-only hook).
- **Evidence** (`node scripts/check-workflow-security.js` → exit 1, 12 violations):
  - CRITICAL `DUPLICATE_API_KEY`: `parallel.yml`, `on-push.yml`
  - HIGH `ID_TOKEN_WRITE`: `parallel.yml:16`, `orchestrator.yml:9`, `opencode.yml:18`,
    `architect-agent.yml:13`
  - HIGH `ACTIONS_WRITE_NON_MERGE`: `parallel.yml:15`, `orchestrator.yml:13`,
    `opencode.yml:22`, `architect-agent.yml:17`
  - HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`: `parallel.yml`, `on-push.yml`, `orchestrator.yml`,
    `architect-agent.yml`
- **Impact**: F037 (public-repo `issue_comment` → unauthenticated write-token LLM agent) and
  F038 (shell RCE via `workflow_dispatch custom_prompt` heredoc injection) remain externally
  triggerable on a PUBLIC repo — unfixed for 28+ runs.
- **Files**: `.github/workflows/parallel.yml`, `on-push.yml`, `orchestrator.yml`,
  `opencode.yml`, `architect-agent.yml`, `.husky/pre-commit`
- **Fix** (blocked on `workflows: write` / Actions-level secrets): delete API_KEY aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop id-token/actions from non-merge workflows, wire the checker as a
  required CI job.

## Issue 3 — F002 no `issues: write` permission — 75 consecutive blocked issues (P1)

- **Category/priority**: `ci` / `P1`
- **Evaluation date**: 2026-08-08
- **Domain**: D. Delivery & Evolution (CI/CD Health); Phase-1 output pipeline
- **Criteria breakdown**: the loop token (`github-actions[bot]` integration) can read/write
  contents and pull-requests but not issues. Every Phase-1 requirement to create GitHub issues
  is denied; the repo convention is to ship labeled docs records instead.
- **Evidence**: `gh issue create` probe → `GraphQL: Resource not accessible by integration
(createIssue)` — 75 consecutive runs (this family). `gh pr create`/`gh pr merge` work
  (proven by the docs PR shipped this run).
- **Effect on this run**: Phase-1 issue output for the findings below is recorded here rather
  than as gh issues.
- **Fix**: add `issues: write` to the workflow/agent token (out-of-loop secret, maintainer action).

## Issue 4 — F018 data STALE 19 days (P1) + F005 Prettier drift (P3, docs-ledger only)

- **Category/priority**: `bug` / `P1` (data freshness); `docs` / `P3` (formatting)
- **Evaluation date**: 2026-08-08
- **Domain**: D. Migration Safety; C. Documentation Accuracy
- **Criteria breakdown**:
  - F018: `node scripts/check-freshness.js` → `Status: STALE`, last update 2026-07-20
    (**19 days**, threshold 7), 2 records. Data pipeline readiness is the single worst runtime
    signal.
  - F005: `npx prettier --check .` → **74 files**, 100% under `docs/issues/**` (0 source
    files). Source (`scripts/`, `src/`, `tests/`) is Prettier-clean; the ledger itself grows a
    couple of files per run (72 → 74 with the 78th-run records).
- **Evidence**: freshness and prettier commands, exit codes captured in the audit report.
- **Files**: `data/schools.csv` (F018); `docs/issues/**` (F005)
- **Fix suggestions**: F018 — re-run `npm run fetch-data` + `npm run etl` when network permits;
  F005 — run `npx prettier --write "docs/issues/**"` in the docs PR pipeline or accept the
  ledger-only drift as a known debt (recommend: do formatting, keep `format:check` on source).
  This run's own records were Prettier-clean at commit to avoid adding to the drift.

## Issue 5 — F014 parallel test flake: clean 5th consecutive run (latent, P2)

- **Category/priority**: `test` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Code Quality (Testability 74; Determinism 78)
- **Criteria breakdown**: the fs/tmp race has been absent five consecutive verification runs;
  the suite completed **1056 pass / 0 fail / 4 skipped twice in this session**.
- **Evidence** (firsthand this run): `npm run test:js` ×2 → `# tests 1060 / # pass 1056 /
  # fail 0 / # skipped 4`both times; post-test`git status` clean (no tracked CSV mutation).
- **Files**: `scripts/etl-run.test.js`, `scripts/fetch-data.test.js`, `package.json` test scripts
- **Risk**: remains latent while tests share DIST_DIR/tmp; probability observed: ~1-in-5 runs
  (F029-family). Keep concurrency guard recommendation in the ledger.
- **Fix suggestion** (retained, low priority): per-test unique tmp workspace with explicit
  cleanup wait, or `--test-concurrency=1` in CI test scripts.

## Issue 6 — F024 build emits sitemap: deterministic 4/4 (maintained RESOLVED, P2)

- **Category/priority**: `bug` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Determinism; B. Stability
- **Criteria breakdown**: the once-observed missing-sitemap regression (F024) has now been
  absent four consecutive runs with three fresh builds in this session.
- **Evidence**: `rm -rf dist && npm run build` ×3 → `dist/sitemap-index.xml` +
  `dist/sitemap-001.xml` present every run (25–32ms, 0 failed pages, budgets met); plus the
  78th-run build → 4/4 total.
- **Files**: `scripts/build-pages.js`, `scripts/sitemap.js`
- **Status**: maintained RESOLVED — keep `robots.txt`/sitemap emission covered by the build
  perf report as the regression guard.

## Issue 7 — F028 npm audit clean: 12th consecutive (maintained RESOLVED, P2)

- **Category/priority**: `security` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: A. Dependency Discipline
- **Criteria breakdown**: the brace-expansion high vulnerability was resolved in the 32nd
  issue-records run and has stayed clean for 12 verification runs.
- **Evidence**: `npm audit` → `found 0 vulnerabilities` (exit 0).
- **Files**: `package.json`, `package-lock.json`
- **Status**: maintained RESOLVED.

## Issue 8 — F064 lint-staged engine mismatch (node >=22.22.1 vs env v20.20.2) (P2)

- **Category/priority**: `ci` / `P2`
- **Evaluation date**: 2026-08-08
- **Domain**: B. Config & Env Parity
- **Criteria breakdown**: `npm ci` completes but emits `EBADENGINE` for lint-staged@17.3.0
  (engine demands ≥22.22.1). `.nvmrc` says `22`, CI `on-pull.yml`/`on-push.yml` can pin node 20.
  Consensus: settle on node 22.22.1+ across `.nvmrc`, CI matrix, and dev-env.
- **Evidence**: `node -v` `v20.20.2`; `npm ci` EBADENGINE warning this run.
- **Files**: `.nvmrc`, `.github/workflows/on-pull.yml`, `.github/workflows/on-push.yml`,
  `package.json`
- **Fix**: align to node 22.22.1 (>=22.22.1) in all node-pinning locations.

## Summary table (findings in this run)

| ID             | Category | Priority | Status                                |
| -------------- | -------- | -------- | ------------------------------------- |
| F063           | ci       | P1       | CONFIRMED 20th consecutive (GH_TOKEN) |
| F037/F038      | security | P0/P1    | HELD (of 12 violations)               |
| F013/F056–F059 | security | P1/P2    | HELD                                  |
| F002           | ci       | P1       | CONFIRMED 75th                        |
| F018           | bug      | P1       | HELD (STALE 19d)                      |
| F005           | docs     | P3       | HELD (docs-ledger only, 74 files)     |
| F014           | test     | P2       | clean ×5 (latent)                     |
| F024           | bug      | P2       | RESOLVED deterministic 4/4            |
| F028           | security | P2       | RESOLVED — 12th clean                 |
| F064           | ci       | P2       | CONFIRMED                             |
