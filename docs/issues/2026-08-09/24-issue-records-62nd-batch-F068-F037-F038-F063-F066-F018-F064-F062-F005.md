# Issue Records — 62nd batch (103rd verification run, 2026-08-09)

**Evaluation Date**: 2026-08-09
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`dad4d8f`)
**Note**: GitHub-issue creation remains blocked by F002 (integration token lacks `issues: write`; 93rd consecutive 403 re-verified this run via REST `gh issue create` → `GraphQL: Resource not accessible by integration (createIssue)`; API permissions probe = all `false`). Per repo convention findings ship as labeled docs records — each carrying exactly one category label and one priority label (contract §4) — ready for bulk creation the moment permissions are granted.

## Issue 1 — F068 (P1, test): pytest gate not reproducible — `No module named pytest` NEW

- **Category**: `test` / **Priority**: `P1`
- **Files affected**: `requirements.txt`, `package.json` (`test:py:pytest`, `test:all`), `tests/test_basic.py`, `tests/test_data_validation.py`, `docs/setup.md`
- **Domain table**: A — Testability 68/100, Dependency Discipline 82/100; D — CI/CD Health 42/100, Config & Env Parity 38/100
- **Evidence** (firsthand this run): `npm run test:py:pytest` → `/usr/bin/python3: No module named pytest`; `npm run test:all` → FAIL at the test:py:pytest stage. `requirements.txt` declares `pytest>=7.0.0`, but the runtime environment has no pytest (pip freeze shows only Jinja2/requests/setuptools). `tests/run_tests.py` fallback passes 27/27 via `npm run test:py`, so `npm test` is green, but the pytest file suite (13 tests incl. F066's dist-wipe test) never executes. `grep -rn "pip" .github/workflows/*.yml` → 0 hits — no CI workflow installs Python deps; `docs/setup.md` marks pytest "Optional".
- **Impact / Risk**: the Python test gate is not reproducible from a clean checkout; CI can silently skip the pytest suite (including the F066 regression test); F066's empirical reproduction depended on an environment that happens to have pytest installed.
- **Fix** (safe): add `pip install -r requirements.txt` to CI (or a `test:py:pytest` setup step), and pin pytest in requirements.txt with a documented install path in setup.md.

## Issue 2 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD

- **Category**: `security` / **Priority**: `P0`
- **Files affected**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`, `opencode.yml`, `orchestrator.yml`, `template.md`
- **Domain table**: B. System Quality — Security Practices 46/100 (criterion-level deduction applied for CI-pipeline surfaces, not runtime)
- **Evidence** (firsthand this run): `node scripts/check-workflow-security.js` (exit code captured unpiped) → exit 1, **12 violations**: 2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml — `API_KEY` == `GEMINI_API_KEY`); 10× HIGH — `ID_TOKEN_WRITE` ×4 (architect/opencode/orchestrator/parallel), `ACTIONS_WRITE_NON_MERGE` ×4, `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×2 (orchestrator.yml:9,13; architect-agent.yml, template.md:33).
- **Impact / Risk**: overbroad CI permissions + duplicate secret alias exposed to AI-agent steps on every push; static site runtime unaffected. `.husky/pre-commit:3` swallows the checker failure (`|| echo skipped`), so the local gate is effectively dead.
- **Fix** (blocked — requires `.github/workflows/*` write): delete `API_KEY` aliases, s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from non-merge workflows, wire the checker as a failing CI job.

## Issue 3 — F063 (P1, ci): orchestrator.yml DEAD — 5/5 recent runs failed

- **Category**: `ci` / **Priority**: `P1`
- **Files affected**: `.github/workflows/orchestrator.yml`
- **Domain table**: D — CI/CD Health 42/100
- **Evidence**: `gh run list --workflow=orchestrator.yml --limit 5` → **5/5 `failure`** (2026-08-05 01:53Z … 2026-08-09 01:09Z, databaseId 31287686640). Workflow uses `secrets.GH_TOKEN` (fictitious; repo has no `GH_TOKEN` secret) at checkout and env. Also confirmed this run: NO CI workflow (`on-push.yml`, `on-pull.yml`, `orchestrator.yml`, `parallel.yml`, `architect-agent.yml`, `opencode.yml`) executes `npm run build|test|lint` as an automated quality gate — the only `npm` reference is `npm ci || true` in parallel.yml (agent caching).
- **Impact / Risk**: scheduled orchestrator job has produced zero successful runs for 5+ consecutive days; maintenance automation for the repo is dead; no automated quality gate blocks regressions.
- **Fix** (blocked): replace `secrets.GH_TOKEN` with `secrets.GITHUB_TOKEN` (2 sites), then re-run; consider adding a true CI gate job (lint + build + test) to on-pull.yml.

## Issue 4 — F066 (P1, test): pytest test wipes real dist/ build output — HELD (root cause by inspection)

- **Category**: `test` / **Priority**: `P1`
- **Files affected**: `tests/test_data_validation.py:70-87`
- **Domain table**: A — Testability 68/100, Determinism 72/100; B — Stability 74/100
- **Evidence** (code inspection this run): `test_dist_directory_can_be_created` computes `dist_dir = root/dist` (the real build output dir) and executes `shutil.rmtree(dist_dir, ignore_errors=True)` before AND after the assertion. Empirical re-reproduction unavailable this run because pytest is absent from the environment (F068), but the wipe code path is unambiguous by inspection and was deterministically reproduced in the 101st/102nd runs (test → dist/ MISSING → rebuild restores).
- **Impact / Risk**: any CI job running pytest before a downstream dist consumer silently deletes build artifacts; incremental builds/validations against dist operate on missing output; local dev confusion (build "disappears").
- **Fix** (safe, atomic): use `tempfile.TemporaryDirectory` for the create/delete check instead of the real `dist/`; keep the assertion semantics (dir creatable) unchanged.

## Issue 5 — F018 (P2, chore): school data STALE 20 days (threshold 7)

- **Category**: `chore` / **Priority**: `P2`
- **Files affected**: `data/schools.csv`, `external/raw.csv`
- **Domain table**: A — Correctness 78/100 (data pipeline currency)
- **Evidence**: `node scripts/check-freshness.js` → "Last Update: 2026-07-20 (20 days ago)", Status ⚠️ STALE; `updated_at` field = 2026-07-20. Data set is a 2-record sample, so real freshness requires the upstream fetch (`npm run fetch-data`) or a documented sample policy.
- **Impact / Risk**: produced site shows stale school data; freshness gate fails on every check until data is refreshed or the threshold is documented for the sample dataset.
- **Fix**: either run `npm run fetch-data` + `npm run etl` to refresh, or if the 2-record file is a fixture, exclude it from freshness policy / document explicitly.

## Issue 6 — F064 (P2, ci): Node engine parity gap — .nvmrc=22 vs CI 20 vs workflow 20

- **Category**: `ci` / **Priority**: `P2`
- **Files affected**: `.nvmrc`, `.github/workflows/on-pull.yml:53`, `package.json` (`lint-staged@17.3.0` engines: node >=22.22.1)
- **Domain table**: D — Config & Env Parity 38/100; A — Dependency Discipline 82/100
- **Evidence**: `.nvmrc` = `22`; runtime `node --version` = v20.20.2; `on-pull.yml` uses `node-version: 20`; `lint-staged@17.3.0` declares engines `node >=22.22.1` (verified via package.json read). Three sources disagree; pre-commit lint-staged may fail at runtime under the pinned engine.
- **Impact / Risk**: local dev on `.nvmrc`-pinned Node 22 diverges from CI Node 20; lint-staged silently degraded.
- **Fix**: align all three on one version (recommend 22 per `.nvmrc`), then re-verify `npm install` clean and lint-staged functional.

## Issue 7 — F062 (P2, ci): no release workflow, 0 git tags, rollback not automated

- **Category**: `ci` / **Priority**: `P2`
- **Files affected**: `.github/workflows/` (absent release workflow), `git tag` (0 tags), `CHANGELOG.md` (manual)
- **Domain table**: D — Release & Rollback Safety 44/100
- **Evidence**: `git tag | wc -l` → 0; no `release` workflow file under `.github/workflows/`; `CHANGELOG.md` hand-maintained; version pinned 1.0.0 in package.json.
- **Impact / Risk**: no reproducible release path, no versioned artifacts, no rollback procedure; deployment is manual and unverifiable.
- **Fix** (blocked on permissions): add a tag-triggered release workflow (tag → build → artifact attach → changelog), document rollback in `docs/deployment.md`.

## Issue 8 — F005 (P3, chore): Prettier drift — 88 files unformatted, docs ledger only

- **Category**: `chore` / **Priority**: `P3`
- **Files affected**: `docs/issues/**` (88 files), 0 source files
- **Domain table**: A — Consistency 55/100; D — Technical Debt Exposure 60/100
- **Evidence**: `npx prettier --check .` → exit 1, 88 files, all under `docs/issues/`; `npx prettier --check scripts/ src/ tests/ public/ .github/ eslint.config.js package.json` → **exit 0**. Source tree is clean; drift is confined to the run-ledger documentation.
- **Impact / Risk**: `npm run format:check` fails repo-wide, blocking CI format gates on unrelated docs churn.
- **Fix** (safe): run `npx prettier --write "docs/issues/**"` once, or exclude `docs/issues/**` from format checking (documented decision).

## Findings verified CLEAN this run (no issue needed)

- F024 — sitemap emitted on every fresh build ✅
- F028 — `npm audit` 0 vulnerabilities ✅
- F057/F017 — `addNumbers` absent from docs/api.md and source ✅
- F014 — 13th+ consecutive clean run, no re-observation ✅
- Build / Lint / JS tests / run_tests.py harness / coverage gate — all green ✅
