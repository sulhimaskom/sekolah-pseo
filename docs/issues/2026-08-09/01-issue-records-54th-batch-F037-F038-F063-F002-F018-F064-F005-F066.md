# Issue Records — 54th batch (95th verification run, 2026-08-09)

**Evaluation Date**: 2026-08-09
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`be54542`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`;
91st consecutive 403 re-verified this run via REST `gh issue create` → `GraphQL: Resource not
accessible by integration (createIssue)`). Per repo convention findings ship as labeled docs
records — each carrying exactly one category label and one priority label (contract §4) —
ready for bulk creation the moment permissions are granted.

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD

- **Category**: `security` / **Priority**: `P0`
- **Files affected**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`,
  `opencode.yml`, `orchestrator.yml`, `template.md`
- **Domain table**: B. System Quality — Security Practices 46/100 (criterion-level deduction
  applied for CI-pipeline surfaces, not runtime)
- **Evidence** (firsthand this run): `node scripts/check-workflow-security.js` → exit 1,
  **12 violations**: 2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml `API_KEY` ==
  `GEMINI_API_KEY`); 10× HIGH — `ID_TOKEN_WRITE` ×4 (architect/opencode/orchestrator/parallel),
  `ACTIONS_WRITE_NON_MERGE` ×4, `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×2 (orchestrator:33,41,
  architect-agent:37, template.md:33)
- **Impact / Risk**: identical to 39th–94th — overbroad CI permissions + duplicate secret
  alias on every push; static site unaffected. `.husky/pre-commit:3` still swallows the
  checker failure (`|| echo skipped`), so the local gate is effectively dead.
- **Fix** (blocked — requires `.github/workflows/*` write): delete `API_KEY` aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from non-merge workflows, wire the
  checker as a CI job.

## Issue 2 — F063 (P1, ci): orchestrator.yml DEAD — fictitious GH_TOKEN, 7th consecutive day

- **Category**: `ci` / **Priority**: `P1`
- **Files affected**: `.github/workflows/orchestrator.yml`
- **Evaluation date**: 2026-08-09 · **Domain**: D — CI/CD Health 46/100
- **Evidence**: `gh run list --workflow=orchestrator.yml --limit 6` → **6/6 `failure`**
  (2026-08-04 01:52Z … 2026-08-09 01:09Z, databaseId 31287686640). Checkout dies in the
  auth step (`fatal: could not read Username for 'https://github.com'`) because
  `orchestrator.yml` passes `token: ${{ secrets.GH_TOKEN }}` / `GH_TOKEN` env — a secret that
  does not exist for scheduled runs (same class as F061). The repo's highest-autonomy
  scheduled agent has therefore been dead for 6 consecutive days with no remediation path.
- **Fix** (blocked on `secrets` visibility / `workflows:write`): point Checkout and env at
  `secrets.GITHUB_TOKEN` or a real PAT — exactly what the security checker demands.

## Issue 3 — F002 (P1, ci): agent token lacks `issues: write` — 91st consecutive

- **Category**: `ci` / **Priority**: `P1`
- **Evaluation date**: 2026-08-09 · **Domain**: D. CI/CD Health
- **Evidence**: REST `gh issue create` → `GraphQL: Resource not accessible by integration
(createIssue)` (FORBIDDEN). Token scopes confirmed: `github-actions[bot]` with
  `contents`/`pull-requests` only. Every Phase-1/2/3 output contract ships as labeled docs
  records (repo convention) until elevated.
- **Impact**: loop cannot satisfy the contract's GitHub-issue output channel directly; docs
  ledger is the substitute channel. `gh pr create` + merge remain functional (94 PR merges
  to date).

## Issue 4 — F018 (P1, bug): data STALE 20 days — grew 19→20d

- **Category**: `bug` / **Priority**: `P1`
- **Evaluation date**: 2026-08-09 · **Domain**: B Genesis (data pipeline)
- **Evidence**: `node scripts/check-freshness.js` → `Last Update: 2026-07-20 (20 days ago)`,
  `Status: STALE` (threshold 7d, 2 records).
- **Impact**: static-site freshness commitment unmet; risk of stale school data republication.
- **Fix** (blocked): upstream source is JSON-only while `fetch-data.js` consumes a CSV
  contract — needs an upstream-contract change (genuine feature cycle, not local code).

## Issue 5 — F064 (P2, ci): lint-staged engine drift — EBADENGINE + node parity gap

- **Category**: `ci` / **Priority**: `P2`
- **Files affected**: `package.json` (devDependency `lint-staged@17.3.0`), `.nvmrc`,
  `.github/workflows/on-pull.yml:53`
- **Evaluation date**: 2026-08-09 · **Domain**: D. Config & Env Parity (≈40/100)
- **Evidence**: `npm ci` → `EBADENGINE: lint-staged@17.3.0 requires node >=22.22.1, current
v20.20.2`. Parity triangle disagree: `.nvmrc` declares 22, `on-pull.yml` pins node 20,
  runner executes v20.20.2. Pre-commit lint-staged hooks may fail at runtime on the CI image.
- **Impact**: low (install warns but succeeds); medium long-term (hook fragility).
- **Fix**: align the three (recommend downgrading `lint-staged` to a node-20-compatible
  major, e.g. ^15, or aligning all surfaces to node 22).

## Issue 6 — F005 (P3, docs): Prettier drift — HELD at 88 files (4th flat)

- **Category**: `docs` / **Priority**: `P3`
- **Evaluation date**: 2026-08-09 · **Domain**: A. Consistency 55/100; D. Tech Debt
- **Evidence**: `npx prettier --check .` → exit 1, **88 files, all under `docs/issues/**`**,
  0 in src/scripts (verified by counting docs/issues path matches == totals). This run's
  records authored compliant → drift stays halted (4th consecutive flat run).
- **Impact**: cosmetic-only; `format:check` fails on docs ledger. `prettier --write
docs/issues` once would close it, but that is a cosmetics-class change the contract's
  Phase 2 prohibits.

## Issue 7 — F066 (P1, test): dist-destruction flake — LATENT (6/6 clean)

- **Category**: `test` / **Priority**: `P1`
- **Evaluation date**: 2026-08-09 · **Status**: LATENT — no re-observation in ~47 cycles
- **Evidence**: `rm -rf dist; for i in 1..6: npm run build; test -f dist/sitemap-index.xml`
  → 6/6 present. Cumulative observed rate ≈ 1/135 cycles (single event at 89th run; never
  reproduced since). No deterministic repro → per FAIL-SAFE no root-cause guess recorded.

## Issue 8 — MAINTAINED: F028 (0 vulns), F024 (sitemap emitted), F057/F017 (no `addNumbers`), F062 (docs drift HELD)

- **Category/priority**: `security`/`P2` (F028), `bug`/`P2` (F024), `docs`/`P3` (F057/F017),
  `docs`/`P3` (F062)
- **Evidence this run**: `npm audit` → 0 vulnerabilities; build output contains
  `dist/sitemap-index.xml` + `dist/sitemap-001.xml` after every fresh build (6/6);
  `grep addNumbers docs/api.md scripts src` → 0 matches; `docs/release.md:67` still
  references phantom `.github/workflows/release.yml` (no such file — ledger).

---

**Net movement this run (95th)**: F005 HELD 88→88 (4th flat run, records authored
compliant); F018 19→20d (STALE); F063 6/6 recent runs failed (incl. tonight's nightly);
F002 91st consecutive; F066 6/6 clean (no re-observation); F024/F028/F057 maintained.
No new root-cause-defect recorded — **13th consecutive run**. Worktree clean; nothing
deleted; no secrets logged.
