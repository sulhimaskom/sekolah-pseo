# Issue Records — 59th batch (100th verification run, 2026-08-09)

**Evaluation Date**: 2026-08-09
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`4bfae71`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`;
96th consecutive 403 re-verified this run via REST `gh issue create` → `GraphQL: Resource
not accessible by integration (createIssue)`). Per repo convention findings ship as labeled
docs records — each carrying exactly one category label and one priority label (contract
§4) — ready for bulk creation the moment permissions are granted.

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD

- **Category**: `security` / **Priority**: `P0`
- **Files affected**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`,
  `opencode.yml`, `orchestrator.yml`
- **Domain table**: B. System Quality — Security Practices 46/100 (criterion-level deduction
  applied for CI-pipeline surfaces, not runtime)
- **Evidence** (firsthand this run): `node scripts/check-workflow-security.js` → exit 1,
  **12 violations**: 2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml `API_KEY`
  == `GEMINI_API_KEY`); 10× HIGH — `ID_TOKEN_WRITE` ×4 (architect-agent.yml:13,
  opencode.yml:18, orchestrator.yml:9, parallel.yml:16); `ACTIONS_WRITE_NON_MERGE` ×4
  (architect-agent.yml:17, opencode.yml:22, orchestrator.yml:13, parallel.yml:15);
  `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×2 (architect-agent.yml, orchestrator.yml — 2
  occurrences). Set identical to 99th run (12 total, 2C + 10H).
- **Impact / Risk**: overbroad CI permissions + duplicate secret alias on every push;
  static site unaffected. `.husky/pre-commit:3` still swallows the checker failure
  (`|| echo skipped`), so the local gate is effectively dead.
- **Fix** (blocked — requires `.github/workflows/*` write): delete `API_KEY` aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from non-merge workflows, wire the
  checker as a CI job.

## Issue 2 — F063 (P1, ci): orchestrator.yml dead — fictitious GH_TOKEN, 11th day

- **Category**: `ci` / **Priority**: `P1`
- **Files affected**: `.github/workflows/orchestrator.yml`
- **Evaluation date**: 2026-08-09 · **Domain**: D — CI/CD Health 46/100
- **Evidence**: `gh run list --workflow=orchestrator.yml --limit 6` → **6/6 `failure`**
  (2026-08-05 01:53Z … 2026-08-09 01:09Z, latest databaseId 31287686640); no new
  orchestrator run since the 99th. Checkout dies in the fiction of `secrets.GH_TOKEN`
  (workflow writes `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×2 — orchestrator.yml:33,41) — root
  cause recorded since run 51.
- **Impact / Risk**: the repository's autonomy contract depends on orchestrator runs that
  never succeed; nightly schedule keeps failing silently.
- **Fix** (blocked — workflow boundary): s/GH_TOKEN/GITHUB_TOKEN in orchestrator.yml then
  re-run; no source logic involved.

## Issue 3 — F018 (P1, enhancement): data STALE 20 days (threshold 7)

- **Category**: `enhancement` / **Priority**: `P1`
- **Files affected**: `data/schools.csv` (2 records, last update 2026-07-20)
- **Evaluation date**: 2026-08-09 · **Domain**: D — Delivery & Evolution Readiness
- **Evidence**: `node scripts/check-freshness.js` → `date: 2026-07-20, daysAgo: 20,
isFresh: false` → exit 1. Steady at 20 days since the 96th.
- **Impact / Risk**: generated site shows a 2-school demo dataset; freshness gate fails
  until the upstream JSON→CSV fetch contract lands (genuine feature cycle).
- **Suggested fix** (deferred): implement the JSON-only ETL fallback documented in
  `docs/issues/2026-07-18/*` and re-run `npm run fetch-data`.

## Issue 4 — F005 (P2, test): Prettier drift HELD at 88 files — 9th flat run

- **Category**: `test` / **Priority**: `P2`
- **Files affected**: 88 files, all under `docs/issues/**` (0 production-source hits)
- **Evaluation date**: 2026-08-09 · **Domain**: A — Consistency 55/100
- **Evidence**: `npx prettier --check .` (NO_COLOR) → "Code style issues found in 88
  files"; per-file scan confirms all 88 under `docs/issues/` (the summary line carries
  `[warn]` too and is excluded from the file count). This run's records authored
  Prettier-compliant → count stays at 88 (+0, 9th flat run 92nd–100th).
- **Impact / Risk**: lint gate (`npm run lint`) still exits 0 (ESLint-only); `format:check`
  would fail until the 88 ledger files are reformatted. Cosmetic-only → Phase-2 bucket.
- **Fix** (deferred — cosmetic bucket): one `npx prettier --write docs/issues` pass.

## Issue 5 — F066 (P2, bug): dist-destruction flake — LATENT, no repro

- **Category**: `bug` / **Priority**: `P2`
- **Files affected**: scripts/build-pages.js / scripts/fs-safe.js (suspected surface)
- **Evaluation date**: 2026-08-09 · **Domain**: B — Stability 74/100 (deduction applied)
- **Evidence**: 6-cycle `rm -rf dist && npm run build && ls dist/sitemap-index.xml` → 6/6
  present. Clean window ≈165 consecutive cycles (~1/171 cumulative). **No deterministic
  repro in-session** — kept LATENT per FAIL-SAFE rule (do not guess / do not blind-fix).

## Issue 6 — F002 (P1, ci): GitHub-issue creation blocked — 96th consecutive

- **Category**: `ci` / **Priority**: `P1`
- **Evidence**: REST `gh issue create` → `GraphQL: Resource not accessible by integration
(createIssue)`; token identity is `github-actions[bot]` (GITHUB_TOKEN) with read-only
  grants — `gh api user` also 403. `open_issues: 0` — the repo's issue-facing surface
  cannot be fed by this agent.
- **Impact / Risk**: contract Phase-1/2/3 output mandate (create issues from findings)
  cannot be executed; findings ship as labeled docs records instead (repo convention).
- **Fix** (deferred): grant `workflows: write` + `issues: write` on the GitHub App token.

## Issue 7 — F064 (P2, config): lint-staged engine mismatch HELD

- **Category**: `chore` / **Priority**: `P2`
- **Files**: package.json (devDeps `lint-staged@17.3.0`), .nvmrc (=22),
  .github/workflows/on-pull.yml:53 (node-version 20)
- **Evaluation date**: 2026-08-09 · **Domain**: D — Config & Env Parity 40/100
- **Evidence**: `npm install --no-audit` → `EBADENGINE Unsupported engine
lint-staged@17.3.0 … required node >=22.22.1, current v20.20.2`. `.nvmrc` pins 22 while
  CI and this runner run 20 → three-way parity gap. Dependabot PR already failed on-push
  for the same reason (recorded since 98th).
- **Impact / Risk**: pre-commit/husky lint-staged fails on Node 20 environments.
- **Fix** (deferred — env parity): align `lint-staged` to the deployed engine or
  standardize the Node version ring at 22.

## Maintained / closed-state ledger (no new action)

| ID   | State                 | Note                                                    |
| ---- | --------------------- | ------------------------------------------------------- |
| F028 | RESOLVED (maintained) | npm audit 0 vulnerabilities                             |
| F057 | FIXED (maintained)    | addNumbers 0 matches in docs/api.md                     |
| F017 | FIXED (maintained)    | (part of F057 family)                                   |
| F062 | HELD (docs-only)      | release.md phantom reference remains (docs-cleanup)     |
| F024 | HELD (maintained)     | sitemap emitted on every fresh build                    |
| F063 | HELD (P1)             | re-confirmed 6/6 (never a separate issue — see Issue 2) |

## Issue-procedure note

No duplicate issues exist (all current findings map 1:1 to ledger IDs, each unique). No
new finding recorded this run — 18th consecutive run without a new root-caused defect.
