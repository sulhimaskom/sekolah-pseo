# Issue Records — 56th batch (97th verification run, 2026-08-09)

**Evaluation Date**: 2026-08-09
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`b9bf091`)
**Note**: GitHub-issue creation remains blocked by F002 (agent token lacks `issues: write`;
93rd consecutive 403 re-verified this run via REST `gh issue create` → `GraphQL: Resource not
accessible by integration (createIssue)`). Per repo convention findings ship as labeled docs
records — each carrying exactly one category label and one priority label (contract §4) —
ready for bulk creation the moment permissions are granted.

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD

- **Category**: `security` / **Priority**: `P0`
- **Files affected**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`,
  `opencode.yml`, `orchestrator.yml`
- **Domain table**: B. System Quality — Security Practices 46/100 (criterion-level deduction
  applied for CI-pipeline surfaces, not runtime)
- **Evidence** (firsthand this run): `node scripts/check-workflow-security.js` → exit 1,
  **12 violations**: 2× CRITICAL `DUPLICATE_API_KEY` (on-push.yml, parallel.yml `API_KEY` ==
  `GEMINI_API_KEY`); 10× HIGH — `ID_TOKEN_WRITE` ×4 (architect-agent.yml:13,
  opencode.yml:18, orchestrator.yml:9, parallel.yml:16); `ACTIONS_WRITE_NON_MERGE` ×4
  (architect-agent.yml:17, opencode.yml:22, orchestrator.yml:13, parallel.yml:15);
  `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×2 (architect-agent.yml — 1 occurrence,
  orchestrator.yml — 2 occurrences). Note: this run's output no longer flags
  `template.md` (has been named in other runs' summaries; first-hand count here = 12).
- **Impact / Risk**: unchanged from 39th–96th — overbroad CI permissions + duplicate secret
  alias on every push; static site unaffected. `.husky/pre-commit:3` still swallows the
  checker failure (`|| echo skipped`), so the local gate is effectively dead.
- **Fix** (blocked — requires `.github/workflows/*` write): delete `API_KEY` aliases,
  s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from non-merge workflows, wire the
  checker as a CI job.

## Issue 2 — F063 (P1, ci): orchestrator.yml DEAD — fictitious GH_TOKEN, 8th consecutive day

- **Category**: `ci` / **Priority**: `P1`
- **Files affected**: `.github/workflows/orchestrator.yml`
- **Evaluation date**: 2026-08-09 · **Domain**: D — CI/CD Health 46/100
- **Evidence**: `gh run list --workflow=orchestrator.yml --limit 6` → **6/6 `failure`**
  (2026-08-04 01:52Z … 2026-08-09 01:09Z, databaseId 31287686640). Checkout dies in the
  fiction of `secrets.GH_TOKEN` (workflow writes `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` ×2 —
  orchestrator.yml:33,41) — the same root cause recorded since run 51.
- **Impact / Risk**: the repository's autonomy contract depends on orchestrator runs that
  never succeed; nightly schedule keeps failing silently.
- **Fix** (blocked — workflow boundary): s/GH_TOKEN/GITHUB_TOKEN in orchestrator.yml then
  re-run; no source logic involved.

## Issue 3 — F018 (P1, enhancement): data STALE 20 days (threshold 7)

- **Category**: `enhancement` / **Priority**: `P1`
- **Files affected**: `data/schools.csv` (2 records, last update 2026-07-20)
- **Evaluation date**: 2026-08-09 · **Domain**: D — Delivery & Evolution Readiness
- **Evidence**: `node scripts/check-freshness.js` → `Last Update: 2026-07-20 (20 days ago)` →
  `Status: STALE` → exit 1. Grew 0d since 96th (had been 19→20→20-steady).
- **Impact / Risk**: generated site shows 2-school demo dataset; freshness gate fails CI
  surface until the upstream JSON→CSV fetch contract lands (genuine feature cycle, not a
  defect).
- **Suggested fix** (deferred): implement the JSON-only ETL fallback documented in
  `docs/issues/2026-07-18/*` and re-run `npm run fetch-data`.

## Issue 4 — F005 (P2, test): Prettier drift HELD at 88 files — 6th flat run

- **Category**: `test` / **Priority**: `P2`
- **Files affected**: 88 files, all under `docs/issues/**` (0 production-source hits)
- **Evaluation date**: 2026-08-09 · **Domain**: A — Consistency 55/100
- **Evidence**: `npx prettier --check .` → "Code style issues found in 88 files"; filtered
  output confirms every file is in `docs/issues/`. This run's records authored
  Prettier-compliant → drift stays at 88 (+0, 6th flat run 92nd–97th).
- **Impact / Risk**: lint gate (`npm run lint`) exits 0 because the checker runs ESLint only;
  `format:check` would fail until the 88 ledger files are reformatted. Cosmetic-only, so
  Phase-2 cosmetic-prohibition keeps the fix deferred.
- **Fix** (deferred — cosmetic bucket): one `npx prettier --write docs/issues` pass when the
  cosmetics constraint lifts.

## Issue 5 — F066 (P2, bug): dist-destruction flake — LATENT, no repro

- **Category**: `bug` / **Priority**: `P2`
- **Files affected**: `scripts/build-pages.js` / `scripts/fs-safe.js` (suspected surface)
- **Evaluation date**: 2026-08-09 · **Domain**: B — Stability 74/100 (deduction applied)
- **Evidence**: 6-cycle `rm -rf dist && npm run build && ls dist/sitemap-index.xml` → 6/6
  present. Clean window ≈147 consecutive cycles (~1/153 cumulative). **No deterministic repro
  in-session** — kept LATENT per FAIL-SAFE rule (do not guess / do not blind-fix).

## Issue 6 — F002 (P1, CI/CD): GitHub-issue creation blocked — 93rd consecutive

- **Category**: `ci` / **Priority**: `P1`
- **Evidence**: REST `gh issue create` → `GraphQL: Resource not accessible by integration
(createIssue)`; same 403 for `/user` and `/repos/…/permissions` (token is
  `github-actions[bot]` with read-only grants). `open_issues: 0`, so the repo's issue-facing
  surface cannot be fed by this agent.
- **Impact / Risk**: contract Phase-1/2/3 output mandate (create issues from findings) cannot
  be executed; findings ship as labeled docs records instead (repo convention.)

## Issue 6 — F064 (P2, config): lint-staged engine mismatch HELD

- **Category**: `chore` / **Priority**: `P2`
- **Files**: `package.json` (devDeps `lint-staged@17.3.0`), `.nvmrc` (=22), `.github/workflows/on-pull.yml:53`
- **Evaluation date**: 2026-08-09 · **Domain**: D — Config & Env Parity 40/100
- **Evidence**: `npm ci` → `EBADENGINE Unsupported engine lint-staged@17.3.0 … required
node >=22.22.1, current v20.20.2`. `.nvmrc` pins 22 while CI and the runner run 20 →
  three-way parity gap.
- **Impact / Risk**: pre-commit/husky lint-staged fails on Node 20 environments; only relevant
  when the runner/CI moves to Node 22.
- **Fix** (deferred — env parity, not source logic): either align `lint-staged` to the used
  engine or standardize the Node version ring at 22.

## Maintained / closed-state ledger (no new action)

| ID   | State                 | Note                                                     |
| ---- | --------------------- | -------------------------------------------------------- |
| F028 | RESOLVED (maintained) | npm audit 0 vulns                                        |
| F057 | FIXED (maintained)    | addNumbers 0 matches in docs/api.md                      |
| F017 | FIXED (maintained)    | (part of F057 family)                                    |
| F024 | HELD (maintained)     | sitemap emitted every fresh build                        |
| F062 | HELD (docs-only)      | release.md phantom reference remains (docs-cleanup-only) |
| F063 | HELD (P1)             | re-confirmed 6/6 (see Issue 2)                           |

## Issue-procedure note

No duplicate issues exist (all current findings map 1:1 to ledger IDs, each unique). No
new finding recorded this run — 15th consecutive run without a new root-caused defect.
