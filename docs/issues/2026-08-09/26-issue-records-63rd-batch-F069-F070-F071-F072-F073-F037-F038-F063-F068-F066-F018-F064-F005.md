# Issue Records — 63rd batch (104th verification run, 2026-08-09)

**Evaluation Date**: 2026-08-09
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`e49b460`)
**Note**: GitHub-issue creation remains blocked by F002 (integration token lacks `issues: write`; 94th consecutive 403 re-verified this run). Per repo convention findings ship as labeled docs records — each with exactly one category label and one priority label (contract §4) — ready for bulk creation when permissions are granted.

## Issue 1 — F069 (P2, bug): build exits 0 when school pages fail — partial-failure masking NEW

- **Category**: `bug` / **Priority**: `P2`
- **Files affected**: `src/services/BuildOrchestrator.js:325-352`, `scripts/build-performance.js:25`
- **Domain table**: A — Correctness 76/100 (this deduction source); B — Resilience 80/100
- **Evidence** (source read + build run this run): `writeSchoolPagesConcurrently` filters `fulfilled` vs `rejected` results, and on `failed > 0` only `logger.warn`s the first 5 failure details `{ reason, npsn, operationName }`. No failure threshold aborts or non-zeros the build; the generated `dist/` silently ships without the failed pages. `PERF_MAX_FAILED_PAGES` exists (`build-performance.js:25`, default 0) but no code path enforces it against the failed count. Build still reports "0 failed" at the top level (`npm run build` → "Generate 2 school pages (0 failed)").
- **Impact / Risk**: CI can report green while pages are missing; stale/missing school pages ship in production silently; the perf "failed pages" budget is dead config.
- **Fix**: after the parallel batch, if `failed > 0 && failed > PERF_MAX_FAILED_PAGES` → throw `IntegrationError` (or `terminate` with non-zero) so the build actually fails; log all failures, not just first 5.

## Issue 2 — F070 (P3, bug): `terminate()` discards error detail (stack + `details` lost)

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `scripts/utils.js:280-292` + all CLI entry `.catch(error => terminate(error.message))` sites (`build-pages.js:60`, `fetch-data.js:423`, `validate-links.js:171`, `sitemap.js:225-227`, `check-freshness.js:232`, `freshness-report.js:314`, `etl.js:448`)
- **Domain table**: C — Debuggability 68/100; A — Error Handling 76/100
- **Evidence**: `terminate(message, code)` only logs the string `message`; callers pass only `error.message`. `IntegrationError` carries rich `details`/`stack`/`circuitBreakerState` that never reaches the log.
- **Impact / Risk**: production incident diagnosis = read only a one-line message; debugging requires code re-run.
- **Fix**: accept an optional error object `terminate(message, code, error)` and `logger.error({ err: error }, message)`.

## Issue 3 — F071 (P3, docs): 6 env vars undocumented in `.env.example`

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `src/services/BuildOrchestrator.js:416` (`GITHUB_STEP_SUMMARY`), `scripts/fetch-data.js:39` (`EXTERNAL_DATA_DIR`), `scripts/build-performance.js:19-25` (`PERF_MAX_BUILD_TIME_MS`, `PERF_MAX_MEMORY_BYTES`, `PERF_MIN_THROUGHPUT`, `PERF_MAX_FAILED_PAGES`)
- **Domain table**: C3 Documentation Accuracy 74/100; D — Config & Env Parity 33/100
- **Evidence**: `grep process.env` confirms 6 reads; `.env.example` declares only 7 (SITE_URL, RAW_DATA_PATH, LOG_LEVEL, ENRICHMENT_ENABLED, BUILD_CONCURRENCY_LIMIT, VALIDATION_CONCURRENCY_LIMIT, MAX_URLS_PER_SITEMAP). Operators cannot know these exist.
- **Impact**: tuning perf budgets / data dir impossible without reading source.
- **Fix**: add the 6 vars with defaults and units to `.env.example`.

## Issue 4 — F072 (P3, refactor): `interactive.js` bypasses pino with 26 raw console calls

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/interactive.js` (26 occurrences), `eslint.config.js` (`no-console: 'off'`)
- **Domain table**: A — Observability 69/100 (System); D — Tech Debt 60/100
- **Evidence**: grep count = 26 `console.log/console.error` in interactive.js; `eslint.config.js` disables `no-console`, so the pattern is unguarded. `logger.js` (pino) is available and used by every other script.
- **Fix**: route interactive.js through `logger`, restore `no-console: ['error', { allow: [] }]` in eslint.

## Issue 5 — F073 (P2, bug): `validate-links.js` silently treats non-IntegrationError probes as valid

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `scripts/validate-links.js:87-96`
- **Domain table**: C — Feedback 78/100; D — Validation coverage 80/100
- **Evidence**: in the `safeStat` catch, only `error.name === 'IntegrationError'` is recorded as broken; ANY other error (EACCES, ENOTDIR, network-layer) is swallowed, and the link is considered valid. A permission or filesystem error yields a false "all links OK".
- **Impact / Risk**: broken/errored links pass validation; sitemap/freshness gate silently misses real problems.
- **Fix**: record non-IntegrationError errors as broken with the error message, or rethrow so the audit surfaces them.

## Issue 6 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD (6th regression)

- **Category**: `security` / **Priority**: `P0`
- **Files**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`, `opencode.yml`, `orchestrator.yml`, `template.md`
- **Evidence** (fresh `node scripts/check-workflow-security.js` → exit 1, 12 violations): 2× CRITICAL `DUPLICATE_API_KEY` (`on-push.yml:26`, `parallel.yml:37`), 4× HIGH `ID_TOKEN_WRITE`, 4× HIGH `ACTIONS_WRITE_NON_MERGE`, 2× HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` (`orchestrator.yml:33`, `architect-agent.yml:37`). Byte-identical to the 102nd/103rd. `SECURITY_AUDIT_NOTE.md` (rows 54-57, 77-79) claims all — the claims contradict the current files (6th occurrence).
- **Fix** (blocked — requires workflow write): remove `API_KEY` aliases, s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from non-merge, enforce `check-workflow-security.js` as a failing pre-commit/CI gate (`.husky/pre-commit:3` swallows it with `|| echo skipped` — dead gate, F013/F021 dormant).

## Issue 7 — F063 (P1, ci): orchestrator.yml dead — 5/5 failed runs

- **Category**: `ci` / **Priority**: `P1`
- **Files**: `.github/workflows/orchestrator.yml`
- **Evidence**: `gh run list --workflow=orchestrator.yml` → 5/5 `failure` dates 2026-08-05 → 2026-08-09 01:09Z (db 31287686640). `secrets.GH_TOKEN` fictitious (no such secret defined) at checkout/env → Job fails pre-run. No CI workflow runs build/lint/test as a gate (only `npm ci || true` in parallel.yml).
- **Fix**: replace `secrets.GH_TOKEN` with `secrets.GITHUB_TOKEN` (2 sites), then re-trigger; then add a true gate (lint+build+test) to on-pull.yml.

## Issue 8 — F066 (P1, test): pytest test wipes real `dist/` — HELD

- **Category**: `test` / **Priority**: `P1`
- **Files**: `tests/test_data_validation.py:70-86`
- **Evidence**: `test_dist_directory_can_be_created` executes `shutil.rmtree(dist_dir)` on the real build output dir before AND after the assertion. pytest absent (F068) so not re-run; root cause unambiguous by inspection (deterministic repro in 101st/102nd).
- **Fix**: use `tempfile.TemporaryDirectory` instead of real `dist/`.

## Issue 9 — F018 (P2, chore): school data STALE 20 days

- **Category**: `chore` / **Priority**: `P2`
- **Files**: `data/schools.csv`, `external/raw.csv`
- **Evidence**: `check-freshness.js` → "Last Update: 2026-07-20 (20 days ago)", ⚠️ STALE (threshold 7). 2-record sample; real freshness requires `npm run fetch-data` or a documented sample policy.
- **Fix**: run the update, or document fixture exemption.

## Issue 10 — F064 (P2, ci): Node engine parity gap

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.nvmrc` (=22), `.github/workflows/on-pull.yml:52`, `parallel.yml` ×4 (node-version 20), `package.json` (`lint-staged@17.3.0` requires node ≥22.22.1)
- **Evidence**: runtime v20.20.2 — three sources disagree; lint-staged pre-commit may fail under pinned engine.
- **Fix**: align all on 22, then re-verify `npm ci` + lint-staged.

## Issue 11 — F068 (P1, test): pytest gate not reproducible — `No module named pytest`

- **Category**: `test` / **Priority**: `P1`
- **Files**: `requirements.txt`, `package.json` (`test:py:pytest`, `test:all`), `tests/test_basic.py`, `tests/test_data_validation.py`
- **Evidence**: `npm run test:py:pytest` → `/usr/bin/python3: No module named pytest`; `test:all` fails at that stage; `tests/run_tests.py` fallback 27/27. requirements.txt declares pytest but no CI installs Python deps (`grep pip` → 0 hits); docs/setup.md marks pytest "Optional".
- **Fix**: add `pip install -r requirements.txt` to CI or a documented setup step.

## Issue 12 — F062 (P3, docs): no release workflow, 0 tags

- **Category**: `docs` / **Priority**: `P3`
- **Evidence**: `git tag | wc -l` → 0, no release workflow, `CHANGELOG.md` manual.
- **Fix**: documented release/rollback process or tag-triggered workflow.

## Findings verified CLEAN (no new issue needed)

- F014 — test-race 14th+ consecutive clean ✅ · F024 — sitemap emitted on build ✅ · F028 — `npm audit` 0 ✅ · F057/F017 — `addNumbers` absent from docs ✅ · lint 0/0 ✅ · build exit 0, budgets PASS ✅ · coverage gate 94.94% stmt · JS 1056/1056 pass ✅ · Python harness 27/27 ✅
