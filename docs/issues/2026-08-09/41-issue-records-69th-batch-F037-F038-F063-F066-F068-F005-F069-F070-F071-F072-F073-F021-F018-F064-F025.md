# Issue Records — 69th batch (110th verification run, 2026-08-09)

**Evaluation Date**: 2026-08-09
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`156a1e1`)
**Note**: GitHub-issue creation remains blocked by F002 (integration token lacks `issues: write`; 100th consecutive 403 re-verified this run via REST `gh issue create` and `/api/user`). Per repo convention findings ship as labeled docs records — each with exactly one category label and one priority label (contract §4) — ready for bulk creation when permissions are granted.

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD (12th regression)

- **Category**: `security` / **Priority**: `P0`
- **Files**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`, `opencode.yml`, `orchestrator.yml`, `template.md`
- **Domain table**: B — Security Practices 46/100; D — CI/CD Health 37/100
- **Evidence** (fresh `node scripts/check-workflow-security.js` → exit 1, **12 violations**): 2× CRITICAL `DUPLICATE_API_KEY` (`on-push.yml`, `parallel.yml`), 4× HIGH `ID_TOKEN_WRITE` (`architect-agent.yml:13`, `opencode.yml:18`, `orchestrator.yml:9`, `parallel.yml:16`), 4× HIGH `ACTIONS_WRITE_NON_MERGE` (`architect-agent.yml:17`, `opencode.yml:22`, `orchestrator.yml:13`, `parallel.yml:15`), 2× HIGH `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` (`orchestrator.yml`, `architect-agent.yml`). Byte-identical to the 103rd–109th. `SECURITY_AUDIT_NOTE.md` claims these fixed — claims contradict current files (12th occurrence).
- **Impact**: repository is PUBLIC; over-permissive tokens + secret duplication remain externally triggerable.
- **Fix** (blocked — requires `workflows: write` (F050) / workflow-file push): remove `API_KEY` aliases, s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from non-merge, and make `check-workflow-security.js` a _failing_ gate (currently `.husky/pre-commit` swallows it — F021).

## Issue 2 — F063 (P1, ci): orchestrator.yml dead — 5/5 failed runs

- **Category**: `ci` / **Priority**: `P1`
- **Files**: `.github/workflows/orchestrator.yml`
- **Evidence**: `gh run list --workflow=orchestrator.yml` → 5/5 `failure`, 2026-08-05 → 2026-08-09 01:09:13Z (db 31287686640). `secrets.GH_TOKEN` fictitious (no such secret defined) at checkout/env → job fails pre-run. No CI workflow runs build/lint/test as a gate.
- **Impact**: the repo's namesake orchestrator automation dead 7 consecutive runs; no gate catches regressions.
- **Fix**: replace `secrets.GH_TOKEN` with `secrets.GITHUB_TOKEN` (2 sites), re-trigger, then add a true gate (lint+build+test) to on-pull.yml.

## Issue 3 — F066 (P1, test): test suite wipes real `dist/` — EMPIRICALLY RE-CONFIRMED (6th consecutive run)

- **Category**: `test` / **Priority**: `P1`
- **Files**: `tests/run_tests.py:229-261`, `tests/test_data_validation.py:70-87`
- **Domain table**: B — Stability 74/100; A — Testability 68/100; C — Build/Test Feedback 70/100
- **Evidence** (this run, firsthand): after a clean `rm -rf dist && npm run build` (exit 0, 14 artifacts incl. sitemaps present, `ls dist/ | wc -l` → 14), `npm run test:py` (fallback harness, **27/27 PASS, exit 0**) left **`dist/` MISSING** (`ls dist/ 2>/dev/null | wc -l` → 0, `dist/ MISSING`). `tests/run_tests.py:229-261` `test_dist_directory_creation` executes `shutil.rmtree(dist_dir)` on the real build-output dir before AND after the assertion (with retry loop). `dist/` rebuilt afterward; no tracked-file change.
- **Impact**: every successful `test:py` run silently deletes the previous build output; build→test→build interleaves lose artifacts.
- **Fix**: use `tempfile.TemporaryDirectory` (or a `/tmp`-based dir) in both `run_tests.py` and `test_data_validation.py` instead of the real `dist/`.

## Issue 4 — F068 (P1, test): pytest gate not reproducible — `No module named pytest`

- **Category**: `test` / **Priority**: `P1`
- **Files**: `requirements.txt`, `package.json` (`test:py:pytest`, `test:all`), `tests/`
- **Evidence**: `npm run test:py:pytest` → `/usr/bin/python3: No module named pytest`; `test:all` fails at that stage; `tests/run_tests.py` fallback 27/27. requirements.txt declares pytest but no CI installs Python deps (`grep pip` → 0 hits); docs/setup.md marks pytest "Optional".
- **Impact**: the pytest-based test path is dead; only the fallback (which wipes dist/, F066) runs.
- **Fix**: add `pip install -r requirements.txt` to CI or document a setup step; align `test:py` and `test:py:pytest` outcomes.

## Issue 5 — F005 (P3, docs): Prettier drift — HELD at 88 files

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/issues/**` (88 files), `docs/audit/**`
- **Evidence**: `npx prettier --check .` → exit 1, "Code style issues found in **88 files**"; all under `docs/issues/`; **0 source files** (`npx prettier --check scripts src` → "All matched files use Prettier code style!"). This run's 3 new records were written Prettier-clean so the ledger did not grow (convention runs 95–109).
- **Impact**: `npx prettier --check` cannot be a CI gate while the docs ledger is non-conformant.
- **Fix**: one-time `npx prettier --write docs/issues docs/audit` docs-only PR.

## Issue 6 — F069 (P2, bug): build exits 0 when school pages fail — partial-failure masking

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `src/services/BuildOrchestrator.js:325-352`, `scripts/build-performance.js:25`
- **Domain table**: A — Correctness 76/100; B — Resilience 80/100
- **Evidence** (source read this run): `writeSchoolPagesConcurrently` collects `fulfilled` vs `rejected`; on `failed > 0` only `logger.warn`s the first 5 `{ reason, npsn, operationName }`. No failure threshold aborts or non-zeros the build. `PERF_MAX_FAILED_PAGES` exists (default 0, `scripts/build-performance.js:25`) but no code path enforces it. Build reports "Generated 2 school pages (0 failed)" green.
- **Impact**: CI green while pages missing; stale pages ship silently; perf failed-pages budget is dead config.
- **Fix**: if `failed > PERF_MAX_FAILED_PAGES` → throw so the build fails; log all failures, not just first 5.

## Issue 7 — F070 (P3, bug): `terminate()` discards error detail (stack + `details` lost)

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `scripts/utils.js:285-292` + CLI `.catch` sites (`build-pages.js:61`, `etl.js:449`, `fetch-data.js:425`, `validate-links.js`, `sitemap.js`, `check-freshness.js:218`, `freshness-report.js`, `data-quality.js:353/387`)
- **Domain table**: C — Debuggability 68/100; A — Error Handling 76/100
- **Evidence** (source read this run): `terminate(message, code = 1)` logs only the string `message`; callers pass only `error.message`. `IntegrationError` `details`/`stack` never reach the log.
- **Impact**: production incident diagnosis = one-line message; debugging requires code re-run.
- **Fix**: accept an optional error object and `logger.error({ err: error }, message)`.

## Issue 8 — F071 (P3, docs): env vars undocumented in `.env.example`

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `src/services/BuildOrchestrator.js:408-421` (`GITHUB_STEP_SUMMARY`), `scripts/fetch-data.js:36-39` (`EXTERNAL_DATA_DIR`), `scripts/build-performance.js:19-25` (`PERF_MAX_BUILD_TIME_MS`, `PERF_MAX_MEMORY_BYTES`, `PERF_MIN_THROUGHPUT`, `PERF_MAX_FAILED_PAGES`)
- **Domain table**: C — Documentation Accuracy 74/100; D — Config & Env Parity 33/100
- **Evidence** (grep this run): 14 `process.env.*` reads in source incl. test-only `TEST_TEMP_DIR` (13 non-test: `BUILD_CONCURRENCY_LIMIT`, `ENRICHMENT_ENABLED`, `EXTERNAL_DATA_DIR`, `GITHUB_STEP_SUMMARY`, `LOG_LEVEL`, `MAX_URLS_PER_SITEMAP`, `PERF_MAX_BUILD_TIME_MS`, `PERF_MAX_FAILED_PAGES`, `PERF_MAX_MEMORY_BYTES`, `PERF_MIN_THROUGHPUT`, `RAW_DATA_PATH`, `SITE_URL`, `VALIDATION_CONCURRENCY_LIMIT`); `.env.example` documents only 7 (SITE_URL, RAW_DATA_PATH, LOG_LEVEL, ENRICHMENT_ENABLED commented, BUILD_CONCURRENCY_LIMIT, VALIDATION_CONCURRENCY_LIMIT, MAX_URLS_PER_SITEMAP). 6 consumed-but-undocumented.
- **Impact**: operators cannot tune perf budgets / data dir without reading source.
- **Fix**: add the 6 vars with defaults and units to `.env.example`.

## Issue 9 — F072 (P3, refactor): `interactive.js` bypasses pino with 28 raw console calls

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/interactive.js` (28 occurrences), `eslint.config.js` (`no-console: 'off'`, lines 21 & 64)
- **Domain table**: B — Observability 69/100; D — Tech Debt 60/100
- **Evidence** (grep this run): 24 `console.log` + 2 `console.error` + 2 `console.clear` = 28 `console.*` in interactive.js; `eslint.config.js` disables `no-console` (2 rule blocks) so the bypass is unguarded. `logger.js` (pino) used by every other script.
- **Impact**: interactive CLI output unobservable/untestable; observability gap in the tooling surface.
- **Fix**: route interactive.js through `logger`/a pino stream, restore `no-console` as error in eslint.

## Issue 10 — F073 (P2, bug): `validate-links.js` silently treats non-IntegrationError probes as valid

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `scripts/validate-links.js:84-96`
- **Domain table**: C — Feedback 78/100; A — Error Handling 76/100
- **Evidence** (source read this run): in the `safeStat` catch, only `error.name === 'IntegrationError'` is recorded broken; ANY other error (EACCES, ENOTDIR, IO) is swallowed and the link considered valid. A permission/filesystem error yields false "all links OK".
- **Impact**: broken/errored links pass validation; sitemap/freshness gates silently miss real problems.
- **Fix**: record non-IntegrationError errors as broken with the message, or rethrow.

## Issue 11 — F021 (P2, ci): pre-commit security gate is dead — swallows violations

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.husky/pre-commit`
- **Evidence** (read this run): `node scripts/check-workflow-security.js 2>/dev/null || echo "⚠️ Workflow security check skipped…"` — the security check's non-zero exit is converted to a passing echo; `npx lint-staged` then proceeds. 12 live violations (F037/F038) pass pre-commit silently.
- **Impact**: the repo's only local security gate is a no-op; regressions land undetected.
- **Fix**: remove the `|| echo` swallow so pre-commit fails on violations (align with the fix for F037/F038).

## Issue 12 — F018 (P2, chore): school data STALE 20 days

- **Category**: `chore` / **Priority**: `P2`
- **Files**: `data/schools.csv`, `external/raw.csv`
- **Evidence** (this run): `check-freshness.js` → "Last Update: 2026-07-20 (20 days ago)", ⚠️ STALE (threshold 7). Record Count: 2.
- **Fix**: run `npm run fetch-data`/ETL refresh, or document fixture exemption policy.

## Issue 13 — F064 (P2, ci): Node engine parity gap

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.nvmrc` (=22), `.github/workflows/on-pull.yml:53`, `parallel.yml:70/267/345/401` (node-version 20), `package.json` (`lint-staged@17.3.0` requires node ≥22.22.1)
- **Evidence**: runtime v20.20.2 — three sources disagree; lint-staged pre-commit may fail under pinned engine (npm install emitted EBADENGINE this run).
- **Fix**: align all on 22, then re-verify `npm install` + lint-staged.

## Issue 14 — F025 (P1, chore): live site root 404 behind green Pages deploy

- **Category**: `chore` / **Priority**: `P1`
- **Files**: `dist/` output, Pages deployment config (`.` vs `/docs` root, CNAME/base path)
- **Evidence** (this run): `curl https://sulhimaskom.github.io/sekolah-pseo/` → **404**; `/robots.txt` → 200. Pages reports "built" but site root 404s.
- **Impact**: deployed product unreachable at root; users land on 404.
- **Fix**: verify Pages source directory/`base` config matches dist/ output (docs/deployment.md).

## Findings verified CLEAN (no new issue needed)

- **F014** — parallel test race: 20th consecutive clean run (1056/1056 pass) ✅
- **F024** — sitemap `sitemap-index.xml` + `sitemap-001.xml` emitted on every build ✅
- **F028** — `npm audit` 0 vulnerabilities ✅ (maintained RESOLVED)
- **F017/F057** — `addNumbers` absent from docs/api.md (0 hits) ✅ (maintained RESOLVED)
- **F029** — `external/raw.csv` untouched; worktree clean post-matrix ✅
- Lint 0/0 ✅ · cover gate 94.94% stmt / 92.2% branch / 96.65% funcs ✅ · Python fallback 27/27 ✅ · build exit 0 budgets PASS ✅
