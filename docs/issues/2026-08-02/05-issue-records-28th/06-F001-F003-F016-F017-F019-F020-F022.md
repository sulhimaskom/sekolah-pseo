# Consolidated Minor Findings — 28th run (basis issues)

**Evaluation Date**: 2026-08-02 (28th run)
**Category**: mixed (chore/test/refactor/docs)
**Priority**: P3
**Status**: open

## F001 — Floating promise (bug, P1)
`scripts/fetch-data.js:338` — `fetchFromGitHub(sourceRepo)` called but **not awaited** inside
synchronous `main()`. Since `fetchFromGitHub` returns a Promise, `copyToRaw(csvPath,...)` on the
following line may receive a Promise instead of a resolved path. Fix: make `main()` async and
`await` the call.

## F016 — README documents non-existent `gitignore-check` (docs, P3)
README.md:283 references a `gitignore-check` workflow that does not exist in
`.github/workflows/`. Fix: either add the workflow or correct the README.

## F017 — docs/api.md documents non-existent `addNumbers()` (docs, P3)
docs/api.md:553–574 documents `addNumbers(a,b)` but no such export exists in
`scripts/utils.js`. Fix: remove the bogus docs section or note it as a planned util.

## F019 — Dead code in `tests/run_tests.py` (refactor, P3)
Duplicate import block lines 20–25 (repeats 12–19), and unreachable code after `return suite`
(lines 523–527). Fix: remove dead branch; de-duplicate imports.

## F020 — Dead script `scripts/apply-caching-patch.sh` (chore, P3)
Targets `feature-ci-incremental-caching.patch` which does not exist in repo root — always fails.
Fix: delete the script or restore the referenced patch.

## F022 — `head-meta.js` has no test (test, P3)
`src/presenters/templates/shared/head-meta.js` is untested. Fix: add `head-meta.test.js`.

## F012 — lint-staged engine mismatch (chaos, P3)
`.nvmrc`=22 and CI uses node 20, but runner v20.20.2 → lint-staged@17.2.0 wants >=22.22.1
(EBADENGINE). Fix: align Node version across `.nvmrc`, CI setup-node, and devDeps.

## Affected
`scripts/fetch-data.js`, `README.md`, `docs/api.md`, `tests/run_tests.py`,
`scripts/apply-caching-patch.sh`, `src/presenters/templates/shared/head-meta.js`, `.nvmrc`