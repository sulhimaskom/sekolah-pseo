# ISSUE RECORDS — F011, F012, F016, F017 (consolidated release/docs findings)

> **Status**: GitHub issue creation BLOCKED (403) — records persisted per repo convention.
> **Evaluation date**: 2026-08-02

---

## F011 — Missing automated release process
**Labels**: `ci`, `P2` | **Files**: `.github/workflows/` (no release workflow), `git tag` → 0

- No release workflow, no tags, no versioned artifacts, no changelog automation
  (CHANGELOG.md exists but is hand-edited).
- **Suggested fix**: add a release workflow (tag → build → attach dist artifact);
  adopt semantic-release or a simple tag-and-changelog job.

---

## F012 — lint-staged engine mismatch
**Labels**: `chore`, `P3` | **Files**: `.nvmrc`, `package.json`, `on-pull.yml`

- Tri-state mismatch: `.nvmrc`=22, CI `setup-node`=20, runner Node v20.20.2,
  `lint-staged@17.2.0` requires `>=22.22.1`. `npm ci` emits EBADENGINE warning;
  husky pre-commit can fail under Node 20.
- **Suggested fix**: align `.nvmrc`, CI setup-node, and engines to one version (22),
  or downgrade lint-staged to a version compatible with Node 20.

---

## F016 — README documents non-existent gitignore-check workflow
**Labels**: `docs`, `P3` | **Files**: `README.md:283`

- README says pushes to main are verified by a `gitignore-check` workflow; no such
  workflow exists in `.github/workflows/`.
- **Suggested fix**: remove the claim or add the workflow.

---

## F017 — docs/api.md documents non-existent addNumbers()
**Labels**: `docs`, `P3` | **Files**: `docs/api.md:553-574`, `scripts/utils.js`

- `docs/api.md` documents `addNumbers(a, b)` with usage examples; no such export exists
  in `scripts/utils.js`. Stale API documentation misleads consumers.
- **Suggested fix**: remove the section or implement the function; audit api.md against
  actual exports.

---

## Domain impact

- D2 Release & Rollback Safety (65/100): F011 −35
- A9 Dependency Discipline (90/100): F012 −10
- D3 Config & Env Parity (78/100): F012 contributes
- C5 API Clarity (88/100): F017 −12
- C7 Documentation Accuracy (70/100): F016/F017 contribute −30 (with F005/stale docs)
- C6 Local Dev Setup (85/100): F012 −15
