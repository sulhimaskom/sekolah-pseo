# Issue Records — 72nd batch (113th verification run, 2026-08-10)

**Evaluation Date**: 2026-08-10
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`230cfc9`)
**Note**: GitHub-issue creation remains blocked by F002 (integration token lacks
`issues: write`; 102nd consecutive 403 re-verified this run via REST `gh issue create`
and `/api/user`). Per repo convention findings ship as labeled docs records — each with
exactly one category label and one priority label (contract §4) — ready for bulk
creation when permissions are granted.

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD (15th regression)

- **Category**: `security` / **Priority**: `P0`
- **Files**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`,
  `opencode.yml`, `orchestrator.yml`, `template.md`
- **Domain table**: B — Security Practices 46/100; D — CI/CD Health 37/100
- **Evidence** (fresh `node scripts/check-workflow-security.js` → exit 1, **12
  violations**): 2× CRITICAL `DUPLICATE_API_KEY` (`on-push.yml:26`, `parallel.yml`),
  4× HIGH `ID_TOKEN_WRITE`, 4× HIGH `ACTIONS_WRITE_NON_MERGE`, 2× HIGH
  `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN`. Byte-identical to the 103rd–112th.
  `SECURITY_AUDIT_NOTE.md` claims these fixed — claims contradict current files (15th
  occurrence).
- **Impact**: repository is PUBLIC; over-permissive tokens + secret duplication remain
  externally triggerable.
- **Fix** (blocked — requires `workflows: write` (F050) / workflow-file push): remove
  `API_KEY` aliases, s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from
  non-merge, and make `check-workflow-security.js` a _failing_ gate (F021).

## Issue 2 — F063 (P1, ci): orchestrator.yml dead — 10/10 failed runs

- **Category**: `ci` / **Priority**: `P1`
- **Files**: `.github/workflows/orchestrator.yml`
- **Evidence**: `gh run list --workflow=orchestrator.yml` → 10/10 `failure` (4/4 this
  run; latest 2026-08-10 01:11:26Z, db 31346441739). `secrets.GH_TOKEN` fictitious at
  checkout/env → job fails pre-run.
- **Impact**: the repo's namesake orchestrator automation dead 10 consecutive runs; no
  CI gate catches regressions.
- **Fix** (blocked — workflow-file write): replace `secrets.GH_TOKEN` with
  `secrets.GITHUB_TOKEN` (2 sites), re-trigger, then add a true gate
  (lint+build+test) to on-pull.yml.

## Issue 3 — F068 (P1, test): pytest gate not reproducible — `No module named pytest`

- **Category**: `test` / **Priority**: `P1`
- **Files**: `requirements.txt`, `package.json` (`test:py:pytest`, `test:all`), `tests/`
- **Evidence**: `npm run test:py:pytest` → `/usr/bin/python3: No module named pytest`;
  `test:all` fails at that stage; `tests/run_tests.py` fallback 27/27 PASS (and no
  longer wipes dist/ — F066 RESOLVED). requirements.txt declares pytest but no CI
  installs Python deps; docs/setup.md marks pytest "Optional".
- **Impact**: the pytest-based test path is dead; only the fallback runs.
- **Fix**: add `pip install -r requirements.txt` to CI or document a setup step; align
  `test:py` and `test:py:pytest` outcomes.

## Issue 4 — F025 (P1, chore): live site root 404 behind green Pages deploy

- **Category**: `chore` / **Priority**: `P1`
- **Files**: `dist/` output, Pages deployment config (`.` vs `/docs` root, CNAME/base)
- **Evidence** (this run): `curl https://sekolah-pseo.github.io/sekolah-pseo/` → **404**;
  Pages API reports "built". Site root 404s.
- **Impact**: deployed product unreachable at root; users land on 404.
- **Fix**: verify Pages source directory/`base` config matches dist/ output
  (docs/deployment.md).

## Issue 5 — F069 (P2, bug): build exits 0 when school pages fail — partial-failure masking

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `src/services/BuildOrchestrator.js:340-352` (`writeSchoolPagesConcurrently`),
  `build()` at 444-506, `scripts/build-performance.js:25` (`MAX_FAILED_PAGES` budget)
- **Domain table**: A — Correctness 76/100; B — Resilience 80/100; B — Stability 82/100
- **Evidence** (source read this run): `writeSchoolPagesConcurrently` collects
  `fulfilled` vs `rejected`; on `failed > 0` only `logger.warn`s the first 5
  `{ reason, npsn, operationName }`. `build()` calls `tracker.recordPageCounts(...)`
  then `finalizeBuild(tracker)` which only logs the report. `checkBudgets()` computes a
  `MAX_FAILED_PAGES` violation (default 0) but **no code path aborts or non-zeros the
  build** — exit stays 0 with failed pages.
- **Impact**: CI green while pages missing; stale pages ship silently.
- **Fix (Phase 2 this run)**: in `build()`, after `recordPageCounts`, throw an
  `IntegrationError(PERFORMANCE_BUDGET_VIOLATION)` when `failed > 0` exceeds the
  `MAX_FAILED_PAGES` budget; keep `writeSchoolPagesConcurrently`'s return contract
  intact (existing tests at `build-pages.test.js:147-191` depend on it).

## Issue 6 — F073 (P2, bug): `validate-links.js` silently treats non-IntegrationError probes as valid

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `scripts/validate-links.js:89-94`
- **Domain table**: C — Feedback 78/100; A — Error Handling 76/100
- **Evidence** (source read this run): in the `safeStat` catch, only
  `error.name === 'IntegrationError'` is recorded broken; ANY other error (EACCES,
  ENOTDIR, IO) is swallowed and the link considered valid.
- **Impact**: broken/errored links pass validation.
- **Fix**: record non-IntegrationError errors as broken with the message, or rethrow.

## Issue 7 — F021 (P2, ci): pre-commit security gate is dead — swallows violations

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.husky/pre-commit`
- **Evidence** (read this run): `node scripts/check-workflow-security.js 2>/dev/null ||
echo "⚠️ … skipped"` — non-zero exit converted to a passing echo; 12 live violations
  (F037/F038) pass pre-commit silently.
- **Impact**: the repo's only local security gate is a no-op.
- **Fix**: remove the `|| echo` swallow so pre-commit fails on violations.

## Issue 8 — F018 (P2, chore): school data STALE 21 days

- **Category**: `chore` / **Priority**: `P2`
- **Files**: `data/schools.csv`, `external/raw.csv`
- **Evidence** (this run): `check-freshness.js` → "Last Update: 2026-07-20 (21 days
  ago)", ⚠️ STALE (threshold 7). Record Count: 2 (fixture-scale dataset).
- **Fix**: run `npm run fetch-data`/ETL refresh, or document fixture exemption policy.

## Issue 9 — F064 (P2, ci): Node engine parity gap

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.nvmrc` (=22), `.github/workflows/on-pull.yml:53`, `parallel.yml`
  (node-version 20), `package.json` (`lint-staged@17.3.0` requires node ≥22.22.1)
- **Evidence**: runtime v20.20.2 — three sources disagree; lint-staged may fail under
  pinned engine. (`npm ci` this run completed clean; no EBADENGINE observed on fresh
  node_modules.)
- **Fix**: align all on 22, then re-verify `npm install` + lint-staged.

## Issue 10 — F070 (P3, bug): `terminate()` discards error detail (stack + `details` lost)

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `scripts/utils.js:285-292` + CLI `.catch` sites
- **Domain table**: C — Debuggability 68/100; A — Error Handling 76/100
- **Evidence** (source read this run): `terminate(message, code = 1)` logs only the
  string; callers pass only `error.message`. `IntegrationError` `details`/`stack`
  never reach the log.
- **Impact**: production incident diagnosis = one-line message.
- **Fix**: accept an optional error object and `logger.error({ err: error }, message)`.

## Issue 11 — F071 (P3, docs): env vars undocumented in `.env.example`

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `src/services/BuildOrchestrator.js:408-421`, `scripts/fetch-data.js:36-39`,
  `scripts/build-performance.js:19-25`
- **Domain table**: C — Documentation Accuracy 74/100; D — Config & Env Parity 33/100
- **Evidence** (grep this run): 15 `process.env.*` reads in source incl. test-only
  `TEST_TEMP_DIR` (14 non-test); `.env.example` documents only 7. 7
  consumed-but-undocumented (GITHUB_STEP_SUMMARY, EXTERNAL_DATA_DIR,
  PERF_MAX_BUILD_TIME_MS, PERF_MAX_FAILED_PAGES, PERF_MAX_MEMORY_BYTES,
  PERF_MIN_THROUGHPUT, plus build-concurrency family).
- **Impact**: operators cannot tune perf budgets / data dir without reading source.
- **Fix**: add the undocumented vars with defaults and units to `.env.example`.

## Issue 12 — F072 (P3, refactor): `interactive.js` bypasses pino with 28 raw console calls

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/interactive.js` (28 occurrences), `eslint.config.js`
  (`no-console: 'off'`)
- **Evidence** (grep this run): 24 `console.log` + 2 `console.error` + 2
  `console.clear` = 28 `console.*`; `eslint.config.js` disables `no-console` (2 rule
  blocks) so the bypass is unguarded.
- **Impact**: interactive CLI output unobservable/untestable.
- **Fix**: route interactive.js through `logger`, restore `no-console` as error.

## Issue 13 — F005 (P3, docs): Prettier drift — HELD at 88 files

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/issues/**` (88 files), `docs/audit/**`
- **Evidence**: `npx prettier --check .` → exit 1, "Code style issues found in **88
  files**"; all under `docs/issues/`; **0 source files** (`npx prettier --check scripts
src` → clean). This run's new records written Prettier-clean so the ledger did not
  grow.
- **Impact**: `npx prettier --check` cannot be a CI gate while the docs ledger is
  non-conformant.
- **Fix**: one-time `npx prettier --write docs/issues docs/audit` docs-only PR.

## Findings verified CLEAN (no new issue needed)

- **F066** — dist-wipe: **RESOLVED** (2/2 clean this run; dist/ preserved by test:py) ✅
- **F014** — parallel test race: 23rd consecutive clean run (1056/1056 pass) ✅
- **F024** — sitemap `sitemap-index.xml` + `sitemap-001.xml` emitted on every build ✅
- **F028** — `npm audit` 0 vulnerabilities ✅ (maintained RESOLVED)
- **F017/F057** — `addNumbers` absent from src/scripts (0 hits) ✅ (maintained RESOLVED)
- Lint 0/0 ✅ · cover gate 94.94% stmt / 92.2% branch / 96.65% funcs ✅ · Python
  fallback 27/27 ✅ (dist intact — F066) · build exit 0 budgets PASS ✅
