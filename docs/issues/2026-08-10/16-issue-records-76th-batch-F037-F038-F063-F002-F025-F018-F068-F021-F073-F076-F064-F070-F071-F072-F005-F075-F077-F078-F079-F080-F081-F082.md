# Issue Records — 76th batch (117th verification run, 2026-08-10)

**Evaluation Date**: 2026-08-10
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`50b4e77` — 116th
records PR #652 merged; HEAD == origin/main)
**Note**: GitHub-issue creation remains blocked by F002 (integration token lacks
`issues: write`; 106th consecutive denial re-verified this run via `gh issue create` →
`GraphQL: Resource not accessible by integration (createIssue)`). Per repo convention
findings ship as labeled docs records — each with exactly one category label and one
priority label (contract §4) — ready for bulk creation when permissions are granted.
Records written Prettier-clean per F005 convention (ledger stable at 88 files, 0
source).

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD (18th regression)

- **Category**: `security` / **Priority**: `P0`
- **Files**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`,
  `opencode.yml`, `orchestrator.yml`
- **Domain table**: B — Security Practices 46/100; D — CI/CD Health 37/100
- **Evidence** (fresh `node scripts/check-workflow-security.js --json` → exit 1, **12
  violations**, byte-identical to 116th): 2× CRITICAL `DUPLICATE_API_KEY`
  (on-push.yml, parallel.yml — API_KEY aliases secrets.GEMINI_API_KEY), 4× HIGH
  `ID_TOKEN_WRITE` (architect-agent.yml:13, opencode.yml:18, orchestrator.yml:9,
  parallel.yml:16), 4× HIGH `ACTIONS_WRITE_NON_MERGE` (architect-agent.yml:17,
  opencode.yml:22, orchestrator.yml:13, parallel.yml:15), 2× HIGH
  `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` (orchestrator.yml ×2, architect-agent.yml ×1).
  `SECURITY_AUDIT_NOTE.md` claims these fixed — claims contradict current files (18th
  regression).
- **Impact**: repository is PUBLIC; over-permissive tokens + secret duplication remain
  externally triggerable.
- **Fix** (blocked — requires `workflows: write` (F050) / workflow-file push): remove
  `API_KEY` aliases, s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from
  non-merge workflows, and make `check-workflow-security.js` a failing gate (F021).

## Issue 2 — F063 (P1, ci): orchestrator.yml dead — 6/6 failed runs

- **Category**: `ci` / **Priority**: `P1`
- **Files**: `.github/workflows/orchestrator.yml`
- **Evidence**: `gh run list --workflow=orchestrator.yml` → 6/6 `failure` (latest
  2026-08-10 01:11:26Z). `secrets.GH_TOKEN` fictitious (no such secret defined) at
  checkout/env → job fails pre-run (verified at orchestrator.yml:33,41). This is also
  one of the 2 `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` violations (F037/F038).
- **Impact**: the repo's namesake orchestrator automation dead 6 consecutive runs; no
  CI workflow runs lint+build+test as a hard gate.
- **Fix** (blocked — workflow-file write): replace `secrets.GH_TOKEN` with
  `secrets.GITHUB_TOKEN` (2 sites), re-trigger, then add a true gate
  (lint+build+test) to on-pull.yml.

## Issue 3 — F002 (P1, chore): GitHub issue/PR metadata creation blocked by token grant

- **Category**: `chore` / **Priority**: `P1`
- **Files**: repository settings / integration token scope
- **Evidence**: `gh issue create` → `GraphQL: Resource not accessible by integration
(createIssue)` (106th consecutive denial). All findings therefore ship as labeled
  docs records.
- **Impact**: issue-driven development velocity is zero; every finding requires a
  docs record + later bulk creation.
- **Fix** (blocked — owner must grant `issues: write` to the integration token).

## Issue 4 — F025 (P1, chore): live site root 404 behind green Pages deploy

- **Category**: `chore` / **Priority**: `P1`
- **Files**: `dist/` output, Pages deployment config (`.` vs `/docs` root, CNAME/base)
- **Evidence** (this run): site root serves 404 while robots.txt serves 200; generated
  `dist/robots.txt` references `https://example.com/sitemap-index.xml` and
  `dist/sitemap-index.xml` uses the `example.com` loc (F006 — SITE_URL placeholder
  unset in generated output, verified this run).
- **Impact**: deployed product unreachable at root; users land on 404; sitemap URL
  points at placeholder domain.
- **Fix**: verify Pages source directory/`base` config matches dist/ output and set
  SITE_URL (docs/deployment.md).

## Issue 5 — F018 (P1, chore): school data STALE 21+ days

- **Category**: `chore` / **Priority**: `P1`
- **Files**: `data/schools.csv`, `external/raw.csv`
- **Evidence**: `node scripts/check-freshness.js` → "Data is stale! Last update was 21
  days ago (threshold: 7 days)"; Record Count 2 (fixture-scale dataset).
- **Fix**: run `npm run fetch-data`/ETL refresh, or document fixture exemption policy.

## Issue 6 — F068 (P1, test): pytest gate not reproducible in CI (environment-shift re-confirmed)

- **Category**: `test` / **Priority**: `P1`
- **Files**: `requirements.txt`, `package.json` (`test:py:pytest`, `test:all`), `tests/`,
  CI workflows
- **Evidence** (this run): with `pip install -r requirements.txt` performed manually,
  `python3 -m pytest tests/` → **13/13 PASS**, dist/ preserved (14 → 14, F066 intact).
  But requirements.txt is still not installed by any CI workflow, so a fresh CI checkout
  still fails at `test:py:pytest` (`No module named pytest`). Fallback
  `tests/run_tests.py` passes 27/27 with dist/ intact.
- **Impact**: pytest path works only when deps are manually installed; CI parity gap.
- **Fix**: add `pip install -r requirements.txt` to CI or document a setup step; align
  `test:py` and `test:py:pytest` outcomes.

## Issue 7 — F021 (P2, ci): pre-commit security gate is dead — swallows violations

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.husky/pre-commit`
- **Evidence** (read this run): `node scripts/check-workflow-security.js 2>/dev/null ||
echo "⚠️ … skipped"` — non-zero exit converted to a passing echo; 12 live violations
  (F037/F038) pass pre-commit silently.
- **Impact**: the repo's only local security gate is a no-op.
- **Fix**: remove the `|| echo` swallow so pre-commit fails on violations.

## Issue 8 — F073 (P2, bug): `validate-links.js` silently treats non-IntegrationError probes as valid

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `scripts/validate-links.js:89-94`, `:148-151`
- **Domain table**: C — Feedback 78/100; A — Error Handling 78/100
- **Evidence** (source read this run): in the `safeStat` catch, only
  `error.name === 'IntegrationError'` is recorded broken; ANY other error (EACCES,
  ENOTDIR, IO) is swallowed and the link considered valid. Additionally
  `processConcurrently` results are filtered `status === 'fulfilled'` — rejected probes
  silently drop out of the broken list.
- **Impact**: broken/errored links pass validation; sitemap/freshness gates silently
  miss real problems.
- **Fix**: record non-IntegrationError errors as broken with the message (or rethrow),
  and count rejected probes as failures rather than filtering them.

## Issue 9 — F076 (P2, ci): on-pull.yml `continue-on-error: true` masks infra failures

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.github/workflows/on-pull.yml:44,51`
- **Domain table**: B — Stability 82/100; D — CI/CD Health 37/100
- **Evidence** (YAML read this run): `actions/checkout@v7` (line 44) and
  `actions/setup-node@v7` (line 51) both set `continue-on-error: true` — a checkout or
  node-setup failure does not fail the job; the opencode `On-Pull` step then runs
  against a potentially empty/stale checkout. Compounding: PR-triggered runs currently
  conclude `action_required` with zero jobs (bot token) — the effective CI is the
  hourly schedule run (observed green 12:52Z + 14:15Z this run).
- **Impact**: infrastructure failures are invisible; runs can "pass" in a broken env.
- **Fix**: remove `continue-on-error` from both steps (fail fast on infra), or gate the
  opencode step on preceding steps' success.

## Issue 10 — F064 (P2, ci): Node engine parity gap

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.nvmrc` (=22), `.github/workflows/on-pull.yml:53`, `parallel.yml`
  (node-version 20), `package.json` (`lint-staged@17.3.0` requires node ≥22.22.1)
- **Evidence**: runtime v20.20.2; three sources disagree on engine.
- **Fix**: align all on 22, then re-verify `npm ci` + lint-staged.

## Issue 11 — F070 (P3, bug): `terminate()` discards error detail (stack + `details` lost)

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `scripts/utils.js:283-294` + CLI `.catch` sites (build-pages.js:61,
  etl.js:449, fetch-data.js:425, validate-links.js, sitemap.js, check-freshness.js:218,
  freshness-report.js, data-quality.js:353/387)
- **Domain table**: C — Debuggability 72/100; A — Error Handling 78/100
- **Evidence** (source read this run): `terminate(message, code = 1)` logs only the
  string `message`; callers pass only `error.message`. `IntegrationError`
  `details`/`stack` never reach the log.
- **Impact**: production incident diagnosis = one-line message.
- **Fix**: accept an optional error object and `logger.error({ err: error }, message)`.

## Issue 12 — F071 (P3, docs): env vars undocumented in `.env.example`

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `src/services/BuildOrchestrator.js:408-421`, `scripts/fetch-data.js:36-39`,
  `scripts/build-performance.js:19-25`, `.env.example`
- **Domain table**: C — Documentation Accuracy 72/100; D — Config & Env Parity 33/100
- **Evidence** (grep this run): **14 unique `process.env.*` reads** in source
  (excluding tests); `.env.example` documents only 7. Consumed-but-undocumented
  include GITHUB_STEP_SUMMARY, EXTERNAL_DATA_DIR, PERF_MAX_BUILD_TIME_MS,
  PERF_MAX_FAILED_PAGES, PERF_MAX_MEMORY_BYTES, PERF_MIN_THROUGHPUT +
  build-concurrency family.
- **Impact**: operators cannot tune perf budgets / data dir without reading source.
- **Fix**: add the undocumented vars with defaults and units to `.env.example`.

## Issue 13 — F072 (P3, refactor): `interactive.js` bypasses pino with 28 raw console calls

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/interactive.js` (28 occurrences), `eslint.config.js`
  (`no-console: 'off'`)
- **Domain table**: B — Observability 69/100; D — Tech Debt 59/100
- **Evidence** (grep this run): 24 `console.log` + 2 `console.error` + 2
  `console.clear` = 28 `console.*`; `eslint.config.js` disables `no-console` so the
  bypass is unguarded. `logger.js` (pino) used by every other script.
- **Impact**: interactive CLI output unobservable/untestable.
- **Fix**: route interactive.js through `logger`, restore `no-console` as error.

## Issue 14 — F005 (P3, docs): Prettier drift — HELD stable at 88 files

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/issues/**` (88 files), `docs/audit/**`
- **Evidence**: `npm run format:check` → exit 1, 88 files (unchanged from 116th's
  post-repair state). All under `docs/issues/`; **0 source files**
  (`npx prettier --check scripts src` → clean). This run's 3 new records written
  Prettier-clean so the ledger does not grow.
- **Impact**: `npx prettier --check` cannot be a CI gate while the docs ledger is
  non-conformant.
- **Fix**: one-time `npx prettier --write docs/issues docs/audit` docs-only PR.

## Issue 15 — F075 (P3, refactor): dead source exports (zero production importers)

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/utils.js:178-180,190-196,198-204,246-278,421-436`,
  `scripts/config.js:18-23,129,136`, `scripts/fs-safe.js:272-274,287`
- **Domain table**: A — Maintainability 71/100; D — Tech Debt 59/100
- **Evidence** (export dump + importer grep, this run — zero production importers):
  `clearEscapeHtmlCache`, `hasCoordinateData`, `formatEmptyValue`, `escapeCsvField`
  (external; only internal use by writeCsv), `validatePath` (external; only internal
  use), `createFsSafe`, `resetCircuitBreakers`.
- **Impact**: dead surface misleads maintainers, inflates API docs.
- **Fix**: remove or fold into internal-only use; keep test coverage intact.

## Issue 16 — F077 (P3, docs): deployment.md + api.md internal contradictions

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/deployment.md:34-45,318`, `docs/api.md` changelog, `package.json`
- **Domain table**: C — Documentation Accuracy 72/100
- **Evidence**: deployment.md:45 claims `dist/data/schools.json` "(symlink or copy)" —
  actual build emits `dist/data/schools.csv` (verified `ls dist/data/` this run).
  api.md Changelog lists "Version 2.0.0 (2026-07-20)" while header says "Current
  Version: 1.0.0" and package.json version is 1.0.0.
- **Impact**: operators look for schools.json in dist/data and find a CSV; version
  confusion in changelog.
- **Fix**: correct deployment.md to schools.csv; reconcile api.md version labels.

## Issue 17 — F078 (P3, refactor): duplication cluster across escaping/status/fields/aggregation

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/utils.js:151-172,185-186,216-278`, `scripts/sitemap.js:41-62`,
  `src/presenters/templates/homepage.js:36,56-96,317-321,358,490-501`,
  `src/presenters/templates/school-page.js:28-31,75`,
  `src/presenters/templates/province-page.js:72,103`, `src/services/PageBuilder.js:10,126-153`,
  `src/services/BuildOrchestrator.js:141,257-266`, `scripts/etl.js:88,91-92,123-124,131`,
  `scripts/data-schema.js:156,263`, `scripts/data-quality.js:190-195`,
  `scripts/check-freshness.js:80`, `scripts/slugify.js:54-58`
- **Domain table**: A — Simplicity 76/100; A — Maintainability 71/100; D — Tech Debt 59/100
- **Evidence** (source reads this run):
  - HTML escaping ×2+: utils.escapeHtml (utils.js:151) vs sitemap.escapeXml
    (sitemap.js:59, near-verbatim) + homepage client-side DOM escapeHtml.
  - Status labels ×6: utils.formatStatus, homepage.js:36/358/495, etl.js:91 (inverse),
    data-quality.js:190.
  - Required-fields ×3: PageBuilder.js:10 and school-page.js:75 (5 fields) vs
    data-schema.js:156 and etl.js:123 (6 fields) — REQUIRED_SCHOOL_FIELDS (TASK-079)
    consolidated but PageBuilder/school-page still inline.
  - Province aggregation ×3: PageBuilder.getUniqueProvinces, homepage.js:56,
    BuildOrchestrator.js:257.
  - Canonical URL ×3, Date normalization ×4, Cache eviction ×2.
- **Impact**: drift-prone (already diverged); 3× maintenance per concept.
- **Fix**: single-source escaping/status/required-fields/province-aggregation helpers;
  templates consume shared utils (contract-friendly, not cosmetic).

## Issue 18 — F079 (P3, bug): homepage client CSV lacks formula-injection protection

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `src/presenters/templates/homepage.js:526-535`,
  `scripts/utils.js:246-278` (`escapeCsvField`)
- **Domain table**: B — Security Practices 46/100; A — Correctness 77/100
- **Evidence** (source read this run): server-side `escapeCsvField` guards CSV formula
  injection (`=`, `+`, `-`, `@` prefix) but the inline client-side CSV downloader only
  quote-doubles (`replace(/"/g, '""')`) — a school name starting with `=` would execute
  as a formula when the downloaded CSV is opened in a spreadsheet.
- **Impact**: CSV injection via untrusted school-name data exported client-side.
- **Fix**: replicate the formula-injection guard in the client CSV writer (or share a
  single escaping routine).

## Issue 19 — F080 (P3, docs): test-count drift — roadmap 902 / testing.md 1030 / actual 1091

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/roadmap.md:19` ("902+ tests"), `docs/testing.md:228` ("1030 test
  cases"), `package.json`, `scripts/*.test.js`
- **Domain table**: C — Documentation Accuracy 72/100
- **Evidence** (fresh `npm run test:js` this run): **1091 tests** (1087 pass, 4
  skipped, 0 fail, 102 suites) + 27 Python (fallback) + 13 pytest. roadmap.md "902+"
  and testing.md "1030" both stale.
- **Impact**: documented test confidence underestimates the real suite.
- **Fix**: update roadmap/testing.md counts (or reference the live `npm run test:ci`
  summary instead of hardcoding).

## Issue 20 — F081 (P3, chore): cache caps inconsistent — 50000 vs 10000

- **Category**: `chore` / **Priority**: `P3`
- **Files**: `scripts/utils.js:149` (`ESCAPE_HTML_CACHE_MAX = 50000`),
  `scripts/config.js:99` (`CACHE_DEFAULTS.MAX_CACHE_SIZE = 10000`),
  `scripts/slugify.js:12`
- **Domain table**: A — Consistency 80/100; D — Config & Env Parity 33/100
- **Evidence** (source read this run): two cache-size constants for the same concept —
  utils.js hardcodes 50000 for the escapeHtml cache while slugify reads
  `CONFIG.CACHE_DEFAULTS.MAX_CACHE_SIZE` (10000). No shared source of truth.
- **Impact**: memory-profile drift between caches; tuning requires editing two places.
- **Fix**: route both caches through a single config constant.

## Issue 21 — F082 (P3, refactor): circular cross-layer dependency + re-export shim

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/sitemap.js:21`, `src/services/BuildOrchestrator.js:16-51`,
  `src/services/PageBuilder.js`, `src/presenters/templates/{homepage,province-page,school-page}.js`,
  `scripts/build-pages.js:22-56`
- **Domain table**: A — Modularity & SRP 68/100; A — Maintainability 71/100
- **Evidence** (require-graph reconstruction this run): cycle scripts → services →
  presenters → scripts (sitemap.js:21 imports PageBuilder; BuildOrchestrator.js:51
  imports sitemap; PageBuilder imports templates; templates import
  scripts/config+utils+slugify+resilience). build-pages.js re-exports 15 orchestrator
  functions "for backward-compatible imports" (shim, lines 22-28 verified).
- **Impact**: any change to shared helpers ripples across all layers; circular imports
  risk init-order surprises.
- **Fix**: move shared helpers to a neutral shared module consumed by all layers;
  retire the re-export shim once callers are migrated.

## Findings verified CLEAN (no new issue needed)

- **F066** — dist-wipe: **RESOLVED** (run_tests.py tempfile isolation; dist/ 14 → 14
  through Python gate, 10th consecutive) ✅
- **F069** — build-exit-0: **RESOLVED** (BuildOrchestrator.js:497-506 throws
  PERFORMANCE_BUDGET_VIOLATION) ✅
- **F074** — api.md phantom exports: **RESOLVED** (homepage.js:701-705 =
  {generateHomepageHtml, aggregateProvinceAndFilters}; api.md:2816 aligned; verified
  this run) ✅
- **F026** — formatBytes NaN: **RESOLVED** (build-performance.js:186-200 `!Number.isFinite`
  guard with explicit F026 comment — output is "NaN", not "NaN undefined") ✅
- **F014** — parallel test race: 27th consecutive clean run (1087/1087 pass) ✅
- **F024** — sitemap `sitemap-index.xml` + `sitemap-001.xml` emitted on every build ✅
- **F028** — `npm audit` 0 vulnerabilities ✅ (maintained RESOLVED)
- **F017/F057** — `addNumbers` absent from docs/api.md (0 hits) ✅ (maintained RESOLVED)
- **F029** — worktree clean post-matrix ✅
- Lint 0/0 ✅ · cover gate 95.19% stmt / 92.9% branch / 97.14% funcs ✅ · Python
  fallback 27/27 + pytest 13/13 ✅ (dist intact — F066) · build exit 0 budgets PASS ✅
