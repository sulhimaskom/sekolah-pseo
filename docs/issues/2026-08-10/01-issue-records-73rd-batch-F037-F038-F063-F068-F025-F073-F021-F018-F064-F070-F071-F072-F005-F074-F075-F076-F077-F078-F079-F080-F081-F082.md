# Issue Records — 73rd batch (114th verification run, 2026-08-10)

**Evaluation Date**: 2026-08-10
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo · Default Branch: main (`0e14115`)
**Note**: GitHub-issue creation remains blocked by F002 (integration token lacks
`issues: write`; 103rd consecutive denial re-verified this run via `gh issue create` →
`GraphQL: Resource not accessible by integration (createIssue)`, collaborator
permission `none`). Per repo convention findings ship as labeled docs records — each
with exactly one category label and one priority label (contract §4) — ready for bulk
creation when permissions are granted. Records written Prettier-clean per F005
convention (ledger did not grow: format:check still 88 files).

## Issue 1 — F037/F038 (P0, security): workflow secret exposure — 12 violations HELD (15th regression)

- **Category**: `security` / **Priority**: `P0`
- **Files**: `.github/workflows/on-push.yml`, `parallel.yml`, `architect-agent.yml`,
  `opencode.yml`, `orchestrator.yml`, `template.md`
- **Domain table**: B — Security Practices 46/100; D — CI/CD Health 37/100
- **Evidence** (fresh `node scripts/check-workflow-security.js` → exit 1, **12
  violations**, byte-identical to 103rd–113th): 2× CRITICAL `DUPLICATE_API_KEY`
  (`on-push.yml`, `parallel.yml` — API_KEY aliases secrets.GEMINI_API_KEY), 4× HIGH
  `ID_TOKEN_WRITE` (architect-agent.yml:13, opencode.yml:18, orchestrator.yml:9,
  parallel.yml:16), 4× HIGH `ACTIONS_WRITE_NON_MERGE` (architect-agent.yml:17,
  opencode.yml:22, orchestrator.yml:13, parallel.yml:15), 2× HIGH
  `GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN` (orchestrator.yml ×2, architect-agent.yml ×1).
  `SECURITY_AUDIT_NOTE.md` claims these fixed — claims contradict current files.
- **Impact**: repository is PUBLIC; over-permissive tokens + secret duplication remain
  externally triggerable.
- **Fix** (blocked — requires `workflows: write` (F050) / workflow-file push): remove
  `API_KEY` aliases, s/GH_TOKEN/GITHUB_TOKEN, drop `id-token`/`actions` from
  non-merge workflows, and make `check-workflow-security.js` a failing gate (F021).

## Issue 2 — F063 (P1, ci): orchestrator.yml dead — 10/10 failed runs

- **Category**: `ci` / **Priority**: `P1`
- **Files**: `.github/workflows/orchestrator.yml`
- **Evidence**: `gh run list --workflow=orchestrator.yml` → 10/10 `failure` (5/5 this
  run; latest 2026-08-10 01:11:26Z). `secrets.GH_TOKEN` fictitious (no such secret
  defined) at checkout/env → job fails pre-run (verified at orchestrator.yml:33,41).
- **Impact**: the repo's namesake orchestrator automation dead 10 consecutive runs; no
  CI workflow runs lint+build+test as a hard gate.
- **Fix** (blocked — workflow-file write): replace `secrets.GH_TOKEN` with
  `secrets.GITHUB_TOKEN` (2 sites), re-trigger, then add a true gate
  (lint+build+test) to on-pull.yml.

## Issue 3 — F068 (P1, test): pytest gate not reproducible — `No module named pytest`

- **Category**: `test` / **Priority**: `P1`
- **Files**: `requirements.txt`, `package.json` (`test:py:pytest`, `test:all`), `tests/`
- **Evidence**: `python3 -m pytest --version` → `/usr/bin/python3: No module named
pytest`; `test:all` fails at that stage; `tests/run_tests.py` fallback 27/27 PASS
  (dist/ preserved — F066 RESOLVED). requirements.txt declares pytest but no CI
  installs Python deps; docs/setup.md marks pytest "Optional".
- **Impact**: the pytest-based test path is dead; only the fallback runs.
- **Fix**: add `pip install -r requirements.txt` to CI or document a setup step; align
  `test:py` and `test:py:pytest` outcomes.

## Issue 4 — F025 (P1, chore): live site root 404 behind green Pages deploy

- **Category**: `chore` / **Priority**: `P1`
- **Files**: `dist/` output, Pages deployment config (`.` vs `/docs` root, CNAME/base)
- **Evidence** (this run): site root serves 404 while Pages reports built; robots.txt
  serves 200; `robots.txt` references `https://example.com/sitemap-index.xml` (F006 —
  SITE_URL placeholder unset in generated output).
- **Impact**: deployed product unreachable at root; users land on 404; sitemap URL
  points at placeholder domain.
- **Fix**: verify Pages source directory/`base` config matches dist/ output and set
  SITE_URL (docs/deployment.md).

## Issue 5 — F073 (P2, bug): `validate-links.js` silently treats non-IntegrationError probes as valid

- **Category**: `bug` / **Priority**: `P2`
- **Files**: `scripts/validate-links.js:89-94`, `:148-151`
- **Domain table**: C — Feedback 75/100; A — Error Handling 76/100
- **Evidence** (source read this run + explore error-handling inventory): in the
  `safeStat` catch, only `error.name === 'IntegrationError'` is recorded broken; ANY
  other error (EACCES, ENOTDIR, IO) is swallowed and the link considered valid.
  Additionally `processConcurrently` results are filtered `status === 'fulfilled'` —
  rejected probes (queue timeout, read failure) silently drop out of the broken list.
- **Impact**: broken/errored links pass validation; sitemap/freshness gates silently
  miss real problems.
- **Fix**: record non-IntegrationError errors as broken with the message (or rethrow),
  and count rejected probes as failures rather than filtering them.

## Issue 6 — F021 (P2, ci): pre-commit security gate is dead — swallows violations

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.husky/pre-commit`
- **Evidence** (read this run): `node scripts/check-workflow-security.js 2>/dev/null ||
echo "⚠️ … skipped"` — non-zero exit converted to a passing echo; 12 live violations
  (F037/F038) pass pre-commit silently.
- **Impact**: the repo's only local security gate is a no-op.
- **Fix**: remove the `|| echo` swallow so pre-commit fails on violations.

## Issue 7 — F018 (P2, chore): school data STALE 21+ days

- **Category**: `chore` / **Priority**: `P2`
- **Files**: `data/schools.csv`, `external/raw.csv`
- **Evidence**: `data/schools.csv` "Last Update: 2026-07-20" (21+ days); threshold 7;
  record count 2 (fixture-scale dataset).
- **Fix**: run `npm run fetch-data`/ETL refresh, or document fixture exemption policy.

## Issue 8 — F064 (P2, ci): Node engine parity gap

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.nvmrc` (=22), `.github/workflows/on-pull.yml:53`, `parallel.yml`
  (node-version 20), `package.json` (`lint-staged@17.3.0` requires node ≥22.22.1)
- **Evidence**: runtime v20.20.2; `npm install` emitted EBADENGINE warning this run —
  three sources disagree on engine.
- **Fix**: align all on 22, then re-verify `npm install` + lint-staged.

## Issue 9 — F070 (P3, bug): `terminate()` discards error detail (stack + `details` lost)

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `scripts/utils.js:285-292` + CLI `.catch` sites (build-pages.js:61,
  etl.js:449, fetch-data.js:425, validate-links.js, sitemap.js, check-freshness.js:218,
  freshness-report.js, data-quality.js:353/387)
- **Domain table**: C — Debuggability 72/100; A — Error Handling 76/100
- **Evidence** (source read this run): `terminate(message, code = 1)` logs only the
  string `message`; callers pass only `error.message`. `IntegrationError`
  `details`/`stack` never reach the log.
- **Impact**: production incident diagnosis = one-line message.
- **Fix**: accept an optional error object and `logger.error({ err: error }, message)`.

## Issue 10 — F071 (P3, docs): env vars undocumented in `.env.example`

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `src/services/BuildOrchestrator.js:408-421`, `scripts/fetch-data.js:36-39`,
  `scripts/build-performance.js:19-25`, `.env.example`
- **Domain table**: C — Documentation Accuracy 68/100; D — Config & Env Parity 33/100
- **Evidence** (grep this run): 15 `process.env.*` reads in source incl. test-only
  `TEST_TEMP_DIR` (14 non-test); `.env.example` documents only 7. 7
  consumed-but-undocumented (GITHUB_STEP_SUMMARY, EXTERNAL_DATA_DIR,
  PERF_MAX_BUILD_TIME_MS, PERF_MAX_FAILED_PAGES, PERF_MAX_MEMORY_BYTES,
  PERF_MIN_THROUGHPUT + build-concurrency family).
- **Impact**: operators cannot tune perf budgets / data dir without reading source.
- **Fix**: add the undocumented vars with defaults and units to `.env.example`.

## Issue 11 — F072 (P3, refactor): `interactive.js` bypasses pino with 28 raw console calls

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/interactive.js` (28 occurrences), `eslint.config.js`
  (`no-console: 'off'`, 2 rule blocks)
- **Domain table**: B — Observability 69/100; D — Tech Debt 58/100
- **Evidence** (grep this run): 24 `console.log` + 2 `console.error` + 2
  `console.clear` = 28 `console.*`; `eslint.config.js` disables `no-console` so the
  bypass is unguarded. `logger.js` (pino) used by every other script.
- **Impact**: interactive CLI output unobservable/untestable.
- **Fix**: route interactive.js through `logger`, restore `no-console` as error.

## Issue 12 — F005 (P3, docs): Prettier drift — HELD at 88 files

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/issues/**` (88 files), `docs/audit/**`
- **Evidence**: `npm run format:check` → exit 1, "Code style issues found in **88
  files**"; all under `docs/issues/`; **0 source files** (`npx prettier --check scripts
src` → clean). This run's 3 new records written Prettier-clean so the ledger did not
  grow (convention runs 95–114).
- **Impact**: `npx prettier --check` cannot be a CI gate while the docs ledger is
  non-conformant.
- **Fix**: one-time `npx prettier --write docs/issues docs/audit` docs-only PR.

## Issue 13 — F074 (NEW, P3, docs): api.md phantom + undocumented exports

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/api.md` (homepage section ~2761-2884, utils, fs-safe, manifest,
  validate-links, etl, fetch-data sections), `src/presenters/templates/homepage.js`,
  `scripts/{utils,fs-safe,manifest,validate-links,etl,fetch-data}.js`
- **Domain table**: C — API Clarity 70/100; C — Documentation Accuracy 68/100
- **Evidence** (node export dump vs api.md export lists, this run):
  - PHANTOM (documented, do not exist): `aggregateByProvince`, `extractFilterOptions`
    as homepage.js exports (api.md:2761,2763,2799,2861,2821,2878) — actual homepage.js
    exports only `aggregateProvinceAndFilters` + `generateHomepageHtml`. Same family as
    the resolved F017 (addNumbers) — a recurrence of the phantom-doc pattern.
  - UNDOCUMENTED (exist, absent from api.md export lists): `processInBatches`
    (utils.js), `fastMkdir` + `fastWriteFile` (fs-safe.js), `removeOrphanedSchoolPages`
    (BuildOrchestrator.js), `isRelativeLink` (validate-links.js),
    `getOrphanedSchoolPaths` (manifest.js), `run` (etl.js), `main` (fetch-data.js).
- **Impact**: consumers coding against api.md hit ReferenceErrors; real API surface
  invisible to maintainers.
- **Fix**: remove phantom entries or implement them; add the 8 undocumented exports to
  api.md export lists (or refactor re-export shims so docs match).

## Issue 14 — F075 (NEW, P3, refactor): 7 dead source exports

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/utils.js:178-180,190-196,198-204,246-278,421-436`,
  `scripts/config.js:18-23,129,136`, `scripts/fs-safe.js:272-274,287`
- **Domain table**: A — Maintainability 70/100; D — Tech Debt 58/100
- **Evidence** (export dump + importer grep, this run — zero production importers):
  `clearEscapeHtmlCache`, `hasCoordinateData`, `formatEmptyValue`, `escapeCsvField`
  (external; only internal use by writeCsv), `validatePath` (external; only internal
  use), `createFsSafe`, `resetCircuitBreakers`.
- **Impact**: dead surface misleads maintainers, inflates API docs, adds
  no-op exports to maintain.
- **Fix**: remove or fold into internal-only use; keep test coverage intact.

## Issue 15 — F076 (NEW, P2, ci): on-pull.yml `continue-on-error: true` masks infra failures

- **Category**: `ci` / **Priority**: `P2`
- **Files**: `.github/workflows/on-pull.yml:44,51`
- **Domain table**: B — Stability 82/100; D — CI/CD Health 37/100
- **Evidence** (YAML read this run): `actions/checkout@v7` step (line 44) and
  `actions/setup-node@v7` step (line 51) both set `continue-on-error: true` — a
  checkout or node-setup failure does not fail the job; the subsequent opencode
  `On-Pull` step runs against a potentially empty/stale checkout or wrong node,
  producing confusing or falsely-green results.
- **Impact**: infrastructure failures are invisible; the hourly run can "pass" while
  executing in a broken environment.
- **Fix**: remove `continue-on-error` from both steps (fail fast on infra), or gate the
  opencode step on the preceding steps' success.

## Issue 16 — F077 (NEW, P3, docs): deployment.md + api.md internal contradictions

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/deployment.md:34-45,318`, `docs/api.md` changelog, `package.json`
- **Domain table**: C — Documentation Accuracy 68/100; C — API Clarity 70/100
- **Evidence**: deployment.md:45 claims `dist/data/schools.json` "(symlink or copy)" —
  actual build emits `dist/data/schools.csv` (verified `ls dist/data/` after build).
  api.md Changelog lists "Version 2.0.0 (2026-07-20)" while header says "Current
  Version: 1.0.0" and package.json version is 1.0.0; two entries labelled
  "Version 1.2.0" carry different dates (2026-06-29 and 2026-07-04).
- **Impact**: operators look for schools.json in dist/data and find a CSV; version
  confusion in changelog.
- **Fix**: correct deployment.md to schools.csv; reconcile api.md version labels.

## Issue 17 — F078 (NEW, P3, refactor): duplication cluster across escaping/status/fields/aggregation

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/utils.js:151-172,185-186,216-278`, `scripts/sitemap.js:41-62`,
  `src/presenters/templates/homepage.js:36,56-96,317-321,358,490-501`,
  `src/presenters/templates/school-page.js:28-31,75`,
  `src/presenters/templates/province-page.js:72,103`, `src/services/PageBuilder.js:10,126-153`,
  `src/services/BuildOrchestrator.js:141,257-266`, `scripts/etl.js:88,91-92,123-124,131`,
  `scripts/data-schema.js:156,263`, `scripts/data-quality.js:190-195`,
  `scripts/check-freshness.js:80`, `scripts/slugify.js:54-58`
- **Domain table**: A — Simplicity 76/100; A — Maintainability 70/100; D — Tech Debt 58/100
- **Evidence** (explore duplication/coupling inventory, this run):
  - HTML escaping ×3: utils.escapeHtml vs sitemap.escapeXml (near-verbatim map+regex;
    only `&apos;` vs `&#39;`) + homepage client-side DOM escapeHtml.
  - Status labels ×6: utils.formatStatus, homepage.js:36/358/495, etl.js:91 (inverse),
    data-quality.js:190.
  - Required-fields ×3+ : PageBuilder.js:10 and school-page.js:75 (5 fields) vs
    data-schema.js:156 and etl.js:123 (6 fields) — lists already diverged.
  - Province aggregation ×3: PageBuilder.getUniqueProvinces, homepage.js:56,
    BuildOrchestrator.js:257.
  - Canonical URL ×3: school-page.js:28, province-page.js:72, BuildOrchestrator.js:141.
  - Date normalization ×4: etl.js:88, sitemap.js:74/88, check-freshness.js:80.
  - Cache eviction ×2: slugify.js:54-58, utils.js:165-168.
- **Impact**: drift-prone (diverged already); 3× maintenance per concept.
- **Fix**: single-source escaping/status/required-fields/province-aggregation
  helpers; templates consume shared utils (contract-friendly, not cosmetic).

## Issue 18 — F079 (NEW, P3, bug): homepage client CSV lacks formula-injection protection

- **Category**: `bug` / **Priority**: `P3`
- **Files**: `src/presenters/templates/homepage.js:490-501`,
  `scripts/utils.js:246-278` (`escapeCsvField`)
- **Domain table**: B — Security Practices 46/100; A — Correctness 76/100
- **Evidence** (source read this run): server-side `escapeCsvField` guards CSV
  formula injection (`=`, `+`, `-`, `@` prefix) but the inline client-side CSV
  downloader only quote-doubles (`replace(/"/g, '""')`) — a school name starting with
  `=` (e.g. "=SUM(...)") would execute as a formula when the downloaded CSV is opened
  in a spreadsheet.
- **Impact**: CSV injection via untrusted school-name data exported client-side.
- **Fix**: replicate the formula-injection guard in the client CSV writer (or share a
  single escaping routine).

## Issue 19 — F080 (NEW, P3, docs): test-count drift — roadmap 902 / testing.md 1030 / actual 1061

- **Category**: `docs` / **Priority**: `P3`
- **Files**: `docs/roadmap.md` ("902+ tests"), `docs/testing.md` ("1030 test cases"),
  `package.json`, `scripts/*.test.js`
- **Domain table**: C — Documentation Accuracy 68/100
- **Evidence** (fresh `npm run test:js` this run): **1061 tests** (1057 pass, 4
  skipped, 0 fail, 98 suites) + 27 Python. roadmap.md "902+" and testing.md "1030"
  both stale.
- **Impact**: documented test confidence underestimates the real suite; counts
  misleading for contribution review.
- **Fix**: update roadmap/testing.md counts (or reference the live `npm run test:ci`
  summary instead of hardcoding).

## Issue 20 — F081 (NEW, P3, chore): cache caps inconsistent — 50000 vs 10000

- **Category**: `chore` / **Priority**: `P3`
- **Files**: `scripts/utils.js:149` (`ESCAPE_HTML_CACHE_MAX = 50000`),
  `scripts/config.js:98-100` (`CACHE_DEFAULTS.MAX_CACHE_SIZE = 10000`),
  `scripts/slugify.js:12`
- **Domain table**: A — Consistency 80/100; D — Config & Env Parity 33/100
- **Evidence** (source read this run): two cache-size constants for the same concept —
  utils.js hardcodes 50000 for the escapeHtml cache while slugify reads
  `CONFIG.CACHE_DEFAULTS.MAX_CACHE_SIZE` (10000). No shared source of truth.
- **Impact**: memory-profile drift between caches; tuning requires editing two places.
- **Fix**: route both caches through a single config constant.

## Issue 21 — F082 (NEW, P3, refactor): circular cross-layer dependency + re-export shim

- **Category**: `refactor` / **Priority**: `P3`
- **Files**: `scripts/sitemap.js:19`, `scripts/freshness-report.js:22`,
  `src/services/BuildOrchestrator.js:16-50`, `src/services/PageBuilder.js:6-8`,
  `src/presenters/templates/{homepage,province-page,school-page}.js:1-3`,
  `scripts/build-pages.js:22-56`
- **Domain table**: A — Modularity & SRP 68/100; A — Maintainability 70/100
- **Evidence** (require-graph reconstruction, explore inventory): cycle
  scripts → services → presenters → scripts (sitemap.js:19 imports PageBuilder;
  BuildOrchestrator.js:50 imports sitemap; PageBuilder imports templates; templates
  import scripts/config+utils+slugify+resilience). build-pages.js re-exports 15
  orchestrator functions "for backward-compatible imports" (shim, lines 22-56).
  Templates reach 3-4 levels into script-layer internals, contradicting the
  blueprint's controller/service/presentation contract.
- **Impact**: any change to shared helpers ripples across all layers; circular
  imports risk init-order surprises.
- **Fix**: move shared helpers to a neutral shared module consumed by all layers;
  retire the re-export shim once callers are migrated.

## Findings verified CLEAN (no new issue needed)

- **F066** — dist-wipe: **RESOLVED** (PR #646; run_tests.py:229-245 tempfile isolation
  verified; test:py left dist/ intact this run) ✅
- **F069** — build-exit-0: **RESOLVED** (PR #647; BuildOrchestrator.js:486-497 throws
  PERFORMANCE_BUDGET_VIOLATION verified) ✅
- **F014** — parallel test race: 24th consecutive clean run (1057/1057 pass) ✅
- **F024** — sitemap `sitemap-index.xml` + `sitemap-001.xml` emitted on every build ✅
- **F028** — `npm audit` 0 vulnerabilities ✅ (maintained RESOLVED)
- **F017/F057** — `addNumbers` absent from docs/api.md (0 hits) ✅ (maintained RESOLVED)
- **F029** — `external/raw.csv` untouched; worktree clean post-matrix ✅
- Lint 0/0 ✅ · cover gate 94.95% stmt / 92.32% branch / 96.65% funcs ✅ · Python
  fallback 27/27 ✅ (dist intact — F066) · build exit 0 budgets PASS ✅
