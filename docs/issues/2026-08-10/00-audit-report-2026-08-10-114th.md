# Phase 1 — Diagnostic & Comprehensive Scoring Report (114th verification, 2026-08-10)

**Evaluation Date**: 2026-08-10
**Evaluator**: Sisyphus (ULW Loop, autonomous run)
**Repository**: sulhimaskom/sekolah-pseo
**Default Branch**: main (`0e14115` — PR #648 dependabot globals bump merged 09:29Z; HEAD == origin/main, verified via `git fetch` + `rev-parse`) [detected: main]
**Trigger**: `ulw-loop` — Phase 0.1/0.2 verified 0 open PRs / 0 open issues (`gh pr list`,
`gh issue list` → both `[]`) → Phase 0.3 EMPTY → PHASE 1 (AUDIT MODE, read-only).
**Mode**: Full command matrix re-executed fresh; every result witnessed firsthand
in-session. No production source modified; worktree clean at start and end.

## Skills used (contract §5)

Project skills surveyed (`.opencode/skill/`, 7 entries):
maxritter-claude-codepro-backend-models-standards, obra-superpowers-systematic-debugging,
modu-ai-moai-adk-moai-tool-opencode, madappgang-claude-code-debugging-strategies,
muratcankoylan-agent-skills-for-context-engineering-memory-systems,
proffesor-for-testing-agentic-qe-skill-builder, vasilyu1983-ai-agents-public-git-commit-message.
All Phase-1 evidence below is empirical: fresh command execution, `gh` API probes,
workflow YAML reads, git forensics, and 4 parallel explore subagents (error handling,
duplication/coupling, security surfaces, docs-vs-code) whose output is quoted in the
criterion evidence. Built-in `security-review` not invoked (no code written; audit only);
`review-work` not invoked (no PR/completed work to QA).

## Delegation self-check (contract §6)

4 parallel `explore` subagents (background) ran for criteria evidence; 3 delivered
complete inventories (error handling 4m16s, duplication/coupling 4m47s, docs-vs-code
4m33s), the security-surface agent stalled in init (0 tool results after 14m37s) and was
cancelled — its scope was re-verified directly in-session (escapeHtml usage, slugify
sanitization, security headers, workflow secrets). Audit commands executed directly for
firsthand evidence — repo convention across runs 1–113.

## Command matrix (all fresh, exit codes recorded)

| Command                                   | Result | Evidence                                                               |
| ----------------------------------------- | ------ | ---------------------------------------------------------------------- |
| `npm install`                             | ✅ 0   | 131 packages; EBADENGINE warn for lint-staged engine (F064)            |
| `npm run build`                           | ✅ 0   | 27ms, 2 pages, 0 failed, all perf budgets PASS (F024 sitemaps emitted) |
| `npm run lint`                            | ✅ 0   | eslint 0 errors / 0 warnings                                           |
| `npm run test:js`                         | ✅ 0   | 1061 tests: 1057 pass, 4 skipped, 0 fail (98 suites)                   |
| `npm run test:py`                         | ✅ 0   | 27/27 PASS (dist/ preserved — F066 RESOLVED)                           |
| `npm run test:js:coverage`                | ✅ 0   | 94.95% stmt / 92.32% branch / 96.65% funcs — gates 80/75/75 met        |
| `npm run format:check`                    | ❌ 1   | 88 files, all `docs/issues/**`, 0 source files (F005 HELD)             |
| `node scripts/check-workflow-security.js` | ❌ 1   | 12 violations: 2 CRITICAL, 10 HIGH (F037/F038 HELD)                    |
| `npm audit`                               | ✅ 0   | 0 vulnerabilities (F028 maintained RESOLVED)                           |
| `gh issue create` probe                   | ❌ 1   | `GraphQL: Resource not accessible by integration (createIssue)` (F002) |
| `python3 -m pytest --version`             | ❌ 1   | `No module named pytest` (F068 HELD)                                   |

Global penalty rules: no build failure (−20), no test failure (−15), no critical npm
vulnerability (−20). Workflow-security CRITICALs are CI-surface findings (F037/F038)
reflected in B — Security Practices, not a runtime CVE penalty.

## Domain score table

| Domain                                | Score        | Grade | vs 113th |
| ------------------------------------- | ------------ | ----- | -------- |
| **A. Code Quality**                   | 77.0/100     | C+    | +2.4     |
| **B. System Quality**                 | 72.0/100     | C     | +0.6     |
| **C. Experience Quality**             | 72.5/100     | C     | +1.6     |
| **D. Delivery & Evolution Readiness** | 54.5/100     | C−    | +0.2     |
| **COMPOSITE**                         | **69.0/100** | **C** | **+1.2** |

Composite **(77.0 + 72.0 + 72.5 + 54.5) / 4 = 69.0** — first composite increase since
the F066/F069 fixes merged (PRs #646/#647): both P1/P2 defects are now RESOLVED and
empirically verified clean this run. Drags: F037/F038 (P0 security), F063 (P1 CI),
F068 (P1 test), F025 (P1 chore), plus 8 new findings F074–F081 (mostly P3 docs/refactor).

---

## A. CODE QUALITY — 77.0/100 (C+)

| Criterion                    | Weight | Score | Weighted |
| ---------------------------- | ------ | ----- | -------- |
| Correctness                  | 15     | 76    | 11.40    |
| Readability & Naming         | 10     | 85    | 8.50     |
| Simplicity                   | 10     | 76    | 7.60     |
| Modularity & SRP             | 15     | 68    | 10.20    |
| Consistency                  | 5      | 80    | 4.00     |
| Testability                  | 15     | 85    | 12.75    |
| Maintainability (Complexity) | 10     | 70    | 7.00     |
| Error Handling               | 10     | 76    | 7.60     |
| Dependency Discipline        | 5      | 78    | 3.90     |
| Determinism & Predictability | 5      | 84    | 4.20     |

### Correctness — 76 (deductions: 24)

- Observations: build + 1057/1057 JS tests + 27/27 Python green; F069 enforcement
  verified (BuildOrchestrator.js:486-497 throws `IntegrationError(PERFORMANCE_BUDGET_VIOLATION)`
  on failed pages > MAX_FAILED_PAGES); manifest v2 length-prefixed hashing in place.
- Evidence: `npm run build`/`test:js`/`test:py` exit 0 (matrix above); source reads
  BuildOrchestrator.js:486-497, manifest.js, run_tests.py:229-245 (tempfile isolation).
- Impact / Risk: residual correctness holes — F073 (validate-links.js:89-94 swallows
  non-IntegrationError → false "all links OK"), F079 (homepage inline client CSV
  lacks formula-injection guard, utils.escapeCsvField has it).
- Score Rationale: −12 for F073/F079 masking bugs; −8 for F018 stale-data drift;
  −4 residual (F070 loses error detail at CLI boundary).

### Readability & Naming — 85 (deductions: 15)

- Observations: named exports, JSDoc on public APIs, descriptive function names
  (`writeSchoolPagesConcurrently`, `computeSchoolHash`), module-level hoisted constants
  (`HTML_HEAD_PREFIX`, `BACK_TO_TOP_SCRIPT_BODY`, `CURRENT_YEAR`).
- Evidence: utils.js, slugify.js, head-meta.js, BuildOrchestrator.js reads; blueprint
  decision log entries 2026-08-03.
- Impact / Risk: minor — magic strings ('untitled' slugify.js:52), mixed server/client
  JS in one template file.
- Score Rationale: −10 for god-module navigation (homepage.js:670L embeds ~370L client
  app); −5 for magic strings.

### Simplicity — 76 (deductions: 24)

- Observations: duplication cluster F078 — escapeHtml/escapeXml near-verbatim
  (utils.js:151-172 vs sitemap.js:41-62), 6 status-label copies (utils.js:185,
  homepage.js:36/358/495, etl.js:91, data-quality.js:190), 3 required-fields lists
  (PageBuilder.js:10, school-page.js:75, data-schema.js:156 + etl.js:123), 3 province
  aggregations (PageBuilder.js:126, homepage.js:56, BuildOrchestrator.js:257), 3
  canonical-URL copies, 4 date-normalization copies, 2 cache-eviction copies.
- Evidence: explore subagent duplication/coupling inventory (full citation list in
  issue record F078).
- Impact / Risk: drift-prone constants (status/required-fields have already diverged:
  5-field vs 6-field lists), 3× maintenance on one concept.
- Score Rationale: −24 across the duplication cluster; core flow otherwise linear.

### Modularity & SRP — 68 (deductions: 32)

- Observations: good layering intent (scripts/services/presenters) and recent SRP work
  (SearchDataService/ExportService extraction, 2026-08-03 decision), but god-modules
  persist — BuildOrchestrator.js 556L (~12 responsibilities), homepage.js 670L
  (template + full client app), etl.js 451L, fetch-data.js 429L, data-quality.js 415L;
  circular cross-layer dependency scripts ↔ services ↔ presenters ↔ scripts
  (sitemap.js:19 → PageBuilder; BuildOrchestrator.js:50 → sitemap; PageBuilder.js:6-8
  → templates; templates → scripts/*); build-pages.js re-exports 15 functions (shim).
- Evidence: `wc -l` (matrix above), require-graph inventory (F082 record).
- Impact / Risk: change blast radius; template-layer import of script internals
  contradicts blueprint layer contract.
- Score Rationale: −20 god-modules (F008 pattern, re-verified: styles.js now 1296L);
  −12 circular coupling + re-export shim (F082).

### Consistency — 80 (deductions: 20)

- Observations: strong taxonomy consistency — IntegrationError + ERROR_CODES
  (resilience.js), fs-safe wrappers everywhere, terminate() exit-code discipline at all
  CLI entry points. Inconsistencies: interactive.js 28 raw console calls (F072),
  check-workflow-security.js uses console.error, cache caps disagree (F081:
  utils.js:149 `ESCAPE_HTML_CACHE_MAX = 50000` vs config.js `CACHE_DEFAULTS.MAX_CACHE_SIZE
= 10000`).
- Evidence: grep counts (F072/F081 records); explore error-handling inventory.
- Score Rationale: −10 observability inconsistency (F072); −10 config parity (F081).

### Testability — 85 (deductions: 15)

- Observations: 1061 JS tests (98 suites), 27 Python; coverage gates pass
  (94.95/92.32/96.65 vs 80/75/75); named exports + injected deps enable unit testing.
- Evidence: test:js / test:js:coverage / test:py outputs (matrix).
- Impact / Risk: F068 — pytest path dead (`No module named pytest`, no CI install);
  F010 — no e2e pipeline test (historical, P3).
- Score Rationale: −10 F068 dead alternate gate; −5 no e2e (F010).

### Maintainability (Complexity) — 70 (deductions: 30)

- Observations: 7 dead exports (F075: clearEscapeHtmlCache, hasCoordinateData,
  formatEmptyValue, escapeCsvField-external, validatePath-external, createFsSafe,
  resetCircuitBreakers); docs drift (F074 phantom api.md exports, F077 deployment.md
  mismatch, F080 test-count drift); CI prompt overcomplexity 2045L (F007 HELD).
- Evidence: node export dump + importer grep (F075), api.md section reads (F074).
- Score Rationale: −12 dead code; −10 docs drift; −8 CI surface (F007).

### Error Handling — 76 (deductions: 24)

- Observations (explore error-handling inventory): GOOD — resilience.js typed taxonomy,
  fs-safe retry/timeout/circuit-breaker with ENOENT/EEXIST handling, fetch-data bare
  `catch { throw IntegrationError }` (108), retry+circuit breaker+cache fallback
  (265/328/396), etl per-record rejected accounting (364), manifest wrap/rethrow
  (73/96), build failure enforcement (BuildOrchestrator 486). BAD — validate-links
  swallows non-IntegrationError (89-94, F073) and filters rejected results silently
  (148-151); enrichment.js:318 `catch { return {} }` swallows corrupt JSON with no log;
  terminate() drops stack/details (F070).
- Evidence: file:line citations in issue record F070/F073; explore agent report.
- Impact / Risk: masked validation failures; opaque incident diagnosis.
- Score Rationale: −10 F070 detail loss; −8 F073 swallowing; −6 enrichment silent
  degradation (debug-level log).

### Dependency Discipline — 78 (deductions: 22)

- Observations: single runtime dep (pino ^10.3.1); 0 vulnerabilities (npm audit);
  dev deps minimal (c8/eslint/husky/lint-staged/prettier). Risks: unpinned `curl -fsSL
https://opencode.ai/install | bash` in CI; actions/checkout@v7 + setup-node@v7
  (bleeding-edge major, unverified vs v4 mainstream); lint-staged@17.3.0 requires node
  ≥22.22.1 vs runtime 20 (F064).
- Evidence: package.json (deps: pino only), npm audit output, on-push.yml/on-pull.yml.
- Score Rationale: −12 unpinned/bleeding-edge installs; −10 engine mismatch (F064).

### Determinism & Predictability — 84 (deductions: 16)

- Observations: F066 RESOLVED — tests no longer wipe dist (run_tests.py:229-245 uses
  tempfile.mkdtemp); manifest version-gated (v1→v2 full rebuild once); incremental
  build deterministic via length-prefixed content hash; module-level CURRENT_YEAR /
  hoisted constants avoid per-call Date/regex drift.
- Evidence: source reads manifest.js, run_tests.py, build outputs consistent across 2
  consecutive builds.
- Impact / Risk: residual — build output depends on SITE_URL env (F006 placeholder
  leaks example.com into robots/sitemap when unset).
- Score Rationale: −16 for env-dependent output (F006) + fixture-scale dataset
  (2 schools) limits determinism verification at scale.

---

## B. SYSTEM QUALITY (RUNTIME) — 72.0/100 (C)

| Criterion                    | Weight | Score | Weighted |
| ---------------------------- | ------ | ----- | -------- |
| Stability                    | 20     | 82    | 16.40    |
| Performance Efficiency       | 15     | 90    | 13.50    |
| Security Practices           | 20     | 46    | 9.20     |
| Scalability Readiness        | 15     | 70    | 10.50    |
| Resilience & Fault Tolerance | 15     | 80    | 12.00    |
| Observability                | 15     | 69    | 10.35    |

### Stability — 82 (deductions: 18)

- Observations: build/tests 100% green; F066 resolved (no dist wipe — stability gain);
  F069 resolved (build aborts on failed pages). Residual: F063 orchestrator.yml 10/10
  failed runs; F076 new — on-pull.yml:44,51 `continue-on-error: true` on Checkout +
  Setup-Node masks infra failures.
- Evidence: matrix; on-pull.yml:44,51; gh run list (F063 record).
- Score Rationale: −12 F063 dead CI; −6 F076 masked infra failures.

### Performance Efficiency — 90 (deductions: 10)

- Observations: build 27ms / 74.07 pages/sec / Peak RSS 57.55MB; long optimization
  history (single-pass escapeHtml, async gzip level 6, province pre-grouping O(n),
  WeakMap path cache, flat-array schools.json, precomputed search text); perf budgets
  enforced (MAX_FAILED_PAGES now throws — F069).
- Evidence: build output (matrix), blueprint decision log 2026-06-08 → 2026-08-03.
- Impact / Risk: none material; dataset is fixture-scale (2 schools).
- Score Rationale: −10 unverifiable at production scale (2-school fixture).

### Security Practices — 46 (deductions: 54)

- Observations: app surface STRONG — escapeHtml applied at every interpolation
  (homepage.js:23/32/38/111/168/225, province-page.js:80-110, school-page.js),
  client search uses DOM API not innerHTML, CSP + X-Frame-Options + nosniff +
  Referrer-Policy + Permissions-Policy in head-meta.js:21-25 and public/_headers,
  slugify strips all non-alphanumerics (no traversal), 0 npm vulns. CI surface WEAK —
  F037/F038: 12 violations re-verified (2 CRITICAL DUPLICATE_API_KEY on-push.yml /
  parallel.yml, 4 HIGH ID_TOKEN_WRITE, 4 HIGH ACTIONS_WRITE_NON_MERGE, 2 HIGH
  GH_TOKEN_INSTEAD_OF_GITHUB_TOKEN); public repo; F004 historical 59-secret exposure.
- Evidence: `node scripts/check-workflow-security.js` (exit 1, 12 violations, 15th
  regression); grep escapeHtml/innerHTML/slugify; head-meta.js + _headers reads.
- Score Rationale: −34 CI workflow violations (F037/F038, 15th occurrence —
  SECURITY_AUDIT_NOTE.md claims fixed but files contradict); −10 F004 secret exposure;
  −10 F079 client CSV formula-injection divergence. App-surface hygiene caps the
  score at 46.

### Scalability Readiness — 70 (deductions: 30)

- Observations: concurrency limits (BUILD 100, VALIDATION 50), rate limiter with
  backpressure, incremental builds + manifest, flat-array search payload (13.2% size
  cut). Limits: CSV single-machine pipeline, no caching layer / DB (roadmap), fixture
  dataset (2 schools) never exercised at 3,474-scale.
- Evidence: config.js defaults, blueprint 2026-06-22 decisions, data/schools.csv count.
- Score Rationale: −15 no scale evidence (fixture only); −15 architecture ceiling
  (CSV→DB migration is roadmap-deferred).

### Resilience & Fault Tolerance — 80 (deductions: 20)

- Observations: comprehensive — fs-safe timeout/retry (3x, backoff 100ms→10s) /
  circuit breaker (5 failures, 60s), external-fetch retry + dedicated circuit breaker +
  fallback cache, rate-limiter queue timeout 30s, enrichment graceful degradation,
  ENOENT/EEXIST selective handling, manifest/PageBuilder wrap-rethrow.
- Evidence: explore error-handling inventory (resilience.js, fs-safe.js, fetch-data.js,
  etl.js citations); blueprint resilience section.
- Impact / Risk: F073 validate-links false-positives; F070 error-detail loss.
- Score Rationale: −12 F073; −8 F070 — pattern depth otherwise exemplary.

### Observability — 69 (deductions: 31)

- Observations: pino structured logging (level configurable), build performance report
  (duration/throughput/RSS/budgets), GITHUB_STEP_SUMMARY append (guarded try/catch),
  freshness/data-quality reports. Gaps: F072 interactive.js 28 raw console calls,
  check-workflow-security.js console.error bypass, F070 one-line error messages
  (no stack/details), enrichment failures logged at debug (invisible at INFO default).
- Evidence: grep counts (F072), logger.js, enrichment.js:215, utils.js terminate().
- Score Rationale: −12 F072; −10 F070; −9 enrichment debug-level hiding.

---

## C. EXPERIENCE QUALITY (UX / DX) — 72.5/100 (C)

Sub-criteria (equal weight, 9 items): Accessibility, User Flow Clarity, Feedback &
Error Messaging, Responsiveness (UX); API Clarity, Local Dev Setup, Documentation
Accuracy, Debuggability, Build/Test Feedback Loop (DX).

| Sub-criterion              | Weight | Score | Weighted |
| -------------------------- | ------ | ----- | -------- |
| Accessibility              | 11.1   | 82    | 9.10     |
| User Flow Clarity          | 11.1   | 82    | 9.10     |
| Feedback & Error Messaging | 11.1   | 75    | 8.33     |
| Responsiveness             | 11.1   | 78    | 8.66     |
| API Clarity                | 11.1   | 70    | 7.77     |
| Local Dev Setup            | 11.1   | 72    | 7.99     |
| Documentation Accuracy     | 11.1   | 68    | 7.55     |
| Debuggability              | 11.1   | 72    | 7.99     |
| Build/Test Feedback Loop   | 11.1   | 70    | 7.77     |

### Accessibility — 82

- Observations: WCAG 2.1 A claims; role="status" + aria-atomic copy feedback, aria-busy
  search loading, dark-mode autocomplete + color-scheme, non-color-only active state
  (inset box-shadow), nav landmarks, breadcrumbs.
- Evidence: blueprint 2026-08-03 a11y decisions (3 entries), school-page.js,
  homepage.js, styles.js reads.
- Score Rationale: −18 residual — no automated a11y test/CI gate (axe/Lighthouse absent).

### User Flow Clarity — 82

- Observations: homepage search + province/type/status filters, province index pages,
  per-school pages, breadcrumbs, back-to-top, canonical URLs.
- Evidence: README features, templates, build output structure.
- Score Rationale: −18 — dataset is 2-school fixture; real-flow validation impossible.

### Feedback & Error Messaging — 75

- Observations: copy "Tersalin!"/"Gagal menyalin" states, search loading state, stale
  data warnings. Gaps: F073 validation can report false "all links OK"; F070 one-line
  CLI errors without context.
- Evidence: F073/F070 records; homepage.js copy feedback.
- Score Rationale: −25 — masking bugs (F073) + minimal error context (F070).

### Responsiveness — 78

- Observations: responsive design tokens, mobile-first claims, dark mode.
- Evidence: styles.js (1296L), blueprint design-system entries.
- Score Rationale: −22 — no automated responsive/visual QA; static screenshots absent.

### API Clarity — 70

- Observations: api.md well-structured (module sections, export lists, usage examples);
  BUT F074 — documents non-existent homepage exports `aggregateByProvince` +
  `extractFilterOptions` (phantom) and omits 8 real exports (processInBatches,
  fastMkdir/fastWriteFile, removeOrphanedSchoolPages, isRelativeLink,
  getOrphanedSchoolPaths, etl.run, fetch-data.main).
- Evidence: api.md vs `node -e` export dump (F074 record).
- Score Rationale: −30 — phantom exports mislead consumers; undocumented exports
  hide API surface.

### Local Dev Setup — 72

- Observations: `npm install` + .env.example (7 vars) + README quickstart + devcontainer
  - pre-commit hooks. Gaps: F064 node engine mismatch (nvmrc 22 vs CI 20 vs
    lint-staged ≥22.22.1 — EBADENGINE observed), F068 pytest requires manual
    `pip install -r requirements.txt` (marked "Optional" in docs, but test:all fails
    without it).
- Evidence: npm install EBADENGINE warn; `python3 -m pytest --version` fail; setup.md.
- Score Rationale: −28 — engine + python setup friction.

### Documentation Accuracy — 68

- Observations: F077 — deployment.md:45 claims `dist/data/schools.json` but build emits
  `dist/data/schools.csv`; api.md Changelog claims "Version 2.0.0 (2026-07-20)" while
  "Current Version: 1.0.0" + package.json 1.0.0 + duplicate 1.2.0 entries; F080 —
  roadmap "902+ tests" vs testing.md "1030" vs actual 1061; F071 — 7 env vars
  consumed-but-undocumented; F005 — 88 docs files Prettier-drift (0 source files).
- Evidence: deployment.md:34-45, api.md changelog, roadmap.md, testing.md,
  format:check output.
- Score Rationale: −32 — 4 active docs-drift findings (F005/F071/F077/F080).

### Debuggability — 72

- Observations: pino structured logs, performance reports, freshness/quality reports,
  IntegrationError codes+details in most paths. Gaps: F070 terminate() drops stack +
  details; F072 console bypass unobservable; F073 masked validation failures.
- Evidence: F070/F072/F073 records; utils.js:285-292.
- Score Rationale: −28.

### Build/Test Feedback Loop — 70

- Observations: fast local build (27ms), coverage reports with gates, test:ci JSON
  output, fresh lint feedback. Gaps: F063 no live CI gate (orchestrator dead 10/10;
  on-pull.yml runs opencode loop, not lint+build+test gate); F068 pytest path dead;
  F076 continue-on-error masks setup failures.
- Evidence: matrix; on-pull.yml; gh run list.
- Score Rationale: −30 — CI feedback loop unreliable.

---

## D. DELIVERY & EVOLUTION READINESS — 54.5/100 (C−)

| Criterion                      | Weight | Score | Weighted |
| ------------------------------ | ------ | ----- | -------- |
| CI/CD Health                   | 20     | 37    | 7.40     |
| Release & Rollback Safety      | 20     | 40    | 8.00     |
| Config & Env Parity            | 15     | 33    | 4.95     |
| Migration Safety               | 15     | 68    | 10.20    |
| Technical Debt Exposure        | 15     | 58    | 8.70     |
| Change Velocity & Blast Radius | 15     | 70    | 10.50    |

### CI/CD Health — 37 (deductions: 63)

- Observations: on-push/on-pull run green; quality gates exist on on-push (lint +
  format:check). But F063 orchestrator.yml dead 10/10 (`secrets.GH_TOKEN` fictitious);
  no workflow runs lint+build+test as a hard gate; F076 continue-on-error:true masks
  checkout/node-setup failures; F068 pytest not wired; F021 pre-commit security gate is
  a no-op (`|| echo` swallow); F007 2045L of workflow YAML + embedded prompts.
- Evidence: gh run list (F063), on-pull.yml:44,51, .husky/pre-commit, wc -l workflows.
- Score Rationale: −30 F063; −13 F076; −10 F021; −10 F007 surface.

### Release & Rollback Safety — 40 (deductions: 60)

- Observations: no tags (git tag → 0), no release workflow (F011), no changelog
  automation; static-site regeneration is trivially rebuildable (rollback = rebuild),
  manifest versioning supports safe migration.
- Evidence: F011 record; git tag count; manifest.js.
- Score Rationale: −60 — no release/rollback machinery; only manual rebuild path.

### Config & Env Parity — 33 (deductions: 67)

- Observations: F071 — 7 env vars consumed but undocumented in .env.example
  (GITHUB_STEP_SUMMARY, EXTERNAL_DATA_DIR, PERF_MAX_BUILD_TIME_MS, PERF_MAX_FAILED_PAGES,
  PERF_MAX_MEMORY_BYTES, PERF_MIN_THROUGHPUT + build-concurrency family); F081 — cache
  caps disagree (50000 vs 10000); F064 — node engine three-way mismatch; F006 —
  SITE_URL placeholder leaks example.com.
- Evidence: grep process.env (15 reads / 14 non-test), .env.example (7), utils.js:149,
  config.js, .nvmrc, on-pull.yml:53.
- Score Rationale: −35 F071; −12 F081; −12 F064; −8 F006.

### Migration Safety — 68 (deductions: 32)

- Observations: manifest v1→v2 version gate (safe one-time rebuild), schema_version
  tracking, backward-compatible CSV format, incremental build manifest persistence,
  cache-keyed path changes safe.
- Evidence: manifest.js MANIFEST_VERSION, data-schema.js SCHEMA_VERSION, blueprint
  2026-08-03 decision.
- Score Rationale: −32 — no automated migration tests; CSV→PostgreSQL (roadmap)
  unmapped.

### Technical Debt Exposure — 58 (deductions: 42)

- Observations: F078 duplication cluster, F075 7 dead exports, F007 CI
  overcomplexity, F008 oversized files (styles.js 1296L), F074/F077/F080 docs drift,
  F072 console bypass, circular coupling (F082).
- Evidence: all F-records this run (73rd batch).
- Score Rationale: −42 across 8 active debt findings (mostly P3, none blocking).

### Change Velocity & Blast Radius — 70 (deductions: 30)

- Observations: fixes land fast and small (F066 #646, F069 #647, globals #648 all
  today); 1057-test safety net; atomic single-PR convention held 113 runs. Blast radius
  risks: god-modules + circular deps (F082) widen impact of any change.
- Evidence: git log (3 merged PRs today), test suite.
- Score Rationale: −30 — architectural coupling offsets velocity.

---

## Ledger delta vs 113th

RESOLVED: **F066** (dist-wipe — PR #646; tempfile isolation verified
run_tests.py:229-245), **F069** (build-exit-0 — PR #647; throw verified
BuildOrchestrator.js:486-497).
HELD (re-verified this run): F037/F038 (12 violations, 15th regression), F063 (10/10
fail), F068, F025, F073, F021, F018, F064, F070, F071, F072, F005 (88 files), F002
(issue creation blocked), F007, F008, F010, F011, F006, F004, F003, F012/F064, F009,
F014 (23rd clean), F024 (clean), F028 (clean), F017/F057 (clean).
NEW this run: F074 (api.md phantom/undocumented exports), F075 (7 dead source
exports), F076 (on-pull continue-on-error masking), F077 (deployment.md schools.json
vs schools.csv + api.md version inconsistency), F078 (duplication cluster),
F079 (homepage client CSV formula-injection gap), F080 (test-count drift 902/1030/1061),
F081 (cache caps 50000 vs 10000), F082 (circular cross-layer dependency + re-export
shim).

## Worktree state

`git status` clean before and after matrix; `dist/` rebuilt by build and intact after
test:py (F066 clean, 14 artifacts incl. sitemap-index.xml + sitemap-001.xml).
